---
wave: 3
depends_on: ['07-03', '07-04', '07-05']
files_modified:
  - frontend/package.json
  - frontend/playwright.config.ts
  - frontend/e2e/physics.spec.ts
  - frontend/src/App.tsx
  - frontend/src/store/simulationSlice.ts
autonomous: true
requirements_addressed: [DEBT-03, FIELD-01]
---

# Plan 07-06: Playwright E2E 测试基础设施

## Objective
引入 Playwright 端到端测试，在真实浏览器中加载 Rapier WASM，验证至少一个物理场景（弹簧振子）的正确性，作为重构安全网。

## Context
当前所有物理测试均为纯数值模拟（Nyquist 风格 jsdom 测试），未在真实浏览器中加载 Rapier WASM。Phase 7 涉及核心物理引擎集成层的重构（RigidBodyRefContext、ForceFieldSystem、VectorRenderer），需要 E2E 测试作为安全网。

Playwright 1.60.0 CLI 已可用，但 Chromium 浏览器未安装。

## Tasks

<task>
<id>1</id>
<description>安装 Playwright 依赖</description>
<read_first>
- frontend/package.json
</read_first>
<action>
1. `cd frontend && npm install -D @playwright/test`
2. `npx playwright install chromium`
3. 确认 `package.json` 中新增了 `@playwright/test` devDependency
</action>
<acceptance_criteria>
- `frontend/package.json` 包含 `"@playwright/test": "^1.60.0"` 或类似版本
- `npx playwright --version` 输出版本号
- Chromium 安装成功（`npx playwright install chromium` 无错误）
</acceptance_criteria>
</task>

<task>
<id>2</id>
<description>配置 playwright.config.ts</description>
<read_first>
- frontend/vite.config.ts（了解 dev server 配置）
</read_first>
<action>
创建 `frontend/playwright.config.ts`：
1. 使用 `http://localhost:5173` 作为 baseURL（Vite dev server）
2. 配置 `webServer` 字段自动启动 Vite dev server：`{ command: 'npm run dev', url: 'http://localhost:5173', timeout: 120000, reuseExistingServer: !process.env.CI }`
3. 配置 `projects` 仅使用 Chromium：`{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }`
4. 设置全局 timeout：`timeout: 60000`（WASM 初始化较慢）
5. 配置 `testDir: './e2e'`
</action>
<acceptance_criteria>
- `frontend/playwright.config.ts` 存在
- 文件包含 `baseURL: 'http://localhost:5173'`
- 文件包含 `testDir: './e2e'`
- 文件包含 `webServer` 配置启动 Vite dev server
</acceptance_criteria>
</task>

<task>
<id>3</id>
<description>App.tsx 暴露 WASM 就绪信号</description>
<read_first>
- frontend/src/App.tsx
- frontend/src/main.tsx（了解应用入口和 WASM 初始化顺序）
</read_first>
<action>
在 `App.tsx` 中（或 WASM 初始化完成后的回调中）设置全局就绪信号：
1. 在 App 组件的 `useEffect` 中，当确认 Rapier WASM 已加载后：
   ```typescript
   useEffect(() => {
     // WASM 初始化完成后设置就绪信号
     // @react-three/rapier 的 Physics 组件内部处理 WASM 加载
     // 可以通过检查 Physics 组件是否已渲染来判断
     (window as any).__APP_READY__ = true;
   }, []);
   ```
2. 更可靠的方案：监听 `@react-three/rapier` 的 `onInitialized` 回调（如果存在），或在 `Physics` 组件的 `children` 渲染后设置标志
3. 若 `@react-three/rapier` 没有直接的初始化回调，可以依赖 `Scene3D` 首次 `useEffect` 执行来设置标志

简化方案：在 `App.tsx` 中 `useEffect` 延迟设置：
```typescript
useEffect(() => {
  // 给 WASM 初始化留足时间（Physics 组件首次渲染后 WASM 开始加载）
  const timer = setTimeout(() => {
    (window as any).__APP_READY__ = true;
  }, 2000);
  return () => clearTimeout(timer);
}, []);
```

更精确的方案：在 `Scene3D.tsx` 中，使用 `useFrame` 首次执行时设置标志（确保 Physics world 已创建）。
</action>
<acceptance_criteria>
- `frontend/src/App.tsx` 或 `Scene3D.tsx` 中包含 `(window as any).__APP_READY__ = true`
- 信号在 WASM 初始化完成后设置（而非页面加载时立即设置）
</acceptance_criteria>
</task>

<task>
<id>4</id>
<description>创建弹簧振子 E2E 测试</description>
<read_first>
- frontend/src/ecs/__tests__/forceFieldCalc.test.ts（了解现有测试风格）
- frontend/src/store/presets.ts 或预设场景 JSON（了解弹簧振子预设）
</read_first>
<action>
创建 `frontend/e2e/physics.spec.ts`：
1. 每个 test 的 `beforeEach`：
   - `await page.goto('http://localhost:5173')`
   - `await page.waitForFunction(() => (window as any).__APP_READY__ === true, { timeout: 30000 })`
2. 测试用例 "spring oscillator loads and oscillates"：
   - 点击弹簧振子预设按钮（通过 `data-testid` 或按钮文字）
   - 点击 Play 按钮启动模拟
   - 等待 3 秒
   - 通过 `page.evaluate` 读取 body 位置数据：
     ```typescript
     const positions = await page.evaluate(async () => {
       const results: number[] = [];
       // 通过全局调试 API 或 store 读取
       const store = (window as any).__DEBUG__?.store || (window as any).useSimulationStore;
       // 或使用直接访问 RigidBody ref 的方式（需暴露全局 API）
       for (let i = 0; i < 60; i++) {
         await new Promise(r => setTimeout(r, 50));
         // 读取某个 entity 的 y 位置
         const y = (window as any).__DEBUG__?.getEntityPosition?.('mass-1')?.y ?? 0;
         results.push(y);
       }
       return results;
     });
     ```
3. 验证：positions 数组长度 = 60，存在至少 2 个峰值（证明在振荡）

**调试 API 暴露方案**：在 `Scene3D.tsx` 或 store 初始化时暴露：
```typescript
if (typeof window !== 'undefined') {
  (window as any).__DEBUG__ = {
    getEntityPosition: (id: string) => {
      const ref = getRef(id);
      const t = ref?.current?.translation();
      return t ? { x: t.x, y: t.y, z: t.z } : null;
    },
    store: useSimulationStore,
  };
}
```
</action>
<acceptance_criteria>
- `frontend/e2e/physics.spec.ts` 存在
- 文件包含 `page.waitForFunction(() => (window as any).__APP_READY__ === true)`
- 文件包含弹簧振子预设加载 + Play 按钮点击
- 文件包含位置采样和峰值检测断言
</acceptance_criteria>
</task>

<task>
<id>5</id>
<description>暴露全局调试 API</description>
<read_first>
- frontend/src/components/Scene3D.tsx
- frontend/src/store/index.ts（了解 store 导出方式）
</read_first>
<action>
在 `Scene3D.tsx` 中（或单独的 debug-bridge.ts 中）暴露全局调试 API：
1. 在 `Scene3DInner` 组件的 `useEffect` 中：
   ```typescript
   useEffect(() => {
     if (typeof window !== 'undefined') {
       (window as any).__DEBUG__ = {
         getEntityPosition: (id: string) => {
           const ref = getRef(id);
           const body = ref?.current;
           if (!body) return null;
           const t = body.translation();
           return { x: t.x, y: t.y, z: t.z };
         },
         getEntityVelocity: (id: string) => {
           const ref = getRef(id);
           const body = ref?.current;
           if (!body) return null;
           const v = body.linvel();
           return { x: v.x, y: v.y, z: v.z };
         },
         getStore: () => useSimulationStore.getState(),
       };
     }
   }, [getRef]);
   ```
2. 注意：仅在 `process.env.NODE_ENV === 'development'` 或 `process.env.E2E === 'true'` 时暴露，避免生产环境泄露内部状态
</action>
<acceptance_criteria>
- `Scene3D.tsx` 中包含 `(window as any).__DEBUG__` 的赋值
- `__DEBUG__` 对象包含 `getEntityPosition` 和 `getStore` 方法
- 仅在开发/E2E 环境下暴露（非生产环境）
</acceptance_criteria>
</task>

## Verification
- [ ] `cd frontend && npx playwright test e2e/physics.spec.ts` 通过
- [ ] 测试在 Chromium 中运行，加载 Rapier WASM 无错误
- [ ] 弹簧振子测试检测到位移振荡（至少 2 个峰值）

## must_haves
1. Playwright 安装并配置完成
2. E2E 测试能在真实浏览器中运行
3. 至少一个物理场景（弹簧振子）被验证
4. 全局调试 API 仅在开发环境暴露
