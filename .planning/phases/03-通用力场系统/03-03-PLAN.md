---
phase: 03-通用力场系统
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - frontend/src/components/ForceFieldDialog.tsx
  - frontend/src/store/uiSlice.ts
  - frontend/src/components/Toolbox.tsx
  - frontend/src/components/PropertyPanel.tsx
  - frontend/src/components/App.tsx
autonomous: true
requirements:
  - FIELD-03
must_haves:
  truths:
    - "用户点击 Toolbox 力场按钮 → ForceFieldDialog 打开，预选对应 kind"
    - "ForceFieldDialog 用 Zod+react-hook-form 验证，确认后创建 forceField 实体"
    - "PropertyPanel 检测到 forceField 组件 → 渲染力场参数编辑 UI"
    - "PropertyPanel 检测到 rigidBody 组件 → 渲染 charge 字段编辑"
    - "UI 状态（对话框开关、预选 kind）通过 uiSlice 管理"
  artifacts:
    - path: "frontend/src/components/ForceFieldDialog.tsx"
      provides: "力场创建/编辑对话框"
      exports:
        - "default"
    - path: "frontend/src/store/uiSlice.ts"
      provides: "力场 UI 状态"
      exports:
        - "openForceFieldDialog"
        - "closeForceFieldDialog"
    - path: "frontend/src/components/Toolbox.tsx"
      provides: "4 个力场按钮"
    - path: "frontend/src/components/PropertyPanel.tsx"
      provides: "力场参数 + charge 编辑"
  key_links:
    - from: "Toolbox.tsx"
      to: "uiSlice.ts openForceFieldDialog"
      via: "store action"
    - from: "ForceFieldDialog.tsx"
      to: "Entity.ts createForceFieldEntity"
      via: "import + 确认回调"
    - from: "PropertyPanel.tsx"
      to: "entitySlice.ts updateComponent"
      via: "力场参数变更回调"
---

<objective>
构建力场系统的完整 UI：创建对话框、工具箱按钮、属性面板编辑。

Purpose: 让用户通过界面创建、编辑、删除力场实体，并设置实体电荷。
Output: ForceFieldDialog + Toolbox 扩展 + PropertyPanel 扩展 + uiSlice 扩展
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-通用力场系统/03-CONTEXT.md
@.planning/phases/03-通用力场系统/03-RESEARCH.md
@frontend/src/components/CreationDialog.tsx
@frontend/src/components/Toolbox.tsx
@frontend/src/components/PropertyPanel.tsx
@frontend/src/store/uiSlice.ts
@frontend/src/components/App.tsx
</context>

<interfaces>
From frontend/src/store/uiSlice.ts (当前):
```typescript
export type ShapeType = 'sphere' | 'box' | 'cylinder' | 'slope';
export interface UiSlice {
  dialogOpen: boolean;
  dialogDefaultShape: ShapeType;
  openDialog: (shape: ShapeType) => void;
  closeDialog: () => void;
  // ...
}
```

From frontend/src/components/CreationDialog.tsx:
- 使用 zodResolver + react-hook-form + Dialog/Slider/Input/Label
- 模式：type selector → dynamic params → confirm

From frontend/src/components/PropertyPanel.tsx:
- 按组件类型分支：isSpring ? spring editor : shape editor
- 使用 updateComponent(entityId, componentType, partialData)
- PhysicsField / Vector3Field 可复用
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 UI Store + Toolbox</name>
  <files>frontend/src/store/uiSlice.ts, frontend/src/components/Toolbox.tsx</files>
  <read_first>
    - frontend/src/store/uiSlice.ts
    - frontend/src/components/Toolbox.tsx
    - frontend/src/components/App.tsx（确认 uiSlice 如何被合并到 store）
  </read_first>
  <action>
    1. 在 uiSlice.ts 中新增力场对话框状态（D-03-04 / D-03-05）：
       - `forceFieldDialogOpen: boolean`
       - `forceFieldDialogKind: ForceFieldKind | null`
       - `openForceFieldDialog: (kind: ForceFieldKind) => void` — 设置 open=true + kind
       - `closeForceFieldDialog: () => void` — 设置 open=false + kind=null
       - 导入 ForceFieldKind 类型（从 types.ts）
    2. 在 Toolbox.tsx 弹簧按钮下方新增分隔线 + 4 个力场图标按钮（D-03-05）：
       - 均匀方向场：ArrowUp（lucide-react）
       - 点引力源：Crosshair
       - 点电荷电场：Zap
       - 均匀磁场：Magnet
       - 点击调用 `openForceFieldDialog(kind)`
       - 样式与现有形状按钮一致（w-10 h-10, 相同 hover/active 样式）
    3. 确认 App.tsx 中 Toolbox 已挂载，无需额外修改（但 ForceFieldDialog 稍后添加）。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <done>
    - `grep -c "forceFieldDialogOpen" frontend/src/store/uiSlice.ts` >= 1
    - `grep -c "openForceFieldDialog" frontend/src/store/uiSlice.ts` >= 1
    - `grep -c "ArrowUp\|Crosshair\|Zap\|Magnet" frontend/src/components/Toolbox.tsx` >= 4
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 2: ForceFieldDialog 组件</name>
  <files>frontend/src/components/ForceFieldDialog.tsx</files>
  <read_first>
    - frontend/src/components/CreationDialog.tsx
    - frontend/src/ecs/types.ts
    - frontend/src/ecs/Entity.ts
    - frontend/src/store/uiSlice.ts（Task 1 修改后）
  </read_first>
  <action>
    1. 创建 `frontend/src/components/ForceFieldDialog.tsx`，复用 CreationDialog 架构（Zod + react-hook-form + Dialog）：
       - Schema：用 z.discriminatedUnion('kind', [...]) 定义 4 种 kind 的字段验证
         - 通用字段：position[x,y,z]（默认 [0,5,0]）、range（0.1-100）
         - uniform：direction[x,y,z]、strength（-1000~1000）
         - gravity：strength（0~10000）、decay（boolean）
         - electric：charge（-100~100）、decay（boolean）
         - magnetic：direction[x,y,z]、strength（0~1000）
       - UI 布局：
         - 顶部：kind 选择器（4 个图标按钮，类似 CreationDialog 的形状选择器）
         - 通用参数区：位置 Vector3 输入 + range Slider
         - 类型专用参数区（根据 kind 动态显示）
         - 底部：确认/取消按钮
       - 确认回调：
         - 调用 `createForceFieldEntity(kind, position, range, params)`
         - `addEntity(entity)` — 复用 store 的 addEntity
         - 若 MAX_ENTITIES 超限，显示 root error
         - 关闭对话框
    2. 在 App.tsx 中挂载 `<ForceFieldDialog />`（与 CreationDialog 同级）。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <done>
    - `grep -c "ForceFieldDialog" frontend/src/components/App.tsx` >= 1
    - `grep -c "z.discriminatedUnion" frontend/src/components/ForceFieldDialog.tsx` >= 1
    - `grep -c "createForceFieldEntity" frontend/src/components/ForceFieldDialog.tsx` >= 1
    - `grep -A2 -B2 "ForceFieldDialog" frontend/src/components/App.tsx` 输出包含 "<ForceFieldDialog" 且与 "<CreationDialog" 在相近 JSX 层级
    - TypeScript 编译无错误
  </done>
</task>

<task type="auto">
  <name>Task 3: PropertyPanel 力场 + charge 编辑</name>
  <files>frontend/src/components/PropertyPanel.tsx</files>
  <read_first>
    - frontend/src/components/PropertyPanel.tsx
    - frontend/src/ecs/types.ts
  </read_first>
  <action>
    1. 在 PropertyPanel 的分支逻辑中新增力场实体分支（D-03-06）：
       - 在 `isSpring ? ... : (...)` 之前插入 `isForceField` 分支
       - 检测：`entity.components.has('forceField')`
       - 力场分支渲染：
         - 类型显示（不可编辑，显示 kind 中文名）
         - 强度 Slider（uniform/magnetic: strength; gravity: strength; electric: charge）
         - 范围 Input（range）
         - 方向 Vector3 输入（uniform/magnetic 的 direction）
         - 衰减 Switch（gravity/electric 的 decay）
         - 删除按钮
       - 所有变更通过 `updateComponent(entityId, 'forceField', partial)` 更新
    2. 在形状实体分支的物理参数区（mass/restitution/friction 下方）新增 charge 字段（D-03-06）：
       - PhysicsField，label="电荷"，unit="C"，min=-10，max=10，step=0.1
       - 变更通过 `updateComponent(entityId, 'rigidBody', { charge: val })`
    3. 确保运行中（disabled=true）时所有字段只读（复用现有 disabled 模式）。
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit --skipLibCheck</automated>
  </verify>
  <done>
    - `grep -c "forceField" frontend/src/components/PropertyPanel.tsx` >= 3
    - `grep -c "charge" frontend/src/components/PropertyPanel.tsx` >= 2
    - `grep -c "updateComponent.*forceField" frontend/src/components/PropertyPanel.tsx` >= 1
    - `grep -c "updateComponent.*rigidBody.*charge" frontend/src/components/PropertyPanel.tsx` >= 1
    - TypeScript 编译无错误
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 -> Zod Schema | 对话框输入在提交前经 Zod 校验 |
| UI State -> Entity Store | 仅通过 addEntity/updateComponent 修改，不可直接赋值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | 力场参数越界 | mitigate | Zod schema 限制 range/strength/charge 范围；Slider 组件限制 min/max |
| T-03-07 | Denial of Service | 快速连续创建力场 | accept | MAX_ENTITIES=50 全局限制已在 entitySlice 中硬编码 |
| T-03-08 | Information Disclosure | 力场参数泄露 | accept | 纯客户端应用，无敏感数据 |
</threat_model>

<verification>
- TypeScript 编译通过
- Toolbox 渲染 4 个力场图标按钮
- ForceFieldDialog 打开/关闭/确认流程可用
- PropertyPanel 力场分支和 charge 字段可编辑
</verification>

<success_criteria>
- Toolbox 有 4 个力场按钮，点击打开 ForceFieldDialog 并预选 kind
- ForceFieldDialog 用 Zod+react-hook-form 验证，确认后创建力场实体
- PropertyPanel 对力场实体显示专用参数编辑器
- PropertyPanel 对刚体实体显示 charge 字段编辑器
- 运行中所有字段自动变为只读
- TypeScript 编译无错误
</success_criteria>

<output>
After completion, create `.planning/phases/03-通用力场系统/03-03-SUMMARY.md`
</output>
