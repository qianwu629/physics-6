---
phase: 03-通用力场系统
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/ecs/types.ts
  - frontend/src/ecs/Entity.ts
  - frontend/src/ecs/__tests__/Entity.test.ts
  - frontend/src/ecs/__tests__/types.test.ts
autonomous: true
requirements:
  - FIELD-01
must_haves:
  truths:
    - "ComponentType 联合包含 'forceField'"
    - "RigidBodyComponent 包含 charge 字段（默认 0）"
    - "ForceFieldComponent 使用 discriminated union：kind + 各类型专用字段"
    - "Entity 工厂提供 createForceFieldEntity()，生成 transform + forceField 轻量实体"
    - "现有形状工厂（createSphereEntity 等）生成的实体包含 charge=0"
  artifacts:
    - path: "frontend/src/ecs/types.ts"
      provides: "扩展的 ComponentType / ForceFieldComponent / RigidBodyComponent"
      exports:
        - "ForceFieldKind"
        - "ForceFieldComponent"
        - "UniformFieldComponent"
        - "GravityFieldComponent"
        - "ElectricFieldComponent"
        - "MagneticFieldComponent"
    - path: "frontend/src/ecs/Entity.ts"
      provides: "createForceFieldEntity 工厂 + charge 默认注入"
      exports:
        - "createForceFieldEntity"
  key_links:
    - from: "Entity.ts createForceFieldEntity"
      to: "types.ts ForceFieldComponent"
      via: "import"
    - from: "Entity.ts createSphereEntity"
      to: "RigidBodyComponent.charge"
      via: "charge: 0 默认值"
---

<objective>
扩展 ECS 类型系统与实体工厂，引入 ForceField 组件和 RigidBody charge 字段，为后续力场计算、UI 和可视化提供数据契约。

Purpose: 建立 Phase 3 所有子系统的共享类型基础；不触碰渲染或物理计算。
Output: 类型定义 + 工厂函数 + 单元测试更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-通用力场系统/03-CONTEXT.md
@.planning/phases/03-通用力场系统/03-RESEARCH.md
@frontend/src/ecs/types.ts
@frontend/src/ecs/Entity.ts
@frontend/src/ecs/__tests__/Entity.test.ts
@frontend/src/ecs/__tests__/types.test.ts
</context>

<interfaces>
From frontend/src/ecs/types.ts (当前):
```typescript
export type ComponentType = 'transform' | 'rigidBody' | 'collider' | 'velocity' | 'material' | 'constraint' | 'trail' | 'vector';
export interface RigidBodyComponent extends Component {
  type: 'rigidBody';
  kind: RigidBodyKind;
  mass: number;
  restitution: number;
  friction: number;
}
```

From frontend/src/ecs/Entity.ts (当前):
```typescript
export function createEntity(id: string, name: string, components: Component[]): Entity
export function createSphereEntity(radius, mass, restitution, friction, color?, velocity?, position?): Entity
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 ECS 类型定义</name>
  <files>frontend/src/ecs/types.ts</files>
  <read_first>
    - frontend/src/ecs/types.ts
    - frontend/src/ecs/__tests__/types.test.ts
  </read_first>
  <action>
    1. 在 ComponentType 联合中追加 `'forceField'`（D-03-01）。
    2. 在 RigidBodyComponent 中新增 `charge: number` 字段，默认 `0`（D-03-02）。
    3. 新增 ForceField 类型族（D-03-03，参照 ColliderComponent 的 shape discriminated union）：
       - `export type ForceFieldKind = 'uniform' | 'gravity' | 'electric' | 'magnetic'`
       - `BaseForceFieldComponent`：type='forceField', kind, position:[n,n,n], range:number
       - `UniformFieldComponent`：kind='uniform', direction:[n,n,n], strength:number
       - `GravityFieldComponent`：kind='gravity', strength:number, decay:boolean
       - `ElectricFieldComponent`：kind='electric', charge:number, decay:boolean
       - `MagneticFieldComponent`：kind='magnetic', direction:[n,n,n], strength:number
       - `export type ForceFieldComponent = UniformFieldComponent | GravityFieldComponent | ElectricFieldComponent | MagneticFieldComponent`
    4. 将 ForceFieldComponent 追加到 AnyComponent 联合中。
    5. 更新 types.test.ts：
       - 添加 ForceFieldComponent 类型守卫测试（确认 kind 分发）
       - 添加 RigidBodyComponent charge 字段存在测试
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/ecs/__tests__/types.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - `grep -c "forceField" frontend/src/ecs/types.ts` >= 5
    - `grep -c "charge" frontend/src/ecs/types.ts` >= 1
    - `grep -c "ForceFieldKind" frontend/src/ecs/types.ts` >= 1
    - types.test.ts 全部通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展实体工厂</name>
  <files>frontend/src/ecs/Entity.ts</files>
  <read_first>
    - frontend/src/ecs/Entity.ts
    - frontend/src/ecs/__tests__/Entity.test.ts
    - frontend/src/ecs/types.ts（Task 1 修改后）
  </read_first>
  <action>
    1. 在所有形状工厂（createSphereEntity, createBoxEntity, createCylinderEntity, createSlopeEntity）的 RigidBodyComponent 中显式加入 `charge: 0`（D-03-02）。
    2. 新增 `createForceFieldEntity(kind, position, range, params)` 工厂函数（D-03-01）：
       - 使用全局 `nextNumber()` 计数器生成 ID：`forcefield-${n}`
       - name 格式：`力场-${kind}-${n}`
       - 组件：transform（position 传入值，rotation=[0,0,0], scale=[1,1,1]）+ forceField（kind + position + range + ...params）
       - 返回 `createEntity(...)` 结果
    3. 更新 Entity.test.ts：
       - 测试 createForceFieldEntity 返回的实体 ID 以 `forcefield-` 开头
       - 测试实体 components Map 包含 transform 和 forceField
       - 测试 createSphereEntity 返回的 rigidBody 组件 charge === 0
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/ecs/__tests__/Entity.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - `grep -c "createForceFieldEntity" frontend/src/ecs/Entity.ts` >= 1
    - `grep -c "charge: 0" frontend/src/ecs/Entity.ts` >= 4
    - Entity.test.ts 全部通过
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 类型定义 -> 运行时 | 类型系统仅编译时约束，运行时需工厂函数保证字段完整 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Entity 工厂参数 | mitigate | 工厂函数使用 Zod schema 在 UI 层校验，类型层保证字段名正确 |
| T-03-02 | Denial of Service | 实体计数器溢出 | accept | JavaScript number 安全整数范围 (~9e15) 远超场景需求 |
</threat_model>

<verification>
- vitest types.test.ts + Entity.test.ts 全部通过
- grep 验证 types.ts 包含 forceField / charge / ForceFieldKind
- grep 验证 Entity.ts 包含 createForceFieldEntity / charge: 0
</verification>

<success_criteria>
- ComponentType 联合包含 'forceField'
- RigidBodyComponent 包含 charge 字段
- ForceFieldComponent 为 discriminated union，4 种 kind 各有独立字段
- createForceFieldEntity 工厂可用，生成轻量实体（无 collider/rigidBody）
- 所有现有形状实体默认 charge=0
- 单元测试全部通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-通用力场系统/03-01-SUMMARY.md`
</output>
