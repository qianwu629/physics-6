---
context: phase
phase: 01.1-ui
phase_stage: discuss
task: 5
total_tasks: 10
status: in_progress
last_updated: 2026-05-09T16:24:01.207Z
---

# Phase 01.1 — UI 重构 · 会话恢复指南

> 上次会话上下文用尽（79%）于 2026-05-09 暂停。`/clear` 后由 `/gsd-resume-work` 或 `/gsd-discuss-phase 01.1` 进入；本文件配套 `.discuss-progress.md` 共同提供完整恢复上下文。

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| 跳过 ui-ux-pro-max 直接产出 UI 方案 | 仅凭通用经验给"凑合用"方案，忽略系统化设计原则、a11y、视觉层级一致性 | blocking | 在 present_gray_areas 之前必须先成功调用 `python "C:\\Users\\ASUS\\.claude\\skills\\ui-ux-pro-max\\scripts\\search.py" "<query>" --design-system`；上次因 cwd 与文档示例路径不一致导致 FileNotFoundError，需用绝对路径或先 cd |
| 把 Phase 2 数据层 bug 拉进 01.1 | C-04..C-07（peReferenceY 重复 / buffer 泄漏 / chart.update 数据缺口）属功能 bugfix 不是 UI 重构 | blocking | discuss 时若用户提到这些 bug，路由到 Phase 02 review-fix，写入 deferred；01.1 只接 UI 范畴的 C-02/C-03/W-01/W-02 |
| 用 [data-toolbar]{top:44px !important} 这类全局 CSS 注入修图层冲突 | App.tsx 当前 useEffect 注入 `<style>` 强行下移 Toolbar，是脏 hack | advisory | 设计系统/布局栅格定型后，由 Toolbar 自身用 `top` token 或 grid 区域承担，删除注入逻辑 |
| ROADMAP Goal 仍是 `[Urgent work - to be planned]` 就直接进入 plan/execute | Phase 01.1 是 INSERTED 占位，未锁定 Goal/Requirements 即下游 | blocking | 本次 discuss 必须以"锁 Goal/Requirements/范围红线"为首要交付物，写回 `01.1-CONTEXT.md` 并回填 ROADMAP |

## Required Reading（按顺序）

1. `.planning/phases/01.1-ui/.discuss-progress.md` — 本会话所有候选灰色地带（G-1..G-5）、代码扫描摘要、范围红线 — **首要读物**
2. `.planning/ROADMAP.md` — 验证 Phase 01.1 块当前仍是 INSERTED 占位
3. `.planning/REQUIREMENTS.md` — 21 项 v2.0 需求；01.1 是 UI 重构非新能力，不应新增
4. `.planning/PROJECT.md` — 用户为高中生 · 教育/实验场景 · 技术栈 React 19 + Tailwind v4 + shadcn/ui + R3F + Rapier3D
5. `.planning/phases/01-持久化与场景库/01-CONTEXT.md` — 已锁定的上游 UI 决策（D-01-04 MenuBar / D-01-05 右侧 Drawer 快照 / D-01-08 三级错误反馈）
6. `.planning/phases/02-实时物理量图表/02-CONTEXT.md` — 已锁定的图表 UI 决策（D-02-02 浮动 ChartPanel / D-02-03 指标优先配色 / D-02-09 PropertyPanel 追踪开关）
7. `.planning/phases/02-实时物理量图表/02-REVIEW.md` — Phase 2 7 Critical/9 Warning bug 列表，需在 discuss 时区分"UI 范畴"vs"数据层范畴"
8. `frontend/src/components/App.tsx` — 当前 8 浮层布局 + Toolbar CSS 注入 hack 的源头
9. `frontend/src/components/Toolbox.tsx` — 视觉系统现状（rgba(26,26,26,0.85) + backdrop-blur + 40×40 触控目标）

## Infrastructure State

- **代码状态:** Phase 1 已合并；Phase 2 代码已落地但 7 Critical 未修、UAT 14 项未跑（不阻塞 01.1 讨论）
- **未提交文件:** ROADMAP.md (M), STATE.md (M), `.planning/phases/02-.../.review-fix-recovery-pending.json` (M), `01-REVIEW-FIX.md` (??), `01.1-ui/.gitkeep` (??), `01.1-ui/.discuss-progress.md` (??), `temp_review_scope.js` (??)
- **背景进程:** 无（dev server 未起）
- **memory 系统:** 已写入 `feedback_frontend_skills.md` 并在 MEMORY.md 注册——Claude 启动即应自动加载，遇 UI/UX 工作必走 ui-ux-pro-max
- **ui-ux-pro-max 路径警示:** SKILL.md 文档示例 `python3 skills/ui-ux-pro-max/scripts/search.py` 仅在 cwd=skill 根目录时成立。Physis 项目 cwd 是 `D:\cty\my_code\physics4`，必须使用绝对路径：

  ```
  python "C:\Users\ASUS\.claude\skills\ui-ux-pro-max\scripts\search.py" "physics simulation tool workbench multi-panel desktop" --design-system -p "Physis Phase 01.1"
  ```

<current_state>
Phase 01.1 处于 discuss 阶段第 5/10 步（已完成 prior-context 加载、codebase 扫描、灰色地带识别、用户偏好持久化、进度暂存）。下一步是先成功调用 ui-ux-pro-max 取得专业框架，再以 G-1 单独一轮 AskUserQuestion 锁住 Phase Goal——因为 ROADMAP 当前 Goal 为 `[Urgent work - to be planned]` 占位，不锁 Goal 不能进 researcher/planner。
</current_state>

<completed_work>

- Task 1: load_prior_context — Done（PROJECT/REQUIREMENTS/STATE/01-CONTEXT/02-CONTEXT/02-REVIEW 全读完）
- Task 2: scout_codebase — Done（44 个 .tsx 分类、App.tsx 8 浮层布局、Toolbox 视觉基线、识别脏 hack）
- Task 3: analyze_phase — Done（识别 5 个候选灰色地带 G-1..G-5）
- Task 4: save user feedback memory — Done（`memory/feedback_frontend_skills.md` + MEMORY.md 索引）
- Task 5: write discuss-progress — Done（`.planning/phases/01.1-ui/.discuss-progress.md` 9.3KB）
</completed_work>

<remaining_work>

- Task 6: ui-ux-pro-max skill 调用 — 用绝对路径成功跑通 `--design-system` 取得设计系统建议
- Task 7: present_gray_areas — 第一轮 AskUserQuestion 仅 G-1（重构焦点范围）multiSelect 6 选项；不要把 G-2..G-5 一起抛给用户
- Task 8: discuss_areas — 根据 G-1 答案，按需追问 G-2(设计系统形态)/G-3(布局范式)/G-4(视觉风格)/G-5(多态适配)
- Task 9: write_context — 产出 `01.1-CONTEXT.md`（决策列表 + 范围红线 + canonical_refs）+ `01.1-DISCUSSION-LOG.md`
- Task 10: git commit + ROADMAP 回填（Goal/Requirements 写实）+ STATE.md 更新
</remaining_work>

<decisions_made>

- 必须先锁 Goal 再讨论技术形态——G-1 是单独一轮，不能与 G-2..G-5 合并
- ui-ux-pro-max 是 Physis 前端工作强制 skill（写入持久 memory）
- 范围红线：Phase 2 数据层 bug（C-04..C-07）不进 01.1，新物理能力不进 01.1
- 候选灰色地带保留 5 个：G-1 焦点范围 / G-2 设计系统形态 / G-3 布局范式 / G-4 视觉风格 / G-5 多态适配
</decisions_made>

<blockers>

- 上下文耗尽（79%）→ workaround: /clear 后重启 discuss，靠本文件 + .discuss-progress.md 跳过前 3 步
- ui-ux-pro-max search.py 路径不与文档示例一致 → workaround: 用绝对路径 `C:\Users\ASUS\.claude\skills\ui-ux-pro-max\scripts\search.py`
</blockers>

<context>
讨论起点的信号极薄："ui重构" + "[Urgent work - to be planned]"。已在代码里看清浮层散乱 / 脏 CSS 注入 / 触控目标欠缺 / 配色未 token 化 / Phase 2 浮层 bug 等具体债务，因此候选灰色地带从"是否要重构"上升到"重构到哪一层"。技术倾向：(b) shadcn 设计 token + (c) 混合栅格布局（核心面板栅格 + ChartPanel 仍浮）+ (a) 维持深色玻璃态 + token 化——但 G-1 用户必须落锤，不能由 Claude 替选。
</context>

<next_action>
1. Read `.planning/phases/01.1-ui/.discuss-progress.md` 完整文件（9.3KB，含候选灰色地带 G-1..G-5 全文）
2. 用绝对路径调用 `python "C:\\Users\\ASUS\\.claude\\skills\\ui-ux-pro-max\\scripts\\search.py" "physics simulation tool workbench multi-panel desktop dark" --design-system -p "Physis Phase 01.1"`，吸收输出
3. 直接进 present_gray_areas，第一轮 AskUserQuestion：仅 G-1（重构焦点范围），multiSelect=true，6 选项 (a) Phase 2 浮层缺陷修复 / (b) 统一设计系统 / (c) 信息架构重组 / (d) a11y 基线 / (e) Phase 3+5 前置准备 / (f) PropertyPanel 折叠态重做
4. 据答案启动 G-2..G-5 二轮（按需）
5. 写 `01.1-CONTEXT.md`（含 Goal / Requirements 字段 / 决策表 / canonical_refs / scope_guardrail）+ `01.1-DISCUSSION-LOG.md`
6. 回填 ROADMAP.md 的 Phase 01.1 块（Goal 与 Requirements）
7. `gsd-sdk query commit "feat(01.1): lock Goal and Requirements via discuss-phase" --files <list>` 并 update_state
</next_action>
