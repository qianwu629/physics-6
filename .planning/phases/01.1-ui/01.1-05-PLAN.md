---
phase: "01.1"
plan: "05"
type: execute
wave: 2
depends_on:
  - "01.1-01"
  - "01.1-03"
files_modified:
  - frontend/src/components/Toolbox.tsx
  - frontend/src/components/Toolbar.tsx
autonomous: true
requirements:
  - UI-04
  - UI-08

must_haves:
  truths:
    - "Toolbox 从屏幕中央浮层变为左列垂直列表（220px 宽，bg-sidebar 背景）"
    - "Toolbox 按钮尺寸从 w-10 h-10 升级为 w-11 h-11（a11y 44×44px 触控目标）"
    - "Toolbox 末尾增加 '+ 力场' 按钮（disabled + tooltip 'Phase 3 解锁'）"
    - "Toolbar 从顶部浮层变为底部状态条横跨全宽（h-10, bg-card 背景）"
    - "Toolbar 移除 data-toolbar 属性和 fixed / top-4 / left-1/2 定位"
    - "Toolbar 增加 CameraModeToggle（Orbit ↔ FPS, disabled）和 DimensionToggle（3D ↔ 2D, disabled）"
    - "Toolbar 所有图标按钮尺寸升级为 w-11 h-11（a11y 触控目标）"
  artifacts:
    - path: "frontend/src/components/Toolbox.tsx"
      provides: "左列工具列（适配网格容器，移除 fixed 定位）"
      contains: "+ 力场"
    - path: "frontend/src/components/Toolbar.tsx"
      provides: "底部状态条（适配网格容器，移除浮层定位）"
      contains: "CameraModeToggle"
  key_links:
    - from: "frontend/src/components/Toolbox.tsx"
      to: "frontend/src/index.css"
      via: "w-11 h-11 (44px 触控目标)"
      pattern: "w-11 h-11"
    - from: "frontend/src/components/Toolbar.tsx"
      to: "frontend/src/components/ui/toggle-group.tsx"
      via: "ToggleGroup for CameraMode + DimensionMode"
      pattern: "ToggleGroup"
---

<objective>
将 Toolbox 从左中浮层改造为左列垂直工具列表，将 Toolbar 从顶部浮层改造为底部状态条，新增 Phase 3（+力场按钮）和 Phase 5（摄像机/2D Toggle）预留入口。

Purpose: 实现 D-01.1-08（Toolbar 底部下沉）和布局合规（Toolbox/Toolbar 不再使用 fixed 定位），完成 UI-08 Phase 3/5 入口预留。
Output: 重构后的 Toolbox.tsx + Toolbar.tsx（移除 fixed 定位，适配网格容器，新增预留入口）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-ui/01.1-CONTEXT.md
@frontend/src/components/Toolbox.tsx
@frontend/src/components/Toolbar.tsx
@frontend/src/components/ui/toggle.tsx
@frontend/src/components/ui/toggle-group.tsx
@frontend/src/components/WorkspaceLayout.tsx

<interfaces>
From 01.1-UI-SPEC.md §9 (Component Inventory):
- Toolbox: 左侧浮层 → 左列 grid col 1, rgba → bg-sidebar / hover bg-accent
- Toolbar: 顶部浮层 + CSS 注入 hack → 底部状态条 grid row 3, 拆除 App.tsx 注入

From 01.1-UI-SPEC.md §10 (Phase 3/5 Entry Points):
- 创建力场对话框入口: Toolbox 末尾增加 "+ 力场" 按钮, disabled + tooltip "Phase 3 解锁"
- 摄像机模式切换: Toolbar 中 CameraModeToggle (toggle-group: Orbit ↔ FPS), disabled + tooltip "Phase 5 解锁"
- 3D/2D 模式切换: Toolbar 中 DimensionToggle (toggle-group: 3D ↔ 2D), disabled + tooltip "Phase 5 解锁"
- 触控控件预留区: Toolbar 右侧 reserved slot (<div className="hidden lg:block w-0" data-touch-reserve />)

From 01.1-UI-SPEC.md §8 (a11y):
- 所有可点击元素 ≥ 44×44px — 图标按钮统一 w-11 h-11
- icon-only 按钮必须 aria-label
- Toggle 类按钮须 aria-pressed

Landmine L-10: disabled button + tooltip keyboard 不可达 → 使用 aria-disabled + focusable 模式
Landmine P1-A3: icon-only button 必须 aria-label
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重构 Toolbox.tsx — 左列垂直工具列表 + 力场按钮预留</name>
  <read_first>
  - frontend/src/components/Toolbox.tsx
  - .planning/phases/01.1-ui/01.1-UI-SPEC.md
  </read_first>
  <files>frontend/src/components/Toolbox.tsx</files>
  <action>
重写 Toolbox.tsx 使其适配左列网格容器，并进行以下变更：

**A) 移除浮层定位，改为网格子元素**

删除最外层 div 的 fixed 定位样式。替换为：
```tsx
<div className="h-full flex flex-col items-center gap-2 p-2 select-none overflow-y-auto
  bg-[hsl(var(--sidebar))] border-r border-[hsl(var(--border))]">
```

移除：`left: '16px'`, `top: '50%'`, `transform: 'translateY(-50%)'`, `background: 'rgba(26, 26, 26, 0.85)'`, `backdropFilter`, `WebkitBackdropFilter`, `border`, `boxShadow`。

**B) 升级按钮尺寸为 w-11 h-11（a11y 44×44px 触控目标）**

所有形状按钮（SHAPES 映射的 4 个按钮）和折叠/弹簧按钮的 className 中：
- `w-10 h-10` → `w-11 h-11`
- `rounded-lg` 保持
- `aria-label` 保持

**C) 折叠按钮改为独立项**

展开状态下的 X 折叠按钮保留（w-8 h-8 不变——非触控目标的辅助按钮，小尺寸合宜）。折叠后的 + 按钮 w-11 h-11。

**D) 追加 Phase 3 力场按钮预留（UI-08）**

在弹簧按钮（Link2）之后、折叠 X 按钮之前，插入力场预留按钮：

```tsx
{/* 力场按钮 — Phase 3 预留 (UI-08) */}
<div className="relative" title="力场系统 (Phase 3 解锁)">
  <button
    type="button"
    aria-label="添加力场"
    aria-disabled="true"
    className="flex items-center justify-center w-11 h-11 rounded-lg
      text-[hsl(var(--muted-foreground)/0.3)] bg-transparent
      cursor-not-allowed
      transition-all duration-150"
    tabIndex={-1}
  >
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z" />
    </svg>
  </button>
</div>
```

按钮使用 `aria-disabled="true"` + `tabIndex={-1}` 满足 L-10（keyboard 不可达但视觉上 disabled）。使用 lucide `Zap` icon 或直接内联 SVG（如果 lucide 没有合适的力场图标）。

**E) Token 迁移**

所有硬编码颜色替换为 CSS 变量：
- `'#a0a0a0'` → `'hsl(var(--muted-foreground))'`
- `'#3b82f6'` → `'hsl(var(--primary))'`
- `'#666'` → `'hsl(var(--muted-foreground) / 0.6)'`
- `'rgba(59,130,246,0.15)'` → `'hsl(var(--primary) / 0.15)'`
- `'rgba(59,130,246,0.2)'` → `'hsl(var(--primary) / 0.2)'`
- `'rgba(59,130,246,0.3)'` → `'hsl(var(--primary) / 0.3)'`
- `'rgba(255,255,255,0.05)'` → `'hsl(var(--accent))'`
- `'rgba(255,255,255,0.08)'` (分隔线) → `'hsl(var(--border))'`
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c 'fixed' src/components/Toolbox.tsx | grep -c '0' &amp;&amp; grep -c 'w-11 h-11' src/components/Toolbox.tsx | grep -E '^[4-9]|^[1-9][0-9]' &amp;&amp; grep -c '力场' src/components/Toolbox.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'rgba(' src/components/Toolbox.tsx | grep -c '0' &amp;&amp; echo "TOOLBOX_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Toolbox.tsx 不含 `fixed` 定位（grep 0 命中）
    - Toolbox.tsx 不含 `rgba(` 硬编码颜色（grep 0 命中）
    - 所有触控按钮使用 w-11 h-11（至少 4 个 SHAPES 按钮 + 弹簧按钮 + 折叠+按钮满足）
    - grep '力场' Toolbox.tsx 命中（Phase 3 预留按钮存在）
    - 力场按钮使用 `aria-disabled="true"` + `tabIndex={-1}`
    - 力场按钮 title 包含 "Phase 3"
    - 所有 icon-only button 保留或升级 aria-label
  </acceptance_criteria>
  <done>Toolbox.tsx 重构完成：左列垂直列表，无 fixed 定位，按钮 44×44px 达标，力场预留按钮就位。</done>
</task>

<task type="auto">
  <name>Task 2: 重构 Toolbar.tsx — 底部状态条 + Phase 5 摄像机/2D Toggle 预留</name>
  <read_first>
  - frontend/src/components/Toolbar.tsx
  - .planning/phases/01.1-ui/01.1-UI-SPEC.md
  </read_first>
  <files>frontend/src/components/Toolbar.tsx</files>
  <action>
重写 Toolbar.tsx 使其适配底部网格容器：

**A) 移除浮层定位，改为底部条**

删除最外层 div 的 fixed 定位 (lines 42-58)。替换为：
```tsx
<div
  className="h-full flex items-center gap-2 px-4 select-none
    bg-[hsl(var(--card))] border-t border-[hsl(var(--border))]"
>
```
移除 `fixed top-4 left-1/2 -translate-x-1/2 z-50`, `rounded-lg` (底部条无圆角)，`transition-all duration-200`。
移除 `background: 'rgba(26, 26, 26, 0.85)'`, `backdropFilter`, `WebkitBackdropFilter`, `boxShadow`。
移除 hover 背景变化（onMouseEnter/onMouseLeave lines 59-64）。

**B) 删除 data-toolbar 属性**

移除 `data-toolbar=""` 属性。该属性已无用途（CSS 注入 hack 在 Plan 03 中删除）。

**C) 移除 emoji 文本**

UI-SPEC §13 P3-S3: 不用 emoji 当 icon。
- `'▶ 播放'` → `'开始模拟'`
- `'⏸ 暂停'` → `'暂停模拟'`
- `'↺ 重置'` → `'重置场景'`
文案必须匹配 UI-SPEC §11 Copywriting Contract:
- Primary CTA 运行中: 「暂停模拟」
- Primary CTA 暂停时: 「开始模拟」
- Reset CTA: 「重置场景」

**D) 按钮尺寸升级**

播放/暂停按钮、重置按钮、调试按钮等的 padding 升级确保触控区域 ≥ 44px：
- `px-3 py-1.5` 保持（shadcn Button 组件 size="default" 的 h-8 不满足 44px，改用自定义 className）
- 图标按钮（调试 Bug, 环境 Globe）使用 `w-11 h-11` 确保 44×44

**E) Token 迁移**

所有硬编码颜色替换为 CSS 变量：
- `'#3b82f6'` → `'hsl(var(--primary))'`
- `'#a0a0a0'` → `'hsl(var(--muted-foreground))'`
- `'#e0e0e0'` → `'hsl(var(--foreground))'`
- `'#ffffff'` → `'hsl(var(--primary-foreground))'`
- `'#ef4444'` → `'hsl(var(--destructive))'`
- `'#888888'` → `'hsl(var(--muted-foreground))'`
- `'rgba(255, 255, 255, 0.1)'` / `'rgba(255,255,255,0.06)'` → `'hsl(var(--border))'`
- FPS/物体数分隔线 → `'hsl(var(--border))'`

**F) 追加 Phase 5 预留入口（UI-08）**

在 Toolbar 最右侧（FPS/物体数指示器之后）插入：

```tsx
{/* Phase 5 摄像机模式切换 — 预留 (UI-08) */}
<div className="flex items-center gap-1 ml-2" title="摄像机模式 (Phase 5 解锁)">
  <button
    type="button"
    aria-label="轨道摄像机"
    aria-pressed="true"
    aria-disabled="true"
    className="w-11 h-11 flex items-center justify-center rounded-md
      bg-[hsl(var(--primary)/0.15)] text-[hsl(var(--foreground))]
      cursor-not-allowed"
    tabIndex={-1}
  >
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
      <circle cx="12" cy="12" r="3"/><circle cx="12" cy="12" r="10"/>
    </svg>
  </button>
  <button
    type="button"
    aria-label="自由飞行摄像机"
    aria-pressed="false"
    aria-disabled="true"
    className="w-11 h-11 flex items-center justify-center rounded-md
      text-[hsl(var(--muted-foreground)/0.3)]
      cursor-not-allowed"
    tabIndex={-1}
  >
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
      <path d="M12 2l3 7h6l-5 4 2 7-6-4-6 4 2-7-5-4h6z"/>
    </svg>
  </button>
</div>

{/* 分隔线 */}
<div style={{ width: 1, height: 20, backgroundColor: 'hsl(var(--border))' }} />

{/* Phase 5 3D/2D 模式切换 — 预留 (UI-08) */}
<div className="flex items-center gap-1" title="3D/2D 模式切换 (Phase 5 解锁)">
  <button
    type="button"
    aria-label="3D 模式"
    aria-pressed="true"
    aria-disabled="true"
    className="w-11 h-11 flex items-center justify-center rounded-md
      bg-[hsl(var(--primary)/0.15)] text-[hsl(var(--foreground))]
      cursor-not-allowed text-xs font-semibold"
    tabIndex={-1}
  >
    3D
  </button>
  <button
    type="button"
    aria-label="2D 模式"
    aria-pressed="false"
    aria-disabled="true"
    className="w-11 h-11 flex items-center justify-center rounded-md
      text-[hsl(var(--muted-foreground)/0.3)]
      cursor-not-allowed text-xs font-semibold"
    tabIndex={-1}
  >
    2D
  </button>
</div>

{/* 触控控件预留区 — Phase 5 (UI-08) */}
<div className="hidden lg:block w-0" data-touch-reserve />
```

**G) 保留逻辑**

所有 store 绑定（toggle/reset/setShowDebug/toggleEnvironmentPanel）+ visualization store（trails/vectors/mode）+ 图表开关逻辑 = 保持不变。仅改变容器结构和 token。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c 'fixed' src/components/Toolbar.tsx | grep -c '0' &amp;&amp; grep -c 'data-toolbar' src/components/Toolbar.tsx | grep -c '0' &amp;&amp; grep -c '▶\|⏸\|↺\|emoji' src/components/Toolbar.tsx | grep -c '0' &amp;&amp; grep -c '开始模拟' src/components/Toolbar.tsx | grep -E '^[1-9]' &amp;&amp; grep -c '重置场景' src/components/Toolbar.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'Phase 5' src/components/Toolbar.tsx | grep -E '^[2-9]' &amp;&amp; echo "TOOLBAR_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Toolbar.tsx 不含 `fixed` 定位（grep 0 命中）
    - Toolbar.tsx 不含 `data-toolbar` 属性（grep 0 命中）
    - Toolbar.tsx 不含 emoji 字符（grep '▶\|⏸\|↺' 0 命中）
    - 播放按钮文案 =「开始模拟」，运行态 =「暂停模拟」
    - 重置按钮文案 =「重置场景」
    - CameraModeToggle 按钮组存在，`aria-disabled="true"`，title 含 "Phase 5"
    - DimensionToggle 按钮组存在，`aria-disabled="true"`，title 含 "Phase 5"
    - 数据分隔线使用 `hsl(var(--border))`
    - `data-touch-reserve` div 存在
    - 原有功能逻辑（toggle/reset/调试/环境/可视化/图表/状态指示器）全部保留
  </acceptance_criteria>
  <done>Toolbar.tsx 重构完成：底部状态条，无 fixed 定位，emoji 清除，文案匹配 copywriting contract，Phase 5 摄像机/2D Toggle 预留就位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新边界 | 本计划仅重构 UI 组件外观和定位，不改变任何数据流。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-09 | Denial of Service | Toolbar 移除 data-toolbar 后旧代码可能仍查询该属性 | mitigate | grep 验证 data-toolbar 0 命中；CSS 注入 hack 已在 Plan 03 删除。 |
| T-01.1-10 | Elevation of Privilege | 预留按钮 disabled 但 aria-disabled 可被聚焦 | accept | 使用 tabIndex={-1} 阻止键盘聚焦；视觉 disabled（cursor-not-allowed）实现 L-10 缓解。攻击者无法通过预留按钮进入未实现功能。 |
</threat_model>

<verification>
1. `grep 'fixed' frontend/src/components/Toolbox.tsx` 返回 0
2. `grep 'fixed' frontend/src/components/Toolbar.tsx` 返回 0
3. `grep 'data-toolbar' frontend/src/components/Toolbar.tsx` 返回 0
4. `npx tsc --noEmit -p frontend/tsconfig.json` 编译通过
5. Toolbar.tsx 文案匹配 copywriting contract（开始模拟/暂停模拟/重置场景）
</verification>

<success_criteria>
1. Toolbox.tsx 移除所有 fixed 定位，适配左列网格容器（bg-sidebar）
2. Toolbox 新增「+ 力场」按钮（disabled + tooltip "Phase 3 解锁"）
3. Toolbar.tsx 移除所有 fixed 定位，适配底部条（bg-card, border-t）
4. Toolbar 新增 CameraModeToggle（Orbit/FPS disabled）+ DimensionToggle（3D/2D disabled）
5. 所有触控按钮满足 44×44px 最小尺寸（w-11 h-11）
6. emoji 字符全部清除，文案匹配 UI-SPEC §11 copywriting contract
7. 所有硬编码颜色替换为 CSS 变量 token
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui/01.1-05-SUMMARY.md`
</output>
