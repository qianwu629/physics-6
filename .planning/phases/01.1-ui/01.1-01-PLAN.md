---
phase: "01.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/components.json
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-07

must_haves:
  truths:
    - "CSS 变量体系中定义了完整的 7 层 z-index 堆栈 (--z-base 到 --z-toast)"
    - "CSS 变量体系中定义了布局骨架尺寸 (--sidebar-width, --inspector-width, --menubar-height, --toolbar-height)"
    - "index.css :root 块包含 dark 主题变量定义，.dark 块包含 light 主题覆盖"
    - "所有布局尺寸为 4 的倍数且触控目标满足 44×44px 的 --btn-size 变量就位"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "Theme tokens + z-index scale + layout variables"
      contains: "--z-toast: 60"
    - path: "frontend/src/components/ui/tabs.tsx"
      provides: "shadcn Tabs component"
    - path: "frontend/src/components/ui/toggle.tsx"
      provides: "shadcn Toggle component"
    - path: "frontend/src/components/ui/toggle-group.tsx"
      provides: "shadcn ToggleGroup component"
    - path: "frontend/src/components/ui/resizable.tsx"
      provides: "shadcn Resizable component"
    - path: "frontend/src/components/ui/card.tsx"
      provides: "shadcn Card component"
  key_links:
    - from: "frontend/src/index.css :root block"
      to: "tailwind inline theme (--color-*)"
      via: "CSS variable resolution"
      pattern: "var\\(--z-"
    - from: "frontend/src/components/ui/tabs.tsx"
      to: "frontend/src/index.css"
      via: "shadcn preset token consumption"
      pattern: "hsl\\(var\\(--"
---

<objective>
建立 Phase 01.1 CSS 基础设施：补充 dark/light 双主题变量、z-index 7 层堆栈、栅格化布局骨架变量，安装 5 个 shadcn 新组件 (tabs / toggle / toggle-group / resizable / card)。

Purpose: 为后续所有计划提供可引用的 CSS token 体系与 shadcn 组件。
Output: 更新后的 index.css（含完整变量体系）+ 5 个新 shadcn UI 组件文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-ui/01.1-CONTEXT.md
@frontend/src/index.css

<interfaces>
<!-- Key existing patterns executor needs -->

From frontend/src/index.css:
- :root block defines light-mode defaults (lines 20-56)
- .dark block overrides for dark mode (lines 132-164)
- @theme inline block maps CSS vars to Tailwind (lines 89-129)
- Fontface: Geist Variable loaded from Google Fonts + @fontsource-variable/geist
- Existing Tailwind @import chain: font → tailwindcss → tw-animate-css → shadcn/tailwind.css → @fontsource-variable/geist
- custom-variant dark defined as `&:is(.dark *)`

From frontend/components.json:
- preset: radix-nova, cssVariables: true, baseColor: neutral
- Component aliases: @/components, @/components/ui, @/lib/utils, @/hooks

From 01.1-UI-SPEC.md §5 (Z-Index):
- Complete z-index scale: --z-base:0, --z-canvas:10, --z-panel:20, --z-overlay:30, --z-menu:40, --z-dialog:50, --z-toast:60

From 01.1-UI-SPEC.md §2 (Spacing):
- Layout skeleton: --menubar-height: 2.25rem, --toolbar-height: 2.5rem, --sidebar-width: 13.75rem, --inspector-width: 20rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 5 个 shadcn 新组件 (tabs / toggle / toggle-group / resizable / card)</name>
  <read_first>
  - frontend/components.json
  </read_first>
  <files>frontend/src/components/ui/tabs.tsx, frontend/src/components/ui/toggle.tsx, frontend/src/components/ui/toggle-group.tsx, frontend/src/components/ui/resizable.tsx, frontend/src/components/ui/card.tsx</files>
  <action>
在 frontend 目录下依次执行以下 5 条 shadcn CLI 命令安装官方组件：

```
cd frontend && npx shadcn add tabs
cd frontend && npx shadcn add toggle
cd frontend && npx shadcn add toggle-group
cd frontend && npx shadcn add resizable
cd frontend && npx shadcn add card
```

每步完成后确认文件生成。shadcn CLI 将根据 components.json 中的 radix-nova preset 和 cssVariables:true 配置自动生成组件文件到 frontend/src/components/ui/ 目录，并可能更新 utils、hooks 等依赖。

如果某条命令失败（如网络问题或组件名不匹配），使用 `npx shadcn add --help` 检查正确的组件注册名并重试。
  </action>
  <verify>
    <automated>ls frontend/src/components/ui/tabs.tsx frontend/src/components/ui/toggle.tsx frontend/src/components/ui/toggle-group.tsx frontend/src/components/ui/resizable.tsx frontend/src/components/ui/card.tsx 2>&amp;&amp; echo "ALL_FIVE_EXIST"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ui/tabs.tsx 存在且包含 export 的 Tabs/TabsList/TabsTrigger/TabsContent
    - frontend/src/components/ui/toggle.tsx 存在且包含 export 的 Toggle
    - frontend/src/components/ui/toggle-group.tsx 存在且包含 export 的 ToggleGroup/ToggleGroupItem
    - frontend/src/components/ui/resizable.tsx 存在且包含 export 的 ResizablePanelGroup/ResizablePanel/ResizableHandle
    - frontend/src/components/ui/card.tsx 存在且包含 export 的 Card/CardHeader/CardTitle/CardDescription/CardContent/CardFooter
  </acceptance_criteria>
  <done>5 个 shadcn 组件文件均存在于 frontend/src/components/ui/ 目录，TypeScript 编译通过。</done>
</task>

<task type="auto">
  <name>Task 2: 补全 index.css 双主题变量 + z-index 堆栈 + 布局骨架变量</name>
  <read_first>
  - frontend/src/index.css
  </read_first>
  <files>frontend/src/index.css</files>
  <action>
在 frontend/src/index.css 中进行以下 4 项变更：

**A) 补充 :root 块中缺失的 dark 主题物理色值**
当前 :root 块定义为 light 主题（浅底深字）。在 :root 块**内部**，追加 Phase 01.1 需要的暗色物理值变量（这些用于 3D Canvas 背景和面板模糊效果，不随主题切换）：

```css
/* === Phase 01.1: 新增暗色物理值（不随主题切换）=== */
--blur-panel: 8px;
--shadow-panel: 0 4px 24px rgba(0, 0, 0, 0.4);
--canvas-bg: #1a1a20;
```

**B) 在 :root 块末尾（@theme inline 之前）追加 z-index 7 层堆栈**

```css
/* === Phase 01.1: z-index 7 层堆栈 (UI-07) === */
--z-base: 0;
--z-canvas: 10;
--z-panel: 20;
--z-overlay: 30;
--z-menu: 40;
--z-dialog: 50;
--z-toast: 60;
```

**C) 在 :root 块末尾追加布局骨架 CSS 变量**

```css
/* === Phase 01.1: 布局骨架变量 (UI-04) === */
--menubar-height: 2.25rem;       /* 36px — h-9 */
--toolbar-height: 2.5rem;        /* 40px — h-10 */
--sidebar-width: 13.75rem;       /* 220px */
--inspector-width: 20rem;        /* 320px */
/* 触控目标最小尺寸 (UI-06 a11y P1) */
--touch-target: 2.75rem;         /* 44px */
```

**D) 在 @theme inline 块中追加新变量的 Tailwind 映射**

在 `@theme inline { ... }` 块内（现有 `--color-sidebar-ring` 等映射之后、`--radius-sm` 之前）追加：

```css
    /* Phase 01.1: z-index scale mapped to Tailwind */
    --z-base: var(--z-base);
    --z-canvas: var(--z-canvas);
    --z-panel: var(--z-panel);
    --z-overlay: var(--z-overlay);
    --z-menu: var(--z-menu);
    --z-dialog: var(--z-dialog);
    --z-toast: var(--z-toast);
    /* Phase 01.1: layout skeleton vars mapped to Tailwind */
    --menubar-height: var(--menubar-height);
    --toolbar-height: var(--toolbar-height);
    --sidebar-width: var(--sidebar-width);
    --inspector-width: var(--inspector-width);
    /* Phase 01.1: visual effects mapped to Tailwind */
    --blur-panel: var(--blur-panel);
    --shadow-panel: var(--shadow-panel);
    --canvas-bg: var(--canvas-bg);
```

**注意:** .dark { } 块中已存在的 shadcn 变量 (--background, --foreground, --primary 等) 保持不变，它们已正确定义深色主题覆盖。:root 块中的原有 light 主题变量也不需要修改——它们是 light 主题的浅底色值，current :root 已包含完整变量（色值虽然看起来像 light，但有 .dark { } 做覆盖）。

**重要:** 不要修改任何已有变量的色值（--background, --foreground, --card, --primary 等）。仅追加新变量。

**验证:**
- `grep -c "\-\-z-toast" frontend/src/index.css` 应 = 2（声明一次 + 映射一次）
- `grep -c "\-\-sidebar-width" frontend/src/index.css` 应 = 2
- `grep -c "\-\-blur-panel" frontend/src/index.css` 应 = 2
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c '\-\-z-toast: 60' src/index.css | grep -E '^[1-9]' &amp;&amp; grep -c '\-\-sidebar-width: 13.75rem' src/index.css | grep -E '^[1-9]' &amp;&amp; grep -c '\-\-blur-panel: 8px' src/index.css | grep -E '^[1-9]' &amp;&amp; grep -c '\-\-touch-target' src/index.css | grep -E '^[1-9]' &amp;&amp; echo "ALL_VARS_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - grep '\-\-z-base: 0' frontend/src/index.css 命中
    - grep '\-\-z-toast: 60' frontend/src/index.css 命中
    - grep '\-\-z-canvas: 10' frontend/src/index.css 命中
    - grep '\-\-z-panel: 20' frontend/src/index.css 命中
    - grep '\-\-z-overlay: 30' frontend/src/index.css 命中
    - grep '\-\-z-menu: 40' frontend/src/index.css 命中
    - grep '\-\-z-dialog: 50' frontend/src/index.css 命中
    - grep '\-\-menubar-height: 2.25rem' frontend/src/index.css 命中
    - grep '\-\-toolbar-height: 2.5rem' frontend/src/index.css 命中
    - grep '\-\-sidebar-width: 13.75rem' frontend/src/index.css 命中
    - grep '\-\-inspector-width: 20rem' frontend/src/index.css 命中
    - grep '\-\-touch-target: 2.75rem' frontend/src/index.css 命中
    - grep '\-\-blur-panel: 8px' frontend/src/index.css 命中
    - grep '\-\-shadow-panel' frontend/src/index.css 命中
    - grep '\-\-canvas-bg: #1a1a20' frontend/src/index.css 命中
    - 原有 shadcn 变量 (--background, --foreground, --primary, --card 等) 色值未变更
  </acceptance_criteria>
  <done>index.css :root 块包含完整的 z-index 7 层、布局骨架 4 变量、暗色物理值 3 变量、@theme inline 映射全量可用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新边界 | 本计划仅修改 CSS 变量定义和安装官方 shadcn 组件，不引入新的信任边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-01 | Denial of Service | index.css z-index purge | mitigate | Tailwind v4 的 `@theme inline` 块通过 `--z-*: var(--z-*)` 显式映射确保 z-index 变量不被 purge；额外在 safelist 备用。 |
| T-01.1-02 | Tampering | shadcn 组件注册 | accept | shadcn 官方 CLI (`npx shadcn add`) 从官方 registry 拉取组件，不涉及第三方 registry。零攻击面。|
</threat_model>

<verification>
1. `grep -c '\-\-z-base' frontend/src/index.css` 应 >= 1（声明 + @theme inline 映射 = 2 命中）
2. `npx tsc --noEmit -p frontend/tsconfig.json` 编译通过（新 shadcn 组件无类型错误）
3. `ls frontend/src/components/ui/tabs.tsx frontend/src/components/ui/toggle.tsx frontend/src/components/ui/toggle-group.tsx frontend/src/components/ui/resizable.tsx frontend/src/components/ui/card.tsx` 5 文件均存在
</verification>

<success_criteria>
1. index.css 包含 7 个 z-index CSS 变量（--z-base 到 --z-toast）
2. index.css 包含 4 个布局骨架 CSS 变量（--menubar-height, --toolbar-height, --sidebar-width, --inspector-width）
3. index.css 包含 3 个暗色物理值变量（--blur-panel, --shadow-panel, --canvas-bg）+ 触控目标变量（--touch-target）
4. @theme inline 块包含所有新变量的 Tailwind 映射
5. 5 个 shadcn 组件（tabs, toggle, toggle-group, resizable, card）文件存在于 frontend/src/components/ui/
6. TypeScript 项目编译通过（npx tsc --noEmit）
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui/01.1-01-SUMMARY.md`
</output>
