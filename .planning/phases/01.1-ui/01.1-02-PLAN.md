---
phase: "01.1"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ThemeProvider.tsx
  - frontend/src/components/MenuBar.tsx
autonomous: true
requirements:
  - UI-02
  - UI-03

must_haves:
  truths:
    - "用户通过 MenuBar [视图] 菜单可切换深色/浅色主题"
    - "主题选择持久化到 localStorage('physis-theme')，刷新后保持"
    - "首次访问无 localStorage 记录时遵循 prefers-color-scheme 系统偏好"
    - "主题切换时 document.documentElement.classList 的 'dark' 类被添加/移除"
    - "ThemeProvider 切换不会导致 R3F Canvas 重新挂载"
  artifacts:
    - path: "frontend/src/components/ThemeProvider.tsx"
      provides: "Theme Context + localStorage persistence + classList toggle"
      contains: "ThemeProvider"
    - path: "frontend/src/components/MenuBar.tsx"
      provides: "视图菜单中新增主题切换入口"
      contains: "切换深色 / 浅色主题"
  key_links:
    - from: "frontend/src/components/ThemeProvider.tsx"
      to: "document.documentElement.classList"
      via: "useEffect on theme change"
      pattern: "classList\\.toggle\\('dark'"
    - from: "frontend/src/components/MenuBar.tsx"
      to: "frontend/src/components/ThemeProvider.tsx"
      via: "useTheme hook"
      pattern: "useTheme\\(\\)"
---

<objective>
创建轻量 ThemeProvider（Context + localStorage + classList toggle）并集成到 MenuBar [视图] 菜单中，实现深/浅双主题切换。不引入 next-themes（Vite SPA 不需要 SSR 适配）。

Purpose: 为全部 UI 组件提供主题感知能力（useTheme hook），实现 OQ-1 决议的 Vite 友好方案。
Output: ThemeProvider.tsx + MenuBar.tsx 新增主题切换菜单项。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-ui/01.1-CONTEXT.md
@frontend/src/index.css
@frontend/src/components/MenuBar.tsx
@frontend/src/main.tsx

<interfaces>
From 01.1-UI-SPEC.md §1 (Design System):
- Theme switcher: 自建 ThemeProvider (Context + localStorage('physis-theme') + documentElement.classList.toggle('dark'))
- 初始化时遵循 prefers-color-scheme
- Default theme: dark

From 01.1-UI-SPEC.md §8 (Accessibility):
- prefers-reduced-motion: UI 动画 0ms；物理 useFrame 不节流

From 01.1-UI-SPEC.md §11 (Copywriting):
- Tooltip 主题切换: 「切换深色 / 浅色主题」

From frontend/src/index.css:
- .dark 块 (lines 132-164) 已定义 dark 主题变量覆盖
- :root 块 (lines 20-56) 定义 light 主题默认值
- custom-variant dark 定义为 `&:is(.dark *)`

From frontend/src/main.tsx:
- React 18+ createRoot 入口 — ThemeProvider 需要包裹在此处

From frontend/src/components/MenuBar.tsx:
- 现有 [视图 ▾] 菜单包含: 显示调试线框、显示 FPS、工具箱、属性面板、环境面板
- 使用 DropdownMenu/DropdownMenuCheckboxItem/DropdownMenuTrigger/DropdownMenuContent/DropdownMenuItem/DropdownMenuSeparator
- Import: DropdownMenu*, Dialog*, Button, lucide icons
- menuButtonClass = 'px-3 py-1 text-sm text-[#a0a0a0]...'

Landmine L-03: ThemeProvider 切换致 R3F Canvas 重新挂载 → Scene3D 需 React.memo 保护（Scene3D 在 App.tsx 中渲染，由 App.tsx 包裹 ThemeProvider）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ThemeProvider.tsx (Context + localStorage + classList)</name>
  <read_first>
  - frontend/src/index.css
  - frontend/src/main.tsx
  </read_first>
  <files>frontend/src/components/ThemeProvider.tsx</files>
  <action>
创建文件 frontend/src/components/ThemeProvider.tsx，包含以下内容：

```typescript
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react';

type Theme = 'dark' | 'light';
const STORAGE_KEY = 'physis-theme';

interface ThemeContextValue {
  theme: Theme;
  setTheme: (theme: Theme) => void;
  toggleTheme: () => void;
}

const ThemeContext = createContext<ThemeContextValue | null>(null);

function getInitialTheme(): Theme {
  // 1. localStorage 优先
  const stored = localStorage.getItem(STORAGE_KEY) as Theme | null;
  if (stored === 'dark' || stored === 'light') return stored;
  // 2. 系统偏好
  if (window.matchMedia('(prefers-color-scheme: light)').matches) return 'light';
  // 3. 默认 dark
  return 'dark';
}

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [theme, setThemeState] = useState<Theme>(getInitialTheme);

  useEffect(() => {
    const root = document.documentElement;
    if (theme === 'dark') {
      root.classList.add('dark');
    } else {
      root.classList.remove('dark');
    }
  }, [theme]);

  const setTheme = (t: Theme) => {
    setThemeState(t);
    localStorage.setItem(STORAGE_KEY, t);
  };

  const toggleTheme = () => {
    setTheme(theme === 'dark' ? 'light' : 'dark');
  };

  return (
    <ThemeContext.Provider value={{ theme, setTheme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error('useTheme must be used within ThemeProvider');
  return ctx;
}
```

关键设计决策（对应 OQ-1、D-01.1-04）：
- 自建 Context + localStorage，不引入 next-themes（Vite SPA 无需 SSR）
- localStorage key = 'physis-theme'
- 初始化顺序：localStorage → prefers-color-scheme → dark 回退
- dark 为主题默认值（教学/演示常态）
- classList.toggle('dark') 激活 index.css 中 `.dark { }` 块的变量覆盖
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c 'localStorage.getItem.*physis-theme' src/components/ThemeProvider.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'classList.*dark' src/components/ThemeProvider.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'prefers-color-scheme' src/components/ThemeProvider.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'export function useTheme' src/components/ThemeProvider.tsx | grep -E '^[1-9]' &amp;&amp; echo "THEME_PROVIDER_OK"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ThemeProvider.tsx 存在
    - 导出 ThemeProvider (包裹组件) 和 useTheme (消费 hook)
    - useTheme 返回 { theme, setTheme, toggleTheme }，类型为 'dark' | 'light'
    - localStorage key 为 'physis-theme'
    - getInitialTheme 按 localStorage → prefers-color-scheme → 'dark' 顺序解析
    - theme 变化时执行 document.documentElement.classList.toggle('dark', theme==='dark')
  </acceptance_criteria>
  <done>ThemeProvider.tsx 创建完成，TypeScript 编译通过，导出 ThemeProvider + useTheme。</done>
</task>

<task type="auto">
  <name>Task 2: 集成 ThemeProvider 到 main.tsx + 添加主题切换入口到 MenuBar [视图] 菜单</name>
  <read_first>
  - frontend/src/main.tsx
  - frontend/src/components/MenuBar.tsx
  - frontend/src/components/ThemeProvider.tsx
  </read_first>
  <files>frontend/src/main.tsx, frontend/src/components/MenuBar.tsx</files>
  <action>
**A) main.tsx: 包裹 ThemeProvider**

在 main.tsx 中，import ThemeProvider 并用其包裹 App 组件。修改 render 调用：

```
import { ThemeProvider } from './components/ThemeProvider';

// 将 <App /> 包裹为:
<ThemeProvider>
  <App />
</ThemeProvider>
```

**B) MenuBar.tsx: 在 [视图 ▾] 菜单中添加主题切换入口**

在 MenuBar.tsx 中：

1. 在文件顶部添加 import：
```typescript
import { useTheme } from './ThemeProvider';
import { Sun, Moon } from 'lucide-react';
```

2. 在 MenuBar 组件函数体内部，调用 useTheme hook：
```typescript
const { theme, toggleTheme } = useTheme();
```

3. 在 [视图 ▾] DropdownMenuContent 中，「环境面板」CheckboxItem 之后、DropdownMenuContent 关闭标签之前，插入新菜单项：

```tsx
<DropdownMenuSeparator />
<DropdownMenuItem onClick={toggleTheme}>
  {theme === 'dark' ? (
    <Sun className={menuItemIconClass} />
  ) : (
    <Moon className={menuItemIconClass} />
  )}
  <span>切换深色 / 浅色主题</span>
</DropdownMenuItem>
```

注意：使用 DropdownMenuItem（普通 onClick 项）而非 DropdownMenuCheckboxItem，因为这是即时切换操作而非持久复选框状态。
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -c 'ThemeProvider' src/main.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'useTheme' src/components/MenuBar.tsx | grep -E '^[1-9]' &amp;&amp; grep -c '切换深色 / 浅色主题' src/components/MenuBar.tsx | grep -E '^[1-9]' &amp;&amp; grep -c 'toggleTheme' src/components/MenuBar.tsx | grep -E '^[1-9]' &amp;&amp; echo "INTEGRATION_OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep 'ThemeProvider' frontend/src/main.tsx 命中（App 被包裹）
    - grep 'useTheme' frontend/src/components/MenuBar.tsx 命中
    - grep 'toggleTheme' frontend/src/components/MenuBar.tsx 命中
    - grep '切换深色 / 浅色主题' frontend/src/components/MenuBar.tsx 命中
    - MenuBar [视图] 菜单中出现主题切换入口，显示 Sun/Moon 图标 + 文案
    - 原有 [视图] 菜单项（调试线框/FPS/工具箱/属性面板/环境面板）保持完整不变
  </acceptance_criteria>
  <done>ThemeProvider 包裹 App，MenuBar [视图] 菜单可切换主题，dark/light 类 toggle 正常。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage | 用户偏好 'physis-theme' 键值存储在浏览器端，无跨域泄露风险 |
| classList toggle | DOM 操作安全——仅添加/移除 'dark' 类名，无注入风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-03 | Denial of Service | ThemeProvider re-render 风暴 | mitigate | useState + Context 标准模式，单一 theme 状态切换不会引起级联重渲染；Scene3D 使用 React.memo 防止 Canvas 重挂载 (L-03)。 |
| T-01.1-04 | Information Disclosure | localStorage theme key | accept | localStorage 仅存储 'dark'/'light' 字符串，不含 PII，low-value target。 |
</threat_model>

<verification>
1. `npx tsc --noEmit -p frontend/tsconfig.json` 编译通过
2. ThemeProvider.tsx 导出 `ThemeProvider` 和 `useTheme`
3. main.tsx 中 `<App />` 被 `<ThemeProvider>` 包裹
4. MenuBar.tsx 中 [视图] 菜单含 `切换深色 / 浅色主题` 入口
</verification>

<success_criteria>
1. ThemeProvider.tsx 存在，提供 useTheme() hook
2. main.tsx 中 App 被 ThemeProvider 包裹
3. MenuBar [视图] 菜单出现「切换深色 / 浅色主题」入口（Sun/Moon 图标切换）
4. 主题切换后 document.documentElement 的 classList 含/不含 'dark'
5. 刷新后主题偏好保持（localStorage）
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui/01.1-02-SUMMARY.md`
</output>
