---
phase: 01.1
phase_name: ui重构
status: discuss-paused
saved_at: "2026-05-10"
context_used_at_pause: 74%
purpose: 下次 /gsd-discuss-phase 01.1 启动时，先 Read 这份文件，可直接跳过 load_prior_context / scout_codebase / analyze_phase 三步，进入 present_gray_areas
---

# Phase 01.1 — UI 重构 · 讨论进度暂存

> 上次会话上下文用尽，已收集足够信息。下次 `/clear` 后重新 `/gsd-discuss-phase 01.1`，请先 Read 本文件再继续。

---

## ❶ ROADMAP 中的 Phase 定义（极薄）

```
### Phase 01.1: ui重构 (INSERTED)
Goal: [Urgent work - to be planned]
Requirements: TBD
Depends on: Phase 1
Plans: 0 plans
```

**结论：** Goal 完全未定义，本次讨论的核心是**锁住 Goal、Requirements 与范围边界**，而不仅仅是技术实现细节。

---

## ❷ 已加载的上游上下文（要点速览）

### 项目（PROJECT.md）
- Physis：Web 物理模拟平台，v1.0 已发布（5 阶段、12/12 需求、8708 LOC、22 测试套件）
- 用户：高中生，物理实验/教学场景
- 当前里程碑：v2.0 力场与多维模拟（21 个需求 / 7 类别）
- 技术栈：React 19 + Vite + TS + **Tailwind v4** + **shadcn/ui** + R3F + Rapier3D + Zustand
- 架构：ECS 组件组合（严禁模板模式 — 废案教训）
- 关键约束：Web 优先 / 严格 TS / WebGL 3D / 120Hz 物理步长 / per-frame 数据不进 Zustand

### v2.0 需求清单（Active）
- ✅ PERSIST-01..04 + DEBT-04（Phase 1 已交付）
- ⏳ CHART-01..03（Phase 2 代码已落地，**有 7 Critical bugs 待修**）
- ⏳ FIELD-01..04（Phase 3）/ EXPR-01..02（Phase 4）/ CAM-01..02 + DIM2-01..02（Phase 5）/ DEBT-01..03（Phase 6）

### Phase 1 锁定的 UI 决策（D-01-04 / D-01-05 / D-01-08 等）
- D-01-04 顶部 **MenuBar**（[文件] [视图] [帮助] 下拉，Blender/Unity 风格）
- D-01-05 快照管理走**右侧 Drawer**（不用 Dialog 遮挡 3D）
- D-01-08 错误分级：Modal / Banner / Toast 三种反馈通道

### Phase 2 锁定的 UI 决策（D-02-02 / D-02-09 等）
- D-02-02 ChartPanel **浮动**可拖可调大小（`position:fixed` + Draggable + Resizable）
- D-02-09 PropertyPanel 内"图表追踪"开关，默认关闭
- 衍生：图表面板顶部 5s/30s/全程切换；Tab 切换 4 类物理量

### STATE.md 关键信号
- Phase 2 已执行完代码但**未修 7 个 Critical bug、未跑 14 项 UAT**
- v1.0 三项 UAT debt（DEBT-01 弹簧选中、DEBT-02 多体稳定性、DEBT-03 性能）排到 Phase 6

---

## ❸ 代码现状扫描（已完成）

### 组件总数：44 个 `.tsx`（`frontend/src/components/`）
分类：
- **基础 UI（shadcn）：** badge / button / dialog / input / label / scroll-area / separator / slider / sonner / sheet / dropdown-menu / switch / tooltip
- **3D 渲染：** Scene3D / EntityRenderer / SpringRenderer / TrajectoryRenderer / VectorRenderer / Arrow3D / RigidBodyRefContext
- **顶层入口：** App / MenuBar / SceneBanner / Toolbar / Toolbox
- **创建/编辑：** CreationDialog / SpringCreationDialog / SpringCreationBanner / PropertyPanel / EnvironmentPanel / EntityList
- **持久化（P1）：** SnapshotManager / PresetSelector / SceneLoader
- **图表（P2）：** ChartPanel / ChartCanvas / ChartMetricTabs
- **加载/错误：** LoadingScreen / ErrorFallback

### App.tsx 当前布局（手写硬编码、无栅格）
```
<MenuBar />                            // 固定顶部 z-50, h-9 (36px)
<SceneBanner />                        // 黄色版本警告横幅
<ConfirmDialogRoot />                  // 全局确认 Modal
<SnapshotManager />                    // 右侧 Drawer (Sheet side="right")
<PresetSelector />                     // 居中 Dialog
<Scene3D />                            // 全屏 R3F Canvas
<Toolbar chartPanelOpen ... />         // 顶部 → CSS 注入 [data-toolbar]{top:44px} 强制下移（脏 hack）
<Toolbox />                            // 左中浮 (top:50%, translateY) z-40
<PropertyPanel />                      // 右浮 (collapsed 时仅剩孤立按钮 right:16,top:80)
<ChartPanel />                         // 自由浮动 (Draggable + Resizable)
<CreationDialog />
<EnvironmentPanel />
<SpringCreationBanner />
<SpringCreationDialog />
```

### 视觉系统现状（Toolbox.tsx 为代表）
- **背景：** `rgba(26, 26, 26, 0.85)` + `backdrop-blur(8px)` + `border rgba(255,255,255,0.06)` + `shadow 0 4px 24px rgba(0,0,0,0.4)`
- **图标按钮：** `w-10 h-10 rounded-lg`，hover 蓝色淡（`rgba(59,130,246,0.15)`）
- **配色硬编码：** `#a0a0a0`（弱文字）/ `#3b82f6`（蓝主色）/ `#666`（更弱）
- **图标库：** lucide-react（已统一）
- **触控目标：** 多数 40×40px（推荐 ≥ 44×44）

### 已识别的视觉/交互债务（来自 Phase 2 02-REVIEW.md）
| 来源 | 问题 |
|---|---|
| C-02 | ChartPanel `panelRef` 同时用作 Draggable nodeRef + Resizable ref → React 类实例非 DOM → 拖拽即抛错 |
| C-03 | 分离模式只有最后一张图更新且每张都画全部实体 → 功能完全失效 |
| W-01 | 4 实体 cap 仅在 UI 文案，store 不强制 → 极端可吃 2.6GB |
| W-02 | re-resizable 句柄类名错（`react-resizable-handle` 是另一库的） |
| 脏 hack | App.tsx 用 `<style>` 注入 `[data-toolbar]{top:44px !important}` |

---

## ❹ 候选灰色地带（下次直接进 present_gray_areas）

下次启动后跳过分析步骤，直接对用户呈现下面 5 个候选并 multiSelect：

### G-1 · 重构焦点的范围与目标 ⭐ **必选**
当前 ROADMAP 上 Goal 为空，必须本次锁住。候选范围（多选/单选待用户选）：
- (a) 修 Phase 2 浮层缺陷（C-02 / C-03 / W-01 / W-02 + 拆 Toolbar 注入 hack）
- (b) 立**统一设计系统**（color tokens / spacing / radius / shadow / z-index 规模 / 主题切换基座）
- (c) **信息架构重组**（左/右/底固定面板 vs 当前散浮模式 vs 工作区栅格）
- (d) **可访问性基线**（44×44 触控、4.5:1 对比、Tab 顺序、prefers-reduced-motion、aria）
- (e) **Phase 5 + Phase 3 前置准备**（FPS 摄像机进入态、2D 模式 UI 降维、力场创建对话框预留入口）
- (f) PropertyPanel 折叠态/重展开/Drawer 化重做（当前折叠后是孤立浮按钮）

> 用户原话只说"ui重构"——下次第一轮要让用户在以上选 1-3 项作为 Phase 01.1 的 Goal。

### G-2 · 设计系统形态
- 选项：(a) 自建 Tailwind v4 token + CSS 变量主题 (b) 全面切到 shadcn/ui 设计 token + theme provider (c) 仅整理已有手写值为 tailwind.config 常量
- 上游约束：项目已用 shadcn/ui 部分原子组件（dialog/sheet/slider/switch...），方向倾向 (b) 但需用户确认

### G-3 · 布局范式
- (a) **保持浮层范式**（修 bug、加吸附 / 边缘磁吸）
- (b) **栅格化工作台**（左 Toolbox 固定列 + 右 Inspector 固定列 + 中央 3D，参考 Blender/Figma）
- (c) **混合**（核心面板栅格化 + ChartPanel 仍浮动，因物理量分析需要灵活摆位）

### G-4 · 视觉风格主轴
- 当前是"深色玻璃态 + 蓝主色"。候选：
- (a) 维持深色玻璃态，仅做 token 化 + 对比度修正
- (b) 引入**深 / 浅双主题**（教学环境投影仪/打印场景）
- (c) 拥抱"科学/教育工作台"调性：低饱和深色 + 高对比文字 + 物理量类别色（蓝-绿-橙-紫，已在 D-02-03 出现）

### G-5 · 多态适配
- 当前仅桌面。Phase 5 已立"触控 fallback"。是否在 01.1 一并处理：
- (a) 仅桌面，触控延后到 Phase 5
- (b) 在 01.1 引入响应式断点骨架（≥1024 desktop / <1024 平板 / <640 隐藏次级面板）
- (c) 仅在 01.1 准备 token 与栅格，触控具体交互留 Phase 5

---

## ❺ 范围红线（scope_guardrail 提醒）

下次讨论需提醒用户区分：
- ✅ 在范围：HOW 实现已有面板的视觉/交互/信息架构
- ❌ 出范围：新增物理能力、新增 v3 需求（CSV 导出 / 时间操控 / 多场景对比 / 量具）→ 进 deferred ideas
- ❌ 出范围：Phase 2 的功能 bugfix（C-04..C-07 数据/内存层 bug）→ 它们应进 Phase 02 review-fix，不属于 UI 重构

---

## ❻ 下次启动指引（给未来的我）

1. 看到 `/gsd-discuss-phase 01.1`，先 Read 本文件
2. 跳过：`load_prior_context` / `scout_codebase` / `analyze_phase`（已完成）
3. 直接进 `present_gray_areas`，把上述 5 个 G-x 渲染成 AskUserQuestion 选项（先做 G-1 单独一轮，因它锁定整个 phase 的 Goal）
4. 对话讨论 → 写 `01.1-CONTEXT.md` + `01.1-DISCUSSION-LOG.md`
5. **强制要求：** 在写 CONTEXT 之前再调用一次 `Skill(ui-ux-pro-max)` 获取选定方向的细化推荐（如选了"设计系统"就 search "design system tokens accessibility"），用户已确立"前端工作必走 ui-ux-pro-max"的偏好（见 memory/feedback_frontend_skills.md）
6. canonical_refs 必含：`.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` / `.planning/PROJECT.md` / `.planning/phases/01-持久化与场景库/01-CONTEXT.md` / `.planning/phases/02-实时物理量图表/02-CONTEXT.md` / `.planning/phases/02-实时物理量图表/02-REVIEW.md`

---

*会话 1 于 2026-05-10 暂停，上下文 74% 用尽。继续时请 `/clear` 后重启 `/gsd-discuss-phase 01.1`。*
