---
phase: "01.1"
plan: "08"
type: execute
wave: 4
depends_on:
  - "01.1-01"
  - "01.1-04"
  - "01.1-05"
  - "01.1-06"
files_modified:
  - frontend/src/components/CreationDialog.tsx
  - frontend/src/components/SpringCreationDialog.tsx
  - frontend/src/components/SpringCreationBanner.tsx
  - frontend/src/components/PresetSelector.tsx
  - frontend/src/components/SnapshotManager.tsx
  - frontend/src/components/SceneLoader.tsx
  - frontend/src/components/ChartMetricTabs.tsx
  - frontend/src/components/MenuBar.tsx
  - frontend/src/components/EntityRenderer.tsx
  - frontend/src/components/LoadingScreen.tsx
  - frontend/src/components/ErrorFallback.tsx
  - frontend/src/components/Scene3D.tsx
autonomous: true
requirements:
  - UI-02

must_haves:
  truths:
    - "所有组件文件中的硬编码 rgba() / 井号色值 / backdrop-blur(Npx) 替换为 CSS 变量引用"
    - "ChartPanel 业务颜色（METRIC_COLORS 和 lightweight-charts 配置色）不受 codemod 影响"
    - "Token 替换后的组件在所有主题下视觉一致"
  artifacts:
    - path: "frontend/src/components/CreationDialog.tsx"
      provides: "实体创建对话框（token 迁移）"
    - path: "frontend/src/components/SpringCreationDialog.tsx"
      provides: "弹簧创建对话框（token 迁移）"
    - path: "frontend/src/components/SpringCreationBanner.tsx"
      provides: "弹簧创建横幅（token 迁移）"
    - path: "frontend/src/components/PresetSelector.tsx"
      provides: "预设场景选择器（token 迁移）"
    - path: "frontend/src/components/SnapshotManager.tsx"
      provides: "快照管理器（token 迁移）"
    - path: "frontend/src/components/SceneLoader.tsx"
      provides: "场景加载器（token 迁移）"
    - path: "frontend/src/components/ChartMetricTabs.tsx"
      provides: "图表指标 Tab（token 迁移）"
    - path: "frontend/src/components/MenuBar.tsx"
      provides: "菜单栏（token 迁移残留）"
    - path: "frontend/src/components/EntityRenderer.tsx"
      provides: "实体 3D 渲染器（仅 UI 色部分 token 迁移）"
    - path: "frontend/src/components/LoadingScreen.tsx"
      provides: "加载屏幕（token 迁移）"
    - path: "frontend/src/components/ErrorFallback.tsx"
      provides: "错误回退（token 迁移）"
    - path: "frontend/src/components/Scene3D.tsx"
      provides: "3D 场景（仅 UI 色部分 token 迁移）"
  key_links:
    - from: "all modified files"
      to: "frontend/src/index.css"
      via: "hsl(var(--*)) CSS variable resolution"
      pattern: "hsl\\(var\\(--"
---

<objective>
全量 token 迁移 codemod：将 Phase 01.1 前 7 个 Plan 未覆盖的组件文件中的硬编码 rgba()/#xxxxxx/backdrop-blur(Npx) 替换为 CSS 变量 token 引用。ChartPanel 业务颜色（图表曲线色和 lightweight-charts 配置色）不受影响。

Purpose: 完成 UI-02 要求的完整 token 迁移——所有 visible UI 颜色均通过 shadcn CSS 变量体系引用。
Output: 12 个组件文件的 token 替换。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01.1-ui/01.1-UI-SPEC.md
@.planning/phases/01.1-ui/01.1-CONTEXT.md
@frontend/src/index.css

<interfaces>
Token 迁移映射表（UI-SPEC §2 Visual Baseline + §4 Color）：

| 当前硬编码 | 替换为 |
|-----------|--------|
| `rgba(26, 26, 26, 0.85)` / `rgba(26,26,26,0.85)` / `rgba(26,26,26,0.9)` / `rgba(26,26,26,0.92)` / `rgba(26,26,26,0.95)` | `hsl(var(--card) / 0.85)` / `hsl(var(--card) / 0.9)` / `hsl(var(--card) / 0.92)` / `hsl(var(--card) / 0.95)` |
| `backdrop-filter: blur(8px)` / `blur(12px)` | `backdrop-filter: blur(var(--blur-panel))` |
| `-webkit-backdrop-filter: blur(8px)` / `blur(12px)` | `-webkit-backdrop-filter: blur(var(--blur-panel))` |
| `border: 1px solid rgba(255, 255, 255, 0.06)` / `0.08` / `0.1` | `border: 1px solid hsl(var(--border))` |
| `borderBottom: '1px solid rgba(255,255,255,0.06)'` | `borderBottom: '1px solid hsl(var(--border))'` |
| `backgroundColor: 'rgba(255,255,255,0.04)'` / hover `rgba(59,130,246,0.1)` | `backgroundColor: 'hsl(var(--accent))'` / `'hsl(var(--primary) / 0.1)'` |
| `boxShadow: '0 4px 24px rgba(0,0,0,0.4)'` | `boxShadow: 'var(--shadow-panel)'` |
| `color: '#a0a0a0'` / `'#a0a0a0'` | `color: 'hsl(var(--muted-foreground))'` |
| `color: '#e0e0e0'` / `'#e0e0e0'` | `color: 'hsl(var(--foreground))'` |
| `color: '#666'` / `'#888'` | `color: 'hsl(var(--muted-foreground) / 0.6)'` |
| `color: '#3b82f6'` / `backgroundColor: '#3b82f6'` | `color: 'hsl(var(--primary))'` / `backgroundColor: 'hsl(var(--primary))'` |
| `background: '#0a0a0a'` / `'#222'` | `backgroundColor: 'hsl(var(--background))'` |
| `borderLeftColor: '#3b82f6'` | `borderLeftColor: 'hsl(var(--primary))'` |
| `color: '#ffffff'` / `'#fff'` (在 UI 文字中) | `color: 'hsl(var(--foreground))'` |

**不替换的例外（保留原值）:**
- ChartCanvas.tsx METRIC_COLORS（图表曲线色 — L-12 dry-run 保护）
- lightweight-charts createChart 的 layout/textColor/grid 配置（图表库内部配色）
- Scene3D.tsx 中的 Three.js material color（3D 材质色，不受 UI token 控制）
- EntityRenderer.tsx 中的 renderOrder / castShadow 等 3D 属性（非颜色）
- 任何 `// NO-TOKEN-MIGRATE` 注释标记的行
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token 迁移 — 对话框 + 浮层组件 (CreationDialog / SpringCreationDialog / SpringCreationBanner / PresetSelector / SnapshotManager / SceneLoader)</name>
  <read_first>
  - frontend/src/components/CreationDialog.tsx
  - frontend/src/components/SpringCreationDialog.tsx
  - frontend/src/components/SpringCreationBanner.tsx
  - frontend/src/components/PresetSelector.tsx
  - frontend/src/components/SnapshotManager.tsx
  - frontend/src/components/SceneLoader.tsx
  </read_first>
  <files>frontend/src/components/CreationDialog.tsx, frontend/src/components/SpringCreationDialog.tsx, frontend/src/components/SpringCreationBanner.tsx, frontend/src/components/PresetSelector.tsx, frontend/src/components/SnapshotManager.tsx, frontend/src/components/SceneLoader.tsx</files>
  <action>
对 6 个对话框/浮层组件进行系统性 token 迁移。按以下规则逐个文件处理：

**批量替换规则:**
1. 搜索 `rgba(` → 找到所有 rgba 表达式，按映射表替换
2. 搜索 `backdrop-filter` / `WebkitBackdropFilter` / `backdropFilter` → 替换为 `var(--blur-panel)`
3. 搜索 `boxShadow.*rgba` → 替换为 `var(--shadow-panel)`
4. 搜索 `#[0-9a-fA-F]{3,6}` → 匹配颜色码（排除 import 路径和注释），按映射表替换
5. 搜索 `border.*rgba` → 替换为 `hsl(var(--border))`

**每文件具体注意事项:**

**CreationDialog.tsx:**
- 对话框背景 → Dialog 组件（shadcn 托管），不修改
- 表单输入框内联 style color/backgroundColor/borderColor → 替换为 hsl(var(--*))
- Shape 选择按钮的背景色/边框色 → 替换

**SpringCreationDialog.tsx:**
- 弹簧预览区域背景 → 替换
- 表单字段内联颜色 → 替换

**SpringCreationBanner.tsx:**
- 横幅背景 `rgba(...)` + `backdropFilter` → 替换
- 边框 `border: '1px solid rgba(...)'` → 替换
- 文字颜色 → 替换

**PresetSelector.tsx:**
- 预设卡片背景 + hover 态 → 替换
- 卡片边框 → 替换
- 标签/描述文字 → 替换

**SnapshotManager.tsx:**
- Sheet 内部（shadcn 托管）→ 不修改
- 快照列表项背景/边框 → 替换
- 按钮内联颜色 → 替换

**SceneLoader.tsx:**
- SceneBanner（黄色横幅）背景/文字色 → 替换为 hsl(var(--primary) / 0.1) 等
- ConfirmDialog — shadcn Dialog 托管 → 不修改
- Toast 消息 — sonner 托管 → 不修改
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in src/components/CreationDialog.tsx src/components/SpringCreationDialog.tsx src/components/SpringCreationBanner.tsx src/components/PresetSelector.tsx src/components/SnapshotManager.tsx src/components/SceneLoader.tsx; do rg_c=$(grep -c 'rgba(' "$f" 2>/dev/null || echo "0"); if [ "$rg_c" -gt 0 ]; then echo "FAIL: $f has $rg_c rgba()"; fi; done; echo "RGBA_CHECK_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - 6 个文件均不含 `rgba(` 硬编码颜色（grep 0 命中，排除 import 路径不含色值）
    - 6 个文件均不含 `backdrop-filter: blur(8px)` 或 `blur(12px)` 硬编码值
    - 所有边框色使用 `hsl(var(--border))` token（非 `rgba(255, 255, 255, ...)`）
    - 所有文字主色 (`#e0e0e0` / `#a0a0a0`) 替换为 `hsl(var(--foreground))` / `hsl(var(--muted-foreground))`
    - shadcn 托管组件（Dialog/Sheet/Sonner）的样式不被覆盖
    - TypeScript 编译通过
  </acceptance_criteria>
  <done>6 个对话框/浮层组件的硬编码颜色全部 token 化。</done>
</task>

<task type="auto">
  <name>Task 2: Token 迁移 — 图表/菜单/3DUI 组件 (ChartMetricTabs / MenuBar / EntityRenderer / LoadingScreen / ErrorFallback / Scene3D)</name>
  <read_first>
  - frontend/src/components/ChartMetricTabs.tsx
  - frontend/src/components/MenuBar.tsx
  - frontend/src/components/EntityRenderer.tsx
  - frontend/src/components/LoadingScreen.tsx
  - frontend/src/components/ErrorFallback.tsx
  - frontend/src/components/Scene3D.tsx
  </read_first>
  <files>frontend/src/components/ChartMetricTabs.tsx, frontend/src/components/MenuBar.tsx, frontend/src/components/EntityRenderer.tsx, frontend/src/components/LoadingScreen.tsx, frontend/src/components/ErrorFallback.tsx, frontend/src/components/Scene3D.tsx</files>
  <action>
对 6 个剩余组件进行 token 迁移。

**ChartMetricTabs.tsx:**
- Tab 按钮颜色 → `hsl(var(--muted-foreground))` / `hsl(var(--foreground))` / `hsl(var(--primary))`
- 激活态背景 → `hsl(var(--primary) / 0.15)`
- 边框 → `hsl(var(--border))`
- **不修改:** 任何与 chart data/colors 相关的业务逻辑

**MenuBar.tsx:**
- menuButtonClass 中的颜色已经部分使用 token — 检查并补全：
  - `'#a0a0a0'` → `'hsl(var(--muted-foreground))'`
  - `'#e0e0e0'` → `'hsl(var(--foreground))'`
  - `'rgba(59,130,246,0.12)'` → `'hsl(var(--primary) / 0.12)'`
  - `'#888'` → `'hsl(var(--muted-foreground) / 0.6)'`
  - `'#666'` → `'hsl(var(--muted-foreground) / 0.6)'`
  - MenuBar 容器背景 (line 157): `'rgba(26, 26, 26, 0.92)'` → `'hsl(var(--card) / 0.92)'`
  - MenuBar 底部边框 (line 160): `'rgba(255, 255, 255, 0.06)'` → `'hsl(var(--border))'`
  - backdropFilter → `'blur(var(--blur-panel))'`

**EntityRenderer.tsx:**
- **仅修改 UI 相关颜色** — 不影响 Three.js 材质色：
  - 选中实体描边色 → 保持 Three.js Color API（3D 渲染，非 UI token）
  - 任何 Three.js material.color / emissive → 不修改
  - **不修改** renderOrder, castShadow, receiveShadow 等 3D 渲染属性
  - 如果组件包含 UI 覆盖层（如标签/提示文字），替换其颜色为 token

**LoadingScreen.tsx:**
- 背景色 → `'hsl(var(--background))'`
- 文字色 → `'hsl(var(--muted-foreground))'`
- 加载动画边框色（如有）→ `'hsl(var(--primary))'`

**ErrorFallback.tsx:**
- 背景色 → `'hsl(var(--background))'`
- 错误标题文字 → `'hsl(var(--destructive))'`
- 描述文字 → `'hsl(var(--muted-foreground))'`
- 按钮使用 shadcn Button variant → 不修改组件代码（token 自动继承）

**Scene3D.tsx:**
- **3D 材质色不受 token 控制** — 仅替换 UI 相关部分:
  - Canvas 容器 wrapper (如果有) 的背景/边框 → token
  - Ground meshStandardMaterial color `'#1a1a1a'` → **保留不修改**（3D 渲染色，非 UI token）
  - Grid 辅助线颜色 → **不修改**（Three.js 原生设置）
  - GizmoHelper 颜色 → **不修改**
  - FpsTracker 使用 requestAnimationFrame — 无需修改
  - 如果文件中有内联 CSS-in-JS 用于 UI 元素（提示/标注），替换为 token
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; for f in src/components/ChartMetricTabs.tsx src/components/MenuBar.tsx src/components/LoadingScreen.tsx src/components/ErrorFallback.tsx; do rg_c=$(grep -c 'rgba(' "$f" 2>/dev/null || echo "0"); if [ "$rg_c" -gt 0 ]; then echo "FAIL: $f has $rg_c rgba()"; fi; done; echo "ROUND2_DONE"</automated>
  </verify>
  <acceptance_criteria>
    - ChartMetricTabs.tsx / MenuBar.tsx / LoadingScreen.tsx / ErrorFallback.tsx 不含 `rgba(` 硬编码（grep 0）
    - MenuBar.tsx 容器背景和底部边框使用 token（hsl(var(--card) / 0.92) / hsl(var(--border))）
    - EntityRenderer.tsx 的 Three.js material.color / emissive 值未被修改（grep diff 确认只有 UI 层变更）
    - Scene3D.tsx 的 Ground meshStandardMaterial color `'#1a1a1a'` 保持不变（3D 固定色）
    - 所有替换后的颜色值使用 `hsl(var(--*))` 或 `hsl(var(--*) / alpha)` 格式
    - TypeScript 编译通过
  </acceptance_criteria>
  <done>12 个组件文件的 token 迁移完成，Three.js 3D 材质色受保护，ChartCanvas 业务颜色不受影响（L-12）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新边界 | 本计划仅执行颜色 token 替换，不改变逻辑。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.1-15 | Tampering | Token 替换误伤 ChartCanvas 业务颜色 (L-12) | mitigate | ChartCanvas.tsx 不在本计划 files_modified 中；Task 2 的 EntityRenderer/Scene3D 明确标注"不修改 Three.js material.color"。 |
| T-01.1-16 | Denial of Service | 错误替换 background/foreground 导致整个 UI 不可读 | mitigate | 严格按映射表替换；执行后 `npx tsc --noEmit` 验证编译；此外建议 visual check（checkpoint:human-verify 应在执行本计划后触发）。 |
</threat_model>

<verification>
1. `grep 'rgba(' frontend/src/components/*.tsx` 在所有目标文件中 0 命中（排除 ChartCanvas / ChartPanel 已 token 化且 ChartCanvas 的业务色保留）
2. `npx tsc --noEmit -p frontend/tsconfig.json` 编译通过
3. Scene3D.tsx 中 `'#1a1a1a'` (Ground color) 未被修改
</verification>

<success_criteria>
1. 全部 12 个目标组件文件中的 UI 颜色（rgba/#/backdrop-blur）替换为 CSS 变量 token
2. ChartCanvas.tsx METRIC_COLORS 和 lightweight-charts 配置色不受影响（未在本计划 files_modified 中）
3. EntityRenderer.tsx / Scene3D.tsx 的 Three.js 3D 材质色保持原值
4. 所有替换严格遵循映射表（无遗漏，无过度替换）
5. TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-ui/01.1-08-SUMMARY.md`
</output>
