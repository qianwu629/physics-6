---
phase: 08-物理引擎重写
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - frontend/src/components/ForceFieldSystem.tsx
  - frontend/src/ecs/__tests__/forceFieldSystem.test.ts
autonomous: true
requirements:
  - FIELD-01
  - FIELD-02
must_haves:
  truths:
    - "ForceFieldSystem 每帧收集场源并计算场-源力"
    - "预设力场力与场-源力正确矢量叠加"
    - "addForce 前调用 resetForces 清除累积力"
    - "磁场处理先电场增量后 Rodriguez 旋转"
    - "DT 硬编码替换为 world.timestep 动态读取"
  artifacts:
    - path: "frontend/src/components/ForceFieldSystem.tsx"
      provides: "力注入系统（含场-源关系）"
      contains: "collectFieldSources"
      contains: "computeForceFromFieldSources"
      contains: "resetForces"
      contains: "world.timestep"
    - path: "frontend/src/ecs/__tests__/forceFieldSystem.test.ts"
      provides: "ForceFieldSystem 集成测试"
      min_tests: 5
  key_links:
    - from: "ForceFieldSystem.useBeforeStep"
      to: "fieldSourceCalc.collectFieldSources"
      via: "每帧收集场源数据"
    - from: "fieldSourceCalc.computeForceFromFieldSources"
      to: "body.addForce"
      via: "resetForces + addForce 安全注入"
    - from: "forceFieldCalc.updateVelocityWithEMField"
      to: "body.setLinvel"
      via: "改进的磁场能量守恒处理"
---

<objective>
改造 ForceFieldSystem 以支持场-源关系力计算，修复 addForce 累积问题，改进磁场处理顺序，动态读取 timeStep。

Purpose: 将场-源关系计算集成到现有力注入管道，确保物理正确性和数值稳定性。
Output: ForceFieldSystem.tsx 完整改造，集成测试通过。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-物理引擎重写/08-RESEARCH-CODE.md

## 现有 ForceFieldSystem.tsx 完整代码（已在上文 context 中提供）

关键问题（per 08-RESEARCH-CODE.md）：
1. 第 28 行：DT = 1/120 硬编码，应与 Physics timeStep 同步
2. 第 127-129 行：addForce 前未调用 resetForces，力会累积
3. 第 132-146 行：磁场处理未先应用电场速度增量，直接旋转原始速度
4. 缺少场-源关系收集和计算

## 新增模块（来自 Plan 08-02）

```typescript
// fieldSourceCalc.ts
export function collectFieldSources(
  entities: Map<string, Entity>,
  getRef: (id: string) => RefObject<RigidBodyAPI | null> | undefined,
  time: number,
): FieldSource[];

export function computeForceFromFieldSources(
  sources: FieldSource[],
  bodyPos: Vec3,
  bodyCharge: number,
  bodyId: string,
): Vec3;
```

## 改进函数（来自 Plan 08-02）

```typescript
// forceFieldCalc.ts
export function updateVelocityWithEMField(
  body: { setLinvel(vel: Vec3, wakeUp: boolean): void; resetForces(wakeUp: boolean): void },
  vel: Vec3,
  F_nonMag: Vec3,
  bodyB: Vec3,
  charge: number,
  mass: number,
  dt: number,
): Vec3;
```

## RigidBodyAPI 扩展（来自 Plan 08-01）

```typescript
export interface RigidBodyAPI {
  // ... 现有方法
  resetForces(wakeUp: boolean): void;  // 新增
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 改造 ForceFieldSystem.tsx</name>
  <files>frontend/src/components/ForceFieldSystem.tsx</files>
  <read_first>
    - frontend/src/components/ForceFieldSystem.tsx（完整文件，已在上文 context 中提供）
    - frontend/src/ecs/fieldSourceCalc.ts（Plan 08-02 产出）
  </read_first>
  <action>
    按以下步骤改造 ForceFieldSystem.tsx：

    ### 1. 导入更新
    在现有 import 基础上添加：
    ```typescript
    import { useRapier } from '@react-three/rapier';
    import {
      collectFieldSources,
      computeForceFromFieldSources,
    } from '../ecs/fieldSourceCalc';
    import type { FieldSourceComponent } from '../ecs/types';
    ```

    ### 2. 删除硬编码 DT
    删除 `const DT = 1 / 120;` 行。改为在 useBeforeStep 中动态读取：
    ```typescript
    const { world } = useRapier();
    // 在 useBeforeStep 内：
    const dt = world.timestep;
    ```

    ### 3. useBeforeStep 回调改造
    保留现有逻辑框架，在关键位置插入场-源关系代码：

    ```typescript
    useBeforeStep(() => {
      const state = useSimulationStore.getState();
      const isRunning = state.isRunning;
      const entities = state.entities;
      const dt = world.timestep;  // 动态读取，替代硬编码 DT

      // 暂停 → 运行：清空缓冲区（保持现有逻辑）
      if (isRunning && !wasRunningRef.current) {
        debugFramesRef.current = [];
        frameCounterRef.current = 0;
        // ... 现有力场快照逻辑
      }
      wasRunningRef.current = isRunning;
      frameCounterRef.current++;

      if (entities.size === 0) return;

      // ── 1. 收集所有力场和 dynamic 实体快照（保持现有逻辑）──
      const fields: ForceFieldComponent[] = [];
      const dynamicBodies: Array<{ entityId: string; rb: RigidBodyComponent; ref: any }> = [];

      for (const [entityId, entity] of entities) {
        const f = entity.components.get('forceField') as ForceFieldComponent | undefined;
        if (f) fields.push(f);

        const rb = entity.components.get('rigidBody') as RigidBodyComponent | undefined;
        if (rb && rb.kind === 'dynamic') {
          const ref = getRef(entityId);
          const body = ref?.current;
          if (body && typeof body.translation === 'function' && typeof body.addForce === 'function') {
            dynamicBodies.push({ entityId, rb, ref });
          }
        }
      }

      // ── 2. 收集场源（新增）──
      const time = performance.now() / 1000;
      const fieldSources = collectFieldSources(entities, getRef, time);

      // ── 3. 预先计算总磁场（保持现有逻辑）──
      const totalB = computeTotalMagneticField(fields, 1);

      // ── 4. 对 dynamic 刚体施加力（改造）──
      for (const { entityId, rb, ref } of dynamicBodies) {
        const body = ref.current;
        if (!body || typeof body.translation !== 'function') continue;
        const pos = body.translation();
        if (typeof body.linvel !== 'function') continue;
        const vel = body.linvel();
        const charge = rb.charge ?? 0;
        const mass = typeof body.mass === 'function' ? body.mass() : rb.mass;

        // 4a) 预设力场（非磁场）
        const F_preset = computeNonMagneticForce(fields, pos, charge);

        // 4b) 场-源关系力（新增）
        const F_sources = computeForceFromFieldSources(fieldSources, pos, charge, entityId);

        // 4c) 合力
        const F_total = {
          x: F_preset.x + F_sources.x,
          y: F_preset.y + F_sources.y,
          z: F_preset.z + F_sources.z,
        };

        // 4d) 安全注入：先清零再施加（修复 addForce 累积问题）
        if (F_total.x !== 0 || F_total.y !== 0 || F_total.z !== 0) {
          if (typeof body.resetForces === 'function') {
            body.resetForces(true);  // 清零旧力
          }
          if (typeof body.addForce === 'function') {
            body.addForce(F_total, true);
          }
        }

        // 4e) 磁场处理（改进：先电场增量，后磁场旋转）
        if (charge !== 0 && mass > 0) {
          const bodyB = computeTotalMagneticField(fields, charge);
          if (bodyB.x !== 0 || bodyB.y !== 0 || bodyB.z !== 0) {
            // 电场（含场-源电场）产生的速度增量
            const dv_electric = {
              x: (F_total.x / mass) * dt,
              y: (F_total.y / mass) * dt,
              z: (F_total.z / mass) * dt,
            };
            const vel_after_electric = {
              x: vel.x + dv_electric.x,
              y: vel.y + dv_electric.y,
              z: vel.z + dv_electric.z,
            };
            // 磁场旋转
            const newVel = rotateVelocityByMagneticField(vel_after_electric, bodyB, charge, mass, dt);
            if (typeof body.resetForces === 'function') {
              body.resetForces(true);  // 确保无累积力
            }
            body.setLinvel(newVel, true);
          }
        }

        // 4f) DEBUG 记录（保持现有逻辑，扩展为包含场-源信息）
        if (debugFramesRef.current.length < 50 && frameCounterRef.current % 12 === 0) {
          debugFramesRef.current.push({
            frame: frameCounterRef.current,
            px: +pos.x.toFixed(4),
            py: +pos.y.toFixed(4),
            pz: +pos.z.toFixed(4),
            vx: +vel.x.toFixed(4),
            vy: +vel.y.toFixed(4),
            vz: +vel.z.toFixed(4),
            Fx: +F_total.x.toFixed(6),
            Fy: +F_total.y.toFixed(6),
            Fz: +F_total.z.toFixed(6),
            q: charge,
            nFields: fields.length,
            nSources: fieldSources.length,  // 新增
            kinds: fields.map(f => f.kind).join(','),
            fSrcKinds: fieldSources.map(s => s.kind).join(','),  // 新增
          });
        }
      }
    });
    ```

    ### 4. 关键变更点总结
    | 位置 | 变更 | 原因 |
    |------|------|------|
    | import | 新增 useRapier, fieldSourceCalc | 动态读取 timestep + 场-源计算 |
    | DT 常量 | 删除 | 改用 world.timestep |
    | 场源收集 | 新增 collectFieldSources | 收集所有 fieldSource 实体 |
    | 力计算 | 新增 F_sources | 场-源关系力 |
    | 力注入 | resetForces + addForce | 防止力累积 |
    | 磁场处理 | 先电场增量后旋转 | 物理正确性 |
    | DEBUG | 新增 nSources, fSrcKinds | 诊断场-源关系 |

    注意：
    - 保持现有 DEBUG 输出格式，新增字段不影响现有诊断
    - resetForces 调用前检查 typeof === 'function' 以兼容旧版本 Rapier
    - fields.length === 0 时不再提前返回（因为 fieldSources 可能非空）
  </action>
  <verify>
    <automated>grep -n "collectFieldSources\|computeForceFromFieldSources\|resetForces\|world.timestep" frontend/src/components/ForceFieldSystem.tsx</automated>
  </verify>
  <done>
    - ForceFieldSystem.tsx 导入 fieldSourceCalc 和 useRapier
    - 硬编码 DT 已删除，使用 world.timestep
    - addForce 前调用 resetForces
    - 磁场处理先电场增量后 Rodriguez 旋转
    - 场-源力与预设力场力正确叠加
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ForceFieldSystem 集成测试</name>
  <files>frontend/src/ecs/__tests__/forceFieldSystem.test.ts</files>
  <behavior>
    - Test 1: 无场源时行为与 Phase 3 一致（预设力场正常工作）
    - Test 2: 单个电荷源对带电体产生正确方向的力（同号排斥、异号吸引）
    - Test 3: 两个电荷源力叠加（矢量合成）
    - Test 4: 自相互作用排除（带电体对自身无力）
    - Test 5: resetForces 在 addForce 前被调用
    - Test 6: 时变电荷源产生的力随时间变化
    - Test 7: 电荷为 0 的实体不受场-源力影响
  </behavior>
  <action>
    创建 frontend/src/ecs/__tests__/forceFieldSystem.test.ts，测试 ForceFieldSystem 的集成行为。

    由于 ForceFieldSystem 依赖 React hooks 和 Rapier WASM，采用以下测试策略：
    1. 测试场-源计算管道的纯函数部分（collectFieldSources + computeForceFromFieldSources 的集成）
    2. 用 mock RigidBodyAPI 测试力注入逻辑
    3. 验证 resetForces/addForce 调用顺序

    测试用例：

    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { collectFieldSources, computeForceFromFieldSources } from '../fieldSourceCalc';
    import { computeNonMagneticForce } from '../forceFieldCalc';
    import type { Entity, RigidBodyComponent, FieldSourceComponent } from '../types';

    describe('ForceFieldSystem integration — field-source pipeline', () => {
      it('preset fields + field sources force superposition', () => {
        // 验证预设力场力和场-源力正确叠加
      });

      it('collectFieldSources extracts charge sources correctly', () => {
        // 验证从 entities Map 正确提取场源
      });

      it('self-interaction is excluded', () => {
        // 验证 excludeId 排除自身
      });

      it('zero-charge body feels no field-source force', () => {
        // 验证 bodyCharge=0 → F=0
      });
    });
    ```

    具体实现：
    1. 创建 mock Entity Map（含 fieldSource + rigidBody 组件）
    2. 创建 mock getRef 函数（返回固定位置）
    3. 调用 collectFieldSources 验证输出
    4. 调用 computeForceFromFieldSources 验证力方向/大小
    5. 验证预设力场 + 场-源力的叠加
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/ecs/__tests__/forceFieldSystem.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - 集成测试 ≥7 个，全部通过
    - 覆盖预设力场兼容、场-源力计算、自相互作用排除、时变参数
    - 测试不依赖 Rapier WASM（纯 mock）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ForceFieldSystem → Rapier body | 力注入跨越 WASM-JS 边界，数值错误导致物理行为异常 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | D (拒绝服务) | addForce 力累积 | mitigate | 每帧 resetForces + addForce |
| T-08-08 | D (拒绝服务) | setLinvel 与积分器冲突 | mitigate | 先 resetForces 再 setLinvel |
| T-08-09 | T (篡改) | world.timestep 被篡改 | accept | timestep 由 Physics 组件控制，只读 |
| T-08-10 | D (拒绝服务) | 场源数量过多导致性能下降 | mitigate | 50 实体上限 + O(S×B) 复杂度可控 |
</threat_model>

<verification>
1. `grep -c "collectFieldSources" frontend/src/components/ForceFieldSystem.tsx` == 1
2. `grep -c "resetForces" frontend/src/components/ForceFieldSystem.tsx` >= 2
3. `grep -c "world.timestep" frontend/src/components/ForceFieldSystem.tsx` == 1
4. `grep -c "const DT" frontend/src/components/ForceFieldSystem.tsx` == 0
5. `cd frontend && npx vitest run src/ecs/__tests__/forceFieldSystem.test.ts` 全部通过
6. `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "error TS"` == 0
</verification>

<success_criteria>
- ForceFieldSystem.tsx 完整改造：场-源收集、力叠加、resetForces、world.timestep、改进磁场处理
- 集成测试 ≥7 个，全部通过
- 现有 forceFieldCalc.test.ts 不受影响（向后兼容）
- 无类型错误
</success_criteria>

<output>
After completion, create `.planning/phases/08-物理引擎重写/08-03-SUMMARY.md`
</output>
