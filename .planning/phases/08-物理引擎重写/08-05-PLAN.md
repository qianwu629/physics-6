---
phase: 08-物理引擎重写
plan: 05
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - frontend/src/components/PropertyPanel.tsx
  - frontend/src/store/entitySlice.ts
autonomous: true
requirements:
  - FIELD-03
must_haves:
  truths:
    - "PropertyPanel 中选中带 fieldSource 的实体时显示场源控制面板"
    - "场源类型切换（charge/current）可用"
    - "时变参数开关和滑块可用"
    - "电流源方向向量输入可用"
    - "运行中（isRunning=true）时所有字段只读"
  artifacts:
    - path: "frontend/src/components/PropertyPanel.tsx"
      provides: "场源控制面板 UI"
      contains: "fieldSource"
      contains: "kind"
      contains: "timeVarying"
    - path: "frontend/src/store/entitySlice.ts"
      provides: "fieldSource 组件 CRUD 支持"
      contains: "fieldSource"
  key_links:
    - from: "PropertyPanel fieldSource UI"
      to: "entitySlice.updateComponent"
      via: "updateComponent(entityId, 'fieldSource', {...})"
    - from: "RigidBodyComponent.charge"
      to: "FieldSourceComponent"
      via: "电荷源复用 rigidBody.charge，UI 显示但不重复编辑"
---

<objective>
在 PropertyPanel 中添加场源控制面板，支持创建/编辑/删除 fieldSource 组件。

Purpose: 用户可通过 UI 控制实体是否为场源、场源类型、时变参数。
Output: PropertyPanel 扩展场源折叠面板，entitySlice 支持 fieldSource CRUD。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-物理引擎重写/08-CONTEXT.md
@.planning/phases/03-通用力场系统/03-CONTEXT.md

## 现有 PropertyPanel.tsx 结构（已在上文 context 中完整提供）

关键模式：
1. 从 selectedEntity.components 提取各组件
2. 使用 PhysicsField / Vector3Field 渲染可编辑字段
3. 运行中（disabled=true）时显示只读值
4. updateComponent(entityId, componentType, partialData) 更新

现有组件提取（第 237-245 行）：
```typescript
const transform = selectedEntity?.components.get('transform') as TransformComponent | undefined;
const rigidBody = selectedEntity?.components.get('rigidBody') as RigidBodyComponent | undefined;
// ...
const forceField = selectedEntity?.components.get('forceField') as ForceFieldComponent | undefined;
```

## entitySlice.ts 模式（来自 Phase 3 CONTEXT）

updateComponent 签名：
```typescript
updateComponent: (entityId: string, type: ComponentType, data: Partial<Component>) => void
```

## FieldSourceComponent 结构（来自 Plan 08-01）

```typescript
interface FieldSourceComponent {
  type: 'fieldSource';
  kind: 'charge' | 'current';
  current?: { magnitude: number; direction: [number, number, number] };
  timeVarying?: { enabled: boolean; amplitude: number; frequency: number; phase: number };
}
```

## 决策映射

- D-08-05: 场-源关系 → UI 需支持 charge/current 切换
- D-08-06: 时变场 → UI 需支持 amplitude/frequency/phase 滑块
- D-08-12: 向后兼容 → UI 交互流程与 Phase 3 一致
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 entitySlice 支持 removeComponent</name>
  <files>frontend/src/store/entitySlice.ts</files>
  <read_first>
    - frontend/src/store/entitySlice.ts（完整文件，已在上文 context 中提供）
  </read_first>
  <action>
    在 entitySlice.ts 中添加 removeComponent action：

    1. **在 EntitySlice 接口中添加 removeComponent 声明**：
       ```typescript
       removeComponent: (entityId: string, type: ComponentType) => void;
       ```

    2. **在 createEntitySlice 实现中添加 removeComponent**：
       ```typescript
       removeComponent: (entityId: string, componentType: ComponentType) =>
         set((state) => {
           const entity = state.entities.get(entityId);
           if (!entity) return state;
           if (!entity.components.has(componentType)) return state;

           const newComponents = new Map(entity.components);
           newComponents.delete(componentType);
           const updatedEntity: Entity = { ...entity, components: newComponents };

           const nextEntities = new Map(state.entities);
           nextEntities.set(entityId, updatedEntity);
           return { entities: nextEntities };
         }),
       ```

    3. **验证 ComponentType 推导包含 'fieldSource'**：
       ComponentType 来自 AnyComponent['type']，由于 08-01 已将 FieldSourceComponent 加入 AnyComponent，'fieldSource' 自动成为合法的 ComponentType。

    注意：
    - removeComponent 是新增功能，用于 UI 中删除场源
    - 不修改 createEntity（fieldSource 不是默认组件）
    - updateComponent 应已支持 fieldSource（ComponentType 自动推导）
  </action>
  <verify>
    <automated>grep -n "removeComponent" frontend/src/store/entitySlice.ts</automated>
  </verify>
  <done>
    - entitySlice 接口包含 removeComponent 声明
    - createEntitySlice 实现 removeComponent，正确删除组件并返回新 Map
    - ComponentType 推导包含 'fieldSource'
  </done>
</task>

<task type="auto">
  <name>Task 2: 扩展 PropertyPanel 场源控制面板</name>
  <files>frontend/src/components/PropertyPanel.tsx</files>
  <read_first>
    - frontend/src/components/PropertyPanel.tsx（完整文件，已在上文 context 中提供）
  </read_first>
  <action>
    在 PropertyPanel.tsx 中添加场源控制面板，位于"物理参数"（电荷）之后、"初始速度"之前：

    ### 1. 导入更新
    在现有 type import 中添加：
    ```typescript
    import type {
      // ... 现有类型
      FieldSourceComponent,
      FieldSourceKind,
    } from '../ecs/types';
    ```

    ### 2. 从 store 获取 removeComponent
    ```typescript
    const removeComponent = useSimulationStore((s) => s.removeComponent);
    ```

    ### 3. 组件提取
    在现有组件提取代码后添加：
    ```typescript
    const fieldSource = selectedEntity?.components.get('fieldSource') as FieldSourceComponent | undefined;
    ```

    ### 4. 事件处理函数
    在现有 handler 之后添加：
    ```typescript
    // 场源开关
    const handleToggleFieldSource = useCallback(
      (enabled: boolean) => {
        if (!selectedEntityId) return;
        if (enabled) {
          // 添加 fieldSource 组件（默认 charge 类型）
          updateComponent(selectedEntityId, 'fieldSource', {
            type: 'fieldSource',
            kind: 'charge',
            timeVarying: { enabled: false, amplitude: 0, frequency: 0, phase: 0 },
          });
        } else {
          // 删除 fieldSource 组件 — 使用 removeComponent（per Task 1 已添加）
          removeComponent(selectedEntityId, 'fieldSource');
        }
      },
      [selectedEntityId, updateComponent, removeComponent],
    );

    const handleFieldSourceKindChange = useCallback(
      (kind: FieldSourceKind) => {
        if (!selectedEntityId || !fieldSource) return;
        const update: Partial<FieldSourceComponent> = { kind };
        if (kind === 'current') {
          update.current = { magnitude: 1, direction: [0, 1, 0] };
        } else {
          update.current = undefined;
        }
        updateComponent(selectedEntityId, 'fieldSource', update);
      },
      [selectedEntityId, fieldSource, updateComponent],
    );

    const handleTimeVaryingToggle = useCallback(
      (enabled: boolean) => {
        if (!selectedEntityId || !fieldSource) return;
        updateComponent(selectedEntityId, 'fieldSource', {
          timeVarying: {
            enabled,
            amplitude: fieldSource.timeVarying?.amplitude ?? 1,
            frequency: fieldSource.timeVarying?.frequency ?? 1,
            phase: fieldSource.timeVarying?.phase ?? 0,
          },
        });
      },
      [selectedEntityId, fieldSource, updateComponent],
    );

    const handleTimeVaryingParamChange = useCallback(
      (key: 'amplitude' | 'frequency' | 'phase', val: number) => {
        if (!selectedEntityId || !fieldSource) return;
        updateComponent(selectedEntityId, 'fieldSource', {
          timeVarying: {
            enabled: fieldSource.timeVarying?.enabled ?? false,
            amplitude: fieldSource.timeVarying?.amplitude ?? 0,
            frequency: fieldSource.timeVarying?.frequency ?? 0,
            phase: fieldSource.timeVarying?.phase ?? 0,
            [key]: val,
          },
        });
      },
      [selectedEntityId, fieldSource, updateComponent],
    );

    const handleCurrentMagnitudeChange = useCallback(
      (val: number) => {
        if (!selectedEntityId || !fieldSource || fieldSource.kind !== 'current') return;
        updateComponent(selectedEntityId, 'fieldSource', {
          current: {
            magnitude: val,
            direction: fieldSource.current?.direction ?? [0, 1, 0],
          },
        });
      },
      [selectedEntityId, fieldSource, updateComponent],
    );

    const handleCurrentDirectionChange = useCallback(
      (index: number, val: number) => {
        if (!selectedEntityId || !fieldSource || fieldSource.kind !== 'current') return;
        const newDir: [number, number, number] = [
          ...(fieldSource.current?.direction ?? [0, 1, 0]),
        ];
        newDir[index] = val;
        updateComponent(selectedEntityId, 'fieldSource', {
          current: {
            magnitude: fieldSource.current?.magnitude ?? 1,
            direction: newDir,
          },
        });
      },
      [selectedEntityId, fieldSource, updateComponent],
    );
    ```

    ### 5. UI 渲染
    在"物理参数"区域（电荷之后）添加场源折叠面板：

    ```tsx
    {/* 场源设置 — Phase 8 */}
    <div className="space-y-2">
      <div className="flex items-center justify-between">
        <span className="text-sm" style={{ color: '#a0a0a0' }}>场源</span>
        <Switch
          checked={!!fieldSource}
          onCheckedChange={handleToggleFieldSource}
          disabled={disabled}
        />
      </div>

      {fieldSource && (
        <div className="space-y-2 pl-2 border-l border-white/10">
          {/* 场源类型 */}
          <div className="flex items-center gap-2">
            <span className="text-xs" style={{ color: '#888' }}>类型</span>
            <div className="flex gap-1">
              {(['charge', 'current'] as FieldSourceKind[]).map((k) => (
                <button
                  key={k}
                  type="button"
                  className={`px-2 py-0.5 text-xs rounded ${
                    fieldSource.kind === k
                      ? 'bg-blue-500/20 text-blue-400 border border-blue-500/30'
                      : 'bg-white/5 text-gray-400 border border-white/10'
                  }`}
                  onClick={() => handleFieldSourceKindChange(k)}
                  disabled={disabled}
                >
                  {k === 'charge' ? '电荷源' : '电流源'}
                </button>
              ))}
            </div>
          </div>

          {/* 电荷源提示 */}
          {fieldSource.kind === 'charge' && (
            <div className="text-xs" style={{ color: '#666' }}>
              电荷值取自物理参数中的"电荷"字段
            </div>
          )}

          {/* 电流源参数 */}
          {fieldSource.kind === 'current' && (
            <div className="space-y-2">
              <PhysicsField
                label="电流大小"
                value={fieldSource.current?.magnitude ?? 1}
                unit="A"
                min={0}
                max={100}
                step={0.1}
                disabled={disabled}
                onChange={handleCurrentMagnitudeChange}
              />
              <Vector3Field
                label="电流方向"
                value={fieldSource.current?.direction ?? [0, 1, 0]}
                min={-1}
                max={1}
                step={0.1}
                disabled={disabled}
                onChange={handleCurrentDirectionChange}
              />
            </div>
          )}

          {/* 时变参数 */}
          <div className="space-y-2 pt-1">
            <div className="flex items-center justify-between">
              <span className="text-xs" style={{ color: '#888' }}>时变场</span>
              <Switch
                checked={fieldSource.timeVarying?.enabled ?? false}
                onCheckedChange={handleTimeVaryingToggle}
                disabled={disabled}
              />
            </div>
            {fieldSource.timeVarying?.enabled && (
              <div className="space-y-2">
                <PhysicsField
                  label="振幅"
                  value={fieldSource.timeVarying.amplitude}
                  unit={fieldSource.kind === 'charge' ? 'C' : 'A'}
                  min={0}
                  max={10}
                  step={0.1}
                  disabled={disabled}
                  onChange={(v) => handleTimeVaryingParamChange('amplitude', v)}
                />
                <PhysicsField
                  label="频率"
                  value={fieldSource.timeVarying.frequency}
                  unit="Hz"
                  min={0}
                  max={10}
                  step={0.1}
                  disabled={disabled}
                  onChange={(v) => handleTimeVaryingParamChange('frequency', v)}
                />
                <PhysicsField
                  label="初相位"
                  value={fieldSource.timeVarying.phase}
                  unit="rad"
                  min={0}
                  max={6.28}
                  step={0.1}
                  disabled={disabled}
                  onChange={(v) => handleTimeVaryingParamChange('phase', v)}
                />
              </div>
            )}
          </div>
        </div>
      )}
    </div>
    ```
  </action>
  <verify>
    <automated>grep -n "fieldSource\|handleToggleFieldSource\|handleFieldSourceKindChange\|handleTimeVarying" frontend/src/components/PropertyPanel.tsx | head -20</automated>
  </verify>
  <done>
    - PropertyPanel 包含场源开关（Switch）
    - 场源类型切换按钮（charge/current）
    - 电流源参数（magnitude + direction）
    - 时变参数开关 + amplitude/frequency/phase 滑块
    - 运行中所有字段 disabled
    - 电荷源提示"电荷值取自物理参数中的电荷字段"
    - 关闭场源时调用 removeComponent 正确删除组件
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PropertyPanel → entitySlice | UI 状态变更通过 Zustand 传播，需确保数据一致性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-14 | D (拒绝服务) | 时变参数频率过高 | mitigate | UI 限制 frequency max=10Hz，fieldSourceCalc 内部限制 1200Hz |
| T-08-15 | D (拒绝服务) | 电流方向向量未归一化 | mitigate | fieldSourceCalc 内部归一化，UI 不强制要求单位向量 |
| T-08-16 | I (信息泄露) | 场源参数暴露 | accept | 仿真参数非敏感信息 |
</threat_model>

<verification>
1. `grep -c "fieldSource" frontend/src/components/PropertyPanel.tsx` >= 5
2. `grep -c "handleToggleFieldSource" frontend/src/components/PropertyPanel.tsx` == 1
3. `grep -c "handleFieldSourceKindChange" frontend/src/components/PropertyPanel.tsx` == 1
4. `grep -c "removeComponent" frontend/src/store/entitySlice.ts` >= 1
5. `cd frontend && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c "error TS"` == 0
</verification>

<success_criteria>
- PropertyPanel 显示场源控制面板（开关 + 类型 + 时变参数）
- 电荷源显示提示"电荷值取自物理参数中的电荷字段"
- 电流源显示 magnitude + direction 输入
- 时变场显示 amplitude/frequency/phase 滑块
- 运行中所有字段 disabled
- entitySlice 支持 removeComponent
- 关闭场源时 removeComponent 正确删除 fieldSource 组件
- 无类型错误
</success_criteria>

<output>
After completion, create `.planning/phases/08-物理引擎重写/08-05-SUMMARY.md`
</output>
