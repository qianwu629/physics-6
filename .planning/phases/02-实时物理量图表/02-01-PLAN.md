---
phase: 02-实时物理量图表
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/store/chartDataStore.ts
  - frontend/src/store/chartBuffer.ts
  - frontend/src/store/__tests__/chartBuffer.test.ts
  - frontend/src/store/simulationSlice.ts
  - frontend/src/store/index.ts
  - frontend/package.json
autonomous: true
requirements:
  - CHART-01
  - CHART-03

must_haves:
  truths:
    - "npm install 成功安装 lightweight-charts@5.2.0 + react-draggable@4.5.0 + re-resizable@6.11.2"
    - "chartDataStore.ts 导出 Zustand store，含 trackedEntityIds / timeWindow / layoutMode / visibleMetrics / peReferenceY"
    - "chartBuffer.ts 导出 ChartDataBuffer 类，Float64Array 环形缓冲，MAX_POINTS=500000，支持 12 指标/实体"
    - "simulationSlice.ts 的 EnvironmentState 新增 peReferenceY: number（默认 0）"
    - "chartBuffer.test.ts 通过 V-CHART-03（内存上限）和 V-CHART-05（重置清空）和 V-CHART-06（时间窗口不修改缓冲）"
  artifacts:
    - path: "frontend/src/store/chartDataStore.ts"
      provides: "Zustand 配置层 — 追踪开关、时间窗口、布局模式、可见指标、势能参考高度"
      exports: ["useChartDataStore", "ChartConfigState"]
      min_lines: 60
    - path: "frontend/src/store/chartBuffer.ts"
      provides: "Float64Array 环形缓冲区 — 每实体 12 指标"
      exports: ["ChartDataBuffer", "METRICS_PER_ENTITY", "MAX_POINTS"]
      min_lines: 80
    - path: "frontend/src/store/__tests__/chartBuffer.test.ts"
      provides: "环形缓冲区单元测试"
      contains: "describe('ChartDataBuffer'"
  key_links:
    - from: "chartDataStore.ts trackedEntityIds"
      to: "PropertyPanel 图表开关"
      via: "toggleTracking(entityId)"
      pattern: "toggleTracking"
    - from: "chartBuffer.ts push(time, metrics)"
      to: "ChartSampler.ts 采样循环"
      via: "模块级 Map<string, ChartDataBuffer>"
      pattern: "chartBuffers.get(entityId)?.push"
    - from: "simulationSlice.ts peReferenceY"
      to: "EnvironmentPanel 滑块输入"
      via: "setPeReferenceY(y)"
      pattern: "peReferenceY"
---

<objective>
安装 Phase 2 所需 npm 依赖，并构建数据层骨架：chartDataStore（Zustand 配置层）+ chartBuffer（Float64Array 环形缓冲）+ simulationSlice 扩展（peReferenceY）。这是所有后续图表功能的基础——没有数据层，采样和渲染无从谈起。

Purpose: 为实时物理量图表提供高性能数据存储和配置管理。遵循 Phase 1 的 "Zustand 配置 + Ref 数据分离" 模式。
Output: 3 个新文件 + 2 个修改文件 + 单元测试
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-实时物理量图表/02-CONTEXT.md (D-02-05, D-02-06, D-02-08, D-02-09)
@.planning/phases/02-实时物理量图表/02-RESEARCH.md (Pattern 2, Pattern 3, Pitfall #1, Pitfall #6)
@.planning/phases/02-实时物理量图表/PATTERNS.md (chartDataStore 模式, chartBuffer 模式)

<interfaces>
从 frontend/src/store/visualizationStore.ts:
```typescript
import { create } from 'zustand';
export const useVisualizationStore = create<VisualizationState>()(
  persist((set) => ({ showTrails: true, toggleTrails: () => set((s) => ({ showTrails: !s.showTrails })) }), { name: 'physis-visualization' })
);
```

从 frontend/src/store/simulationSlice.ts (当前):
```typescript
export interface EnvironmentState {
  gravity: [number, number, number];
  frictionScale: number;
  restitutionScale: number;
  drag: number;
}
export const DEFAULT_ENVIRONMENT: EnvironmentState = { gravity: [0, -9.81, 0], frictionScale: 1.0, restitutionScale: 1.0, drag: 0.1 };
```

从 frontend/src/ecs/TrajectoryBuffer.ts:
```typescript
export class TrajectoryBuffer {
  private positions: Float32Array;
  private timestamps: Float32Array;
  private head = 0;
  private count = 0;
  push(position: Vector3, time: number): void;
  getPoints(currentTime: number): { positions: Vector3[]; count: number };
  clear(): void;
}
```

从 frontend/src/store/index.ts:
```typescript
export const useSimulationStore = create<SimulationSlice & EntitySlice & UiSlice>()((...args) => ({
  ...createSimulationSlice(...args),
  ...createEntitySlice(...args),
  ...createUiSlice(...args),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 npm 依赖</name>
  <files>frontend/package.json</files>
  <action>
在 `frontend/` 目录下执行：

```bash
npm install lightweight-charts@5.2.0 react-draggable@4.5.0 re-resizable@6.11.2
npm install -D @types/react-draggable
```

验证安装成功：
```bash
cat frontend/package.json | grep -E "lightweight-charts|react-draggable|re-resizable"
```

预期输出包含三个包的版本号。
  </action>
  <verify>
    <automated>cd frontend && cat package.json | grep -E "lightweight-charts|react-draggable|re-resizable" | wc -l</automated>
  </verify>
  <done>package.json 中包含 lightweight-charts@5.2.0、react-draggable@4.5.0、re-resizable@6.11.2</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 chartBuffer.ts — Float64Array 环形缓冲区</name>
  <files>frontend/src/store/chartBuffer.ts, frontend/src/store/__tests__/chartBuffer.test.ts</files>
  <behavior>
    - Test 1: new ChartDataBuffer() 后 count === 0
    - Test 2: push 一次后 count === 1，getSeriesData(0, 0, 100) 返回 1 条数据
    - Test 3: push 500001 次后 count === 500000（MAX_POINTS 上限）
    - Test 4: push 500001 次后 head 正确回绕，getSeriesData 返回按时间顺序的数据
    - Test 5: clear() 后 count === 0，再次 push 从索引 0 开始
    - Test 6: getSeriesData(metricIndex, startTime, endTime) 只返回时间范围内的数据，不修改底层缓冲
    - Test 7: 同时存储 12 个指标，getSeriesData 按 metricIndex 返回正确指标值
    - Test 8: 时间戳使用秒级单位（performance.now() / 1000）
  </behavior>
  <action>
创建 `frontend/src/store/chartBuffer.ts`：

```typescript
/**
 * ChartDataBuffer — Float64Array 环形缓冲区
 *
 * 每实体一个实例，存储 12 个物理量指标：
 *   0:x, 1:y, 2:z, 3:vx, 4:vy, 5:vz, 6:ax, 7:ay, 8:az, 9:KE, 10:PE, 11:TotalE
 *
 * D-02-05: MAX_POINTS = 500_000（约 10 分钟 @ 60Hz）
 * D-02-06: 不经过 Zustand，模块级独立存储
 */

export const MAX_POINTS = 500_000;
export const METRICS_PER_ENTITY = 12;

export class ChartDataBuffer {
  private data: Float64Array;
  private timestamps: Float64Array;
  private head = 0;
  private count = 0;

  constructor() {
    this.data = new Float64Array(MAX_POINTS * METRICS_PER_ENTITY);
    this.timestamps = new Float64Array(MAX_POINTS);
  }

  /** 写入一组指标（length 必须为 METRICS_PER_ENTITY） */
  push(time: number, metrics: Float64Array): void {
    if (metrics.length !== METRICS_PER_ENTITY) {
      throw new Error(`metrics length must be ${METRICS_PER_ENTITY}, got ${metrics.length}`);
    }
    const idx = this.head * METRICS_PER_ENTITY;
    this.data.set(metrics, idx);
    this.timestamps[this.head] = time;
    this.head = (this.head + 1) % MAX_POINTS;
    this.count = Math.min(this.count + 1, MAX_POINTS);
  }

  /**
   * 获取指定指标、指定时间范围的数据点
   * 返回 {time, value}[] 供 lightweight-charts 消费
   * 不修改底层缓冲（V-CHART-06）
   */
  getSeriesData(
    metricIndex: number,
    startTime: number,
    endTime: number,
  ): { time: number; value: number }[] {
    const result: { time: number; value: number }[] = [];
    const start = this.count < MAX_POINTS ? 0 : this.head;
    for (let i = 0; i < this.count; i++) {
      const bufIdx = (start + i) % MAX_POINTS;
      const t = this.timestamps[bufIdx];
      if (t >= startTime && t <= endTime) {
        result.push({
          time: t,
          value: this.data[bufIdx * METRICS_PER_ENTITY + metricIndex],
        });
      }
    }
    return result;
  }

  /** 获取全部时间范围内的指定指标数据（用于"全程"视图） */
  getAllSeriesData(metricIndex: number): { time: number; value: number }[] {
    const result: { time: number; value: number }[] = [];
    const start = this.count < MAX_POINTS ? 0 : this.head;
    for (let i = 0; i < this.count; i++) {
      const bufIdx = (start + i) % MAX_POINTS;
      result.push({
        time: this.timestamps[bufIdx],
        value: this.data[bufIdx * METRICS_PER_ENTITY + metricIndex],
      });
    }
    return result;
  }

  clear(): void {
    this.head = 0;
    this.count = 0;
  }

  getCount(): number {
    return this.count;
  }
}

/** 模块级全局缓冲区映射 — key = entityId */
export const chartBuffers = new Map<string, ChartDataBuffer>();

/** 获取或创建实体的缓冲区 */
export function getOrCreateBuffer(entityId: string): ChartDataBuffer {
  let buf = chartBuffers.get(entityId);
  if (!buf) {
    buf = new ChartDataBuffer();
    chartBuffers.set(entityId, buf);
  }
  return buf;
}

/** 清空所有缓冲区（重置时调用） */
export function clearAllBuffers(): void {
  for (const buf of chartBuffers.values()) {
    buf.clear();
  }
  chartBuffers.clear();
}
```

创建 `frontend/src/store/__tests__/chartBuffer.test.ts`，覆盖上述 8 个 behavior case。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/store/__tests__/chartBuffer.test.ts --reporter=verbose</automated>
  </verify>
  <done>ChartDataBuffer 类可用；8 个单元测试全部通过；500K 上限和环形回绕行为正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 创建 chartDataStore.ts + 扩展 simulationSlice.ts（peReferenceY）</name>
  <files>frontend/src/store/chartDataStore.ts, frontend/src/store/simulationSlice.ts, frontend/src/store/index.ts</files>
  <behavior>
    - Test 1: useChartDataStore 初始状态：trackedEntityIds 为空 Set，timeWindow='30s'，layoutMode='overlay'，visibleMetrics 包含全部 4 类，peReferenceY=0
    - Test 2: toggleTracking('e1') 后 trackedEntityIds 包含 'e1'；再次 toggle 后移除
    - Test 3: setTimeWindow('5s') 后 timeWindow === '5s'
    - Test 4: setLayoutMode('separate') 后 layoutMode === 'separate'
    - Test 5: setVisibleMetrics 只保留 'position' 后，visibleMetrics.size === 1
    - Test 6: setPeReferenceY(5.5) 后 peReferenceY === 5.5
  </behavior>
  <action>
创建 `frontend/src/store/chartDataStore.ts`：

```typescript
import { create } from 'zustand';

export type TimeWindow = '5s' | '30s' | 'all';
export type LayoutMode = 'overlay' | 'separate';
export type MetricType = 'position' | 'velocity' | 'acceleration' | 'energy';

export interface ChartConfigState {
  trackedEntityIds: Set<string>;
  timeWindow: TimeWindow;
  layoutMode: LayoutMode;
  visibleMetrics: Set<MetricType>;
  peReferenceY: number;

  toggleTracking: (id: string) => void;
  setTimeWindow: (w: TimeWindow) => void;
  setLayoutMode: (m: LayoutMode) => void;
  setVisibleMetrics: (metrics: Set<MetricType>) => void;
  setPeReferenceY: (y: number) => void;
}

export const useChartDataStore = create<ChartConfigState>()((set) => ({
  trackedEntityIds: new Set(),
  timeWindow: '30s',
  layoutMode: 'overlay',
  visibleMetrics: new Set<MetricType>(['position', 'velocity', 'acceleration', 'energy']),
  peReferenceY: 0,

  toggleTracking: (id) =>
    set((s) => {
      const next = new Set(s.trackedEntityIds);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return { trackedEntityIds: next };
    }),
  setTimeWindow: (w) => set({ timeWindow: w }),
  setLayoutMode: (m) => set({ layoutMode: m }),
  setVisibleMetrics: (metrics) => set({ visibleMetrics: metrics }),
  setPeReferenceY: (y) => set({ peReferenceY: y }),
}));
```

修改 `frontend/src/store/simulationSlice.ts`：
1. 在 `EnvironmentState` 接口中添加 `peReferenceY: number`
2. 在 `DEFAULT_ENVIRONMENT` 中添加 `peReferenceY: 0`
3. 在 `SimulationSlice` 接口中添加 `setPeReferenceY: (y: number) => void`
4. 在 `createSimulationSlice` 实现中添加：
   ```typescript
   setPeReferenceY: (y) => set((s) => ({ environment: { ...s.environment, peReferenceY: y } })),
   ```

修改 `frontend/src/store/index.ts`：
- 导入并导出 `useChartDataStore`（独立 store，不合并到 useSimulationStore）
  ```typescript
  export { useChartDataStore } from './chartDataStore';
  ```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2&gt;&1 | grep -i "chartDataStore\|peReferenceY" | wc -l</automated>
  </verify>
  <done>chartDataStore 可用；simulationSlice 扩展 peReferenceY；TypeScript 编译无新增错误</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chartBuffer Float64Array 索引计算 | 内部模块，无外部输入直接操作 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | chartBuffer 索引溢出 | mitigate | push 中使用模运算，head 指针严格 % MAX_POINTS；metricIndex 由内部常量控制 |
| T-02-02 | Denial of Service | 缓冲区无限增长 | mitigate | MAX_POINTS = 500_000 硬上限，超限时覆盖最旧数据 |
</threat_model>

<verification>
### 自动化验证

```bash
cd frontend
# 依赖安装验证
grep -E "lightweight-charts|react-draggable|re-resizable" package.json

# 类型检查
npx tsc --noEmit

# 单元测试
npx vitest run src/store/__tests__/chartBuffer.test.ts --reporter=verbose
```
</verification>

<success_criteria>
1. lightweight-charts@5.2.0、react-draggable@4.5.0、re-resizable@6.11.2 安装成功
2. chartBuffer.ts 导出 ChartDataBuffer 类，Float64Array 环形缓冲，MAX_POINTS=500000，METRICS_PER_ENTITY=12
3. chartBuffer.test.ts 8 个 test case 全部 PASS（含 500K 上限、重置清空、时间窗口不修改缓冲）
4. chartDataStore.ts 导出 useChartDataStore，含 toggleTracking / setTimeWindow / setLayoutMode / setVisibleMetrics / setPeReferenceY
5. simulationSlice.ts 的 EnvironmentState 含 peReferenceY（默认 0），并提供 setPeReferenceY action
6. TypeScript 编译通过，无新增错误
</success_criteria>

<output>
After completion, create `.planning/phases/02-实时物理量图表/02-01-SUMMARY.md`
</output>
