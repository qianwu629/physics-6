---
phase: 02-实时物理量图表
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - frontend/src/components/ChartPanel.tsx
  - frontend/src/components/__tests__/ChartPanel.test.tsx
autonomous: false
requirements:
  - CHART-02
  - CHART-03

must_haves:
  truths:
    - "ChartPanel.tsx 是浮动面板，position: fixed，z-index 40"
    - "react-draggable 实现拖拽（handle = panel-header），bounds=parent"
    - "re-resizable 实现大小调整，min 320x200，max 1200x800"
    - "面板顶部显示时间窗口按钮 5s/30s/全程，切换时只改 visibleRange"
    - "面板内部显示 ChartMetricTabs + ChartCanvas（当前激活指标）"
    - "overlay 模式下单 Chart 显示所有追踪实体曲线；separate 模式每实体独立子 Chart"
    - "面板可关闭，关闭后不影响采样（数据继续写入 buffer）"
  artifacts:
    - path: "frontend/src/components/ChartPanel.tsx"
      provides: "浮动图表面板（拖拽 + resize + Tab + 时间窗口 + 布局切换）"
      exports: ["ChartPanel"]
      min_lines: 180
    - path: "frontend/src/components/__tests__/ChartPanel.test.tsx"
      provides: "ChartPanel 交互测试"
      contains: "describe('ChartPanel'"
  key_links:
    - from: "ChartPanel.tsx"
      to: "ChartCanvas.tsx"
      via: "ref 调用 refreshAll() 和 setTimeWindow()"
      pattern: "chartCanvasRef"
    - from: "ChartPanel.tsx"
      to: "chartDataStore.ts"
      via: "读取 timeWindow / layoutMode / trackedEntityIds，调用 setTimeWindow / setLayoutMode"
      pattern: "useChartDataStore"
    - from: "ChartPanel.tsx"
      to: "ChartMetricTabs.tsx"
      via: "activeMetric state 传递给 ChartMetricTabs 和 ChartCanvas"
      pattern: "activeMetric"
---

<objective>
构建浮动图表面板 ChartPanel.tsx。整合 react-draggable（拖拽）+ re-resizable（调整大小）+ ChartMetricTabs（Tab 切换）+ ChartCanvas（图表渲染）+ 时间窗口按钮 + 布局模式切换。这是用户直接交互的图表容器。

Purpose: 提供可拖拽、可调整大小的浮动图表面板，让用户在 3D 场景旁实时观察物理量曲线。遵循 D-02-02（叠加/分离双模式）和 D-02-03（时间窗口切换）。
Output: ChartPanel.tsx + 测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-实时物理量图表/02-CONTEXT.md (D-02-02, D-02-03)
@.planning/phases/02-实时物理量图表/02-RESEARCH.md (Pattern: 浮动面板实现, Code Example: react-draggable + re-resizable)
@.planning/phases/02-实时物理量图表/PATTERNS.md (ChartPanel 模式 — SnapshotManager/PropertyPanel 类比)

<interfaces>
从 frontend/src/components/ChartCanvas.tsx (Plan 03 产出):
```typescript
export interface ChartCanvasHandle {
  refreshAll: () => void;
  setTimeWindow: (window: '5s' | '30s' | 'all') => void;
}
export const ChartCanvas = forwardRef<ChartCanvasHandle, { metric: MetricType }>(...);
```

从 frontend/src/components/ChartMetricTabs.tsx (Plan 03 产出):
```typescript
export function ChartMetricTabs({ activeMetric, onChange }: { activeMetric: MetricType; onChange: (metric: MetricType) => void }): JSX.Element;
```

从 RESEARCH.md 浮动面板示例:
```typescript
import Draggable from 'react-draggable';
import { Resizable } from 're-resizable';
// Draggable handle=".panel-header" bounds="parent" cancel=".react-resizable-handle"
// Resizable defaultSize={{ width: 600, height: 400 }} minWidth={320} minHeight={200}
```

从 PATTERNS.md (PropertyPanel 固定定位面板):
```typescript
<div className="fixed z-40 rounded-xl flex flex-col" style={{
  right: '16px', top: '80px',
  backgroundColor: 'rgba(26, 26, 26, 0.85)',
  backdropFilter: 'blur(8px)',
  border: '1px solid rgba(255, 255, 255, 0.08)',
}}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ChartPanel.tsx — 浮动图表面板</name>
  <files>frontend/src/components/ChartPanel.tsx, frontend/src/components/__tests__/ChartPanel.test.tsx</files>
  <behavior>
    - Test 1: 渲染后面板容器存在，包含 header、ChartMetricTabs、ChartCanvas
    - Test 2: 点击时间窗口按钮（5s/30s/全程）触发 setTimeWindow
    - Test 3: 点击 layoutMode 切换按钮在 'overlay' 和 'separate' 间切换
    - Test 4: 点击关闭按钮触发 onClose
    - Test 5: overlay 模式下渲染 1 个 ChartCanvas；separate 模式下渲染 N 个（N = trackedEntityIds.size）
    - Test 6: 拖拽 handle 为 panel-header，resize handle 不触发拖拽
  </behavior>
  <action>
创建 `frontend/src/components/ChartPanel.tsx`：

```typescript
import { useState, useRef, useCallback, useEffect } from 'react';
import Draggable from 'react-draggable';
import { Resizable } from 're-resizable';
import { X, LayoutTemplate, LayoutGrid } from 'lucide-react';
import { useChartDataStore, type MetricType } from '../store/chartDataStore';
import { ChartCanvas, type ChartCanvasHandle } from './ChartCanvas';
import { ChartMetricTabs } from './ChartMetricTabs';

interface ChartPanelProps {
  open: boolean;
  onClose: () => void;
}

export function ChartPanel({ open, onClose }: ChartPanelProps) {
  const [activeMetric, setActiveMetric] = useState<MetricType>('position');
  const chartCanvasRef = useRef<ChartCanvasHandle>(null);

  const timeWindow = useChartDataStore((s) => s.timeWindow);
  const setTimeWindow = useChartDataStore((s) => s.setTimeWindow);
  const layoutMode = useChartDataStore((s) => s.layoutMode);
  const setLayoutMode = useChartDataStore((s) => s.setLayoutMode);
  const trackedEntityIds = useChartDataStore((s) => s.trackedEntityIds);

  // 每 60Hz 刷新图表渲染（仅调用 lightweight-charts update()，不采样物理数据）
  // 采样在 ChartSampler.useFrame 中共享 R3F rAF；此处 rAF 仅驱动图表视口更新
  // 若实测 FPS 下降，可将 refreshAll 合并到 ChartSampler 的 useFrame 中
  useEffect(() => {
    if (!open) return;
    let rafId: number;
    const tick = () => {
      chartCanvasRef.current?.refreshAll();
      rafId = requestAnimationFrame(tick);
    };
    rafId = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(rafId);
  }, [open, activeMetric]);

  const handleTimeWindowChange = useCallback(
    (window: '5s' | '30s' | 'all') => {
      setTimeWindow(window);
      chartCanvasRef.current?.setTimeWindow(window);
    },
    [setTimeWindow]
  );

  if (!open) return null;

  const entityArray = Array.from(trackedEntityIds);

  return (
    <Draggable
      handle=".panel-header"
      bounds="parent"
      cancel=".react-resizable-handle, button, .panel-body"
    >
      <Resizable
        defaultSize={{ width: 640, height: 420 }}
        minWidth={320}
        minHeight={200}
        maxWidth={1200}
        maxHeight={800}
        enable={{
          top: false,
          right: true,
          bottom: true,
          left: false,
          topRight: false,
          bottomRight: true,
          bottomLeft: false,
          topLeft: false,
        }}
        onResizeStop={() => {
          // Resize 完成后通知 chart 调整尺寸
          chartCanvasRef.current?.refreshAll();
        }}
      >
        <div
          className="panel-container fixed z-40 rounded-xl flex flex-col overflow-hidden"
          style={{
            right: '16px',
            top: '80px',
            width: '100%',
            height: '100%',
            backgroundColor: 'rgba(26, 26, 26, 0.9)',
            backdropFilter: 'blur(12px)',
            WebkitBackdropFilter: 'blur(12px)',
            border: '1px solid rgba(255, 255, 255, 0.08)',
            boxShadow: '0 8px 32px rgba(0, 0, 0, 0.4)',
          }}
        >
          {/* Header */}
          <div
            className="panel-header flex items-center justify-between px-3 shrink-0 cursor-move"
            style={{ height: '40px', borderBottom: '1px solid rgba(255,255,255,0.06)' }}
          >
            <div className="flex items-center gap-2">
              <span className="text-sm font-semibold" style={{ color: '#e0e0e0' }}>
                实时物理量图表
              </span>
              <span className="text-xs" style={{ color: '#666' }}>
                ({trackedEntityIds.size}/4 实体)
              </span>
            </div>

            <div className="flex items-center gap-1.5">
              {/* 布局模式切换 */}
              <button
                type="button"
                onClick={() => setLayoutMode(layoutMode === 'overlay' ? 'separate' : 'overlay')}
                className="rounded p-1 hover:bg-white/5 transition-colors"
                title={layoutMode === 'overlay' ? '切换为分离模式' : '切换为叠加模式'}
              >
                {layoutMode === 'overlay' ? (
                  <LayoutTemplate size={14} style={{ color: '#a0a0a0' }} />
                ) : (
                  <LayoutGrid size={14} style={{ color: '#a0a0a0' }} />
                )}
              </button>

              {/* 时间窗口按钮 */}
              {(['5s', '30s', 'all'] as const).map((w) => (
                <button
                  key={w}
                  type="button"
                  onClick={() => handleTimeWindowChange(w)}
                  className={`px-2 py-0.5 rounded text-xs transition-all
                    ${timeWindow === w
                      ? 'bg-[rgba(59,130,246,0.2)] border border-[#3b82f6] text-[#e0e0e0]'
                      : 'bg-[rgba(255,255,255,0.04)] border border-transparent text-[#888] hover:bg-[rgba(59,130,246,0.1)]'}
                  `}
                >
                  {w === 'all' ? '全程' : w}
                </button>
              ))}

              {/* 关闭按钮 */}
              <button
                type="button"
                className="rounded hover:bg-white/5 transition-colors p-1 ml-1"
                onClick={onClose}
                aria-label="关闭图表面板"
              >
                <X size={14} style={{ color: '#a0a0a0' }} />
              </button>
            </div>
          </div>

          {/* Metric Tabs */}
          <ChartMetricTabs activeMetric={activeMetric} onChange={setActiveMetric} />

          {/* Chart Body */}
          <div className="panel-body flex-1 min-h-0 px-3 pb-3 relative">
            {layoutMode === 'overlay' ? (
              <ChartCanvas ref={chartCanvasRef} metric={activeMetric} />
            ) : (
              <div className="flex flex-col gap-2 h-full overflow-y-auto">
                {entityArray.map((entityId) => (
                  <div key={entityId} className="flex-1 min-h-[120px] flex flex-col">
                    <span className="text-xs text-[#888] mb-1">{entityId}</span>
                    <div className="flex-1 min-h-0">
                      <ChartCanvas ref={chartCanvasRef} metric={activeMetric} />
                    </div>
                  </div>
                ))}
                {entityArray.length === 0 && (
                  <div className="flex items-center justify-center h-full text-sm" style={{ color: '#666' }}>
                    未追踪任何实体 — 在属性面板中开启图表追踪
                  </div>
                )}
              </div>
            )}
            {layoutMode === 'overlay' && entityArray.length === 0 && (
              <div className="absolute inset-0 flex items-center justify-center text-sm" style={{ color: '#666' }}>
                未追踪任何实体 — 在属性面板中开启图表追踪
              </div>
            )}
          </div>
        </div>
      </Resizable>
    </Draggable>
  );
}
```

创建 `frontend/src/components/__tests__/ChartPanel.test.tsx`，覆盖上述 6 个 behavior case。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/ChartPanel.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ChartPanel 可用；6 个交互测试通过；拖拽、resize、Tab 切换、时间窗口、布局模式、关闭全部就位</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChartPanel → 用户输入 | 拖拽/resize 是纯本地交互，无网络请求 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | rAF 循环泄漏 | mitigate | useEffect cleanup 中 cancelAnimationFrame(rafId) |
| T-04-02 | Denial of Service | 面板尺寸过大 | mitigate | maxWidth=1200, maxHeight=800 限制 |
</threat_model>

<verification>
### 自动化验证

```bash
cd frontend
# 类型检查
npx tsc --noEmit

# 单元测试
npx vitest run src/components/__tests__/ChartPanel.test.tsx --reporter=verbose
```
</verification>

<success_criteria>
1. ChartPanel.tsx 是浮动面板，position: fixed，z-index 40
2. react-draggable 实现拖拽（handle = panel-header），bounds=parent
3. re-resizable 实现大小调整，min 320x200，max 1200x800
4. 时间窗口按钮 5s/30s/全程切换时调用 setTimeWindow + chartCanvasRef.setTimeWindow
5. 布局模式切换按钮在 overlay 和 separate 间切换
6. 面板关闭后 onClose 被调用，不影响底层采样
7. ChartPanel.test.tsx 6 个 test case 全部 PASS
8. TypeScript 编译通过，无新增错误
</success_criteria>

<output>
After completion, create `.planning/phases/02-实时物理量图表/02-04-SUMMARY.md`
</output>
