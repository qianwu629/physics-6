---
phase: 02-实时物理量图表
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/components/ChartCanvas.tsx
  - frontend/src/components/ChartMetricTabs.tsx
  - frontend/src/components/__tests__/ChartCanvas.test.tsx
autonomous: true
requirements:
  - CHART-01
  - CHART-02

must_haves:
  truths:
    - "ChartCanvas.tsx 使用 useRef + imperative API 封装 lightweight-charts，不触发 React re-render"
    - "支持 overlay 模式（单 Chart 多 Series）和 separate 模式（每实体独立子 Chart）"
    - "曲线颜色按指标优先：位置蓝/速度绿/加速度橙/能量紫，深浅区分实体"
    - "update() 增量更新，不用 setData() 全量替换（Pitfall #3）"
    - "ResizeObserver 驱动 chart.applyOptions({width, height})"
    - "cleanup 中调用 chart.remove() + disconnect ResizeObserver（Pitfall #2）"
    - "ChartMetricTabs.tsx 提供 4 类指标 Tab 切换，样式与 EnvironmentPanel 预设按钮一致"
  artifacts:
    - path: "frontend/src/components/ChartCanvas.tsx"
      provides: "lightweight-charts imperative wrapper"
      exports: ["ChartCanvas"]
      min_lines: 200
    - path: "frontend/src/components/ChartMetricTabs.tsx"
      provides: "位置/速度/加速度/能量 Tab 切换"
      exports: ["ChartMetricTabs"]
      min_lines: 60
    - path: "frontend/src/components/__tests__/ChartCanvas.test.tsx"
      provides: "ChartCanvas 集成测试（mock Canvas）"
      contains: "describe('ChartCanvas'"
  key_links:
    - from: "ChartCanvas.tsx"
      to: "chartBuffer.ts getSeriesData()"
      via: "useImperativeHandle 暴露 updateSeries()，父组件从 buffer 读取后调用"
      pattern: "getSeriesData"
    - from: "ChartCanvas.tsx"
      to: "lightweight-charts createChart"
      via: "useEffect 初始化 chart 实例"
      pattern: "createChart"
    - from: "ChartMetricTabs.tsx"
      to: "chartDataStore.ts visibleMetrics"
      via: "onChange 回调更新 visibleMetrics"
      pattern: "visibleMetrics"
---

<objective>
构建图表渲染核心：ChartCanvas.tsx（lightweight-charts imperative wrapper）+ ChartMetricTabs.tsx（4 类指标 Tab 切换）。这是图表数据的"消费端"——将环形缓冲区的数据渲染为可视化曲线。

Purpose: 提供高性能 Canvas 实时折线图渲染，支持多实体多曲线叠加/分离双模式，遵循 D-02-01（lightweight-charts）、D-02-02（叠加+分离）、D-02-03（指标优先配色）。
Output: 2 个新组件 + 1 个测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-实时物理量图表/02-CONTEXT.md (D-02-01, D-02-02, D-02-03)
@.planning/phases/02-实时物理量图表/02-RESEARCH.md (Pattern 1, Pitfall #2, Pitfall #3, Code Examples: 多 Series 叠加 + 时间窗口切换)
@.planning/phases/02-实时物理量图表/PATTERNS.md (ChartCanvas 模式, ChartMetricTabs 模式)

<interfaces>
从 frontend/src/store/chartBuffer.ts (Plan 01 产出):
```typescript
export class ChartDataBuffer {
  getSeriesData(metricIndex: number, startTime: number, endTime: number): { time: number; value: number }[];
  getAllSeriesData(metricIndex: number): { time: number; value: number }[];
}
export const chartBuffers = new Map<string, ChartDataBuffer>();
```

从 frontend/src/store/chartDataStore.ts (Plan 01 产出):
```typescript
export type MetricType = 'position' | 'velocity' | 'acceleration' | 'energy';
export interface ChartConfigState {
  trackedEntityIds: Set<string>;
  timeWindow: '5s' | '30s' | 'all';
  layoutMode: 'overlay' | 'separate';
  visibleMetrics: Set<MetricType>;
}
```

从 RESEARCH.md Pattern 1:
```typescript
import { createChart, LineSeries, type IChartApi, type ISeriesApi } from 'lightweight-charts';
// useRef 持有 chart 实例
// useEffect 初始化 + cleanup 释放
// ResizeObserver 驱动尺寸更新
```

从 PATTERNS.md (EnvironmentPanel 预设按钮):
```typescript
<div className="flex gap-1.5">
  {PRESETS.map((p) => (
    <button key={p.label} className={`flex-1 px-1 py-1.5 rounded-lg text-xs transition-all
      ${active ? 'bg-[rgba(59,130,246,0.2)] border border-[#3b82f6] text-[#e0e0e0]' : 'bg-[rgba(255,255,255,0.04)] border border-transparent text-[#888]'}`}>
      {p.label}
    </button>
  ))}
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 ChartCanvas.tsx — lightweight-charts imperative wrapper</name>
  <files>frontend/src/components/ChartCanvas.tsx, frontend/src/components/__tests__/ChartCanvas.test.tsx</files>
  <behavior>
    - Test 1: 挂载后 createChart 被调用，chartRef.current 不为 null
    - Test 2: 添加追踪实体后，chart.addSeries 被调用创建 LineSeries
    - Test 3: updateSeries(entityId, metric, time, value) 调用 series.update()
    - Test 4: 卸载组件时 chart.remove() 被调用（无泄漏）
    - Test 5: setTimeWindow('5s') 调用 timeScale.setVisibleRange()
    - Test 6: layoutMode 切换时，overlay 模式用单 Chart，separate 模式创建多个子 Chart
  </behavior>
  <action>
创建 `frontend/src/components/ChartCanvas.tsx`：

```typescript
import { useEffect, useRef, useImperativeHandle, forwardRef } from 'react';
import {
  createChart,
  LineSeries,
  type IChartApi,
  type ISeriesApi,
  type LineData,
  type Time,
} from 'lightweight-charts';
import { useChartDataStore, type MetricType } from '../store/chartDataStore';
import { chartBuffers } from '../store/chartBuffer';

// ── 配色方案 — D-02-03 指标优先 ──
const METRIC_COLORS: Record<MetricType, string[]> = {
  position: ['#3b82f6', '#60a5fa', '#93c5fd'],   // 蓝系
  velocity: ['#22c55e', '#4ade80', '#86efac'],   // 绿系
  acceleration: ['#f97316', '#fb923c', '#fdba74'], // 橙系
  energy: ['#a855f7', '#c084fc', '#d8b4fe'],     // 紫系
};

const METRIC_NAMES: Record<MetricType, string[]> = {
  position: ['x', 'y', 'z'],
  velocity: ['vx', 'vy', 'vz'],
  acceleration: ['ax', 'ay', 'az'],
  energy: ['KE', 'PE', 'E'],
};

const METRIC_INDICES: Record<MetricType, number[]> = {
  position: [0, 1, 2],
  velocity: [3, 4, 5],
  acceleration: [6, 7, 8],
  energy: [9, 10, 11],
};

/** 生成 series key: entityId + metric + axis */
function makeSeriesKey(entityId: string, metric: MetricType, axisIndex: number): string {
  return `${entityId}::${metric}::${axisIndex}`;
}

export interface ChartCanvasHandle {
  /** 刷新所有 series 数据（从 buffer 读取最新数据并 update） */
  refreshAll: () => void;
  /** 切换时间窗口 */
  setTimeWindow: (window: '5s' | '30s' | 'all') => void;
}

interface ChartCanvasProps {
  metric: MetricType;
}

export const ChartCanvas = forwardRef<ChartCanvasHandle, ChartCanvasProps>(
  function ChartCanvas({ metric }, ref) {
    const containerRef = useRef<HTMLDivElement>(null);
    const chartRef = useRef<IChartApi | null>(null);
    const seriesMapRef = useRef<Map<string, ISeriesApi<'Line'>>>(new Map());

    const trackedEntityIds = useChartDataStore((s) => s.trackedEntityIds);
    const timeWindow = useChartDataStore((s) => s.timeWindow);

    // 初始化 chart
    useEffect(() => {
      const container = containerRef.current;
      if (!container) return;

      const chart = createChart(container, {
        width: container.clientWidth,
        height: container.clientHeight,
        layout: {
          background: { type: 'solid', color: '#1a1a2e' },
          textColor: '#d1d4dc',
        },
        timeScale: {
          timeVisible: true,
          secondsVisible: true,
          borderColor: '#2B2B43',
        },
        rightPriceScale: {
          borderColor: '#2B2B43',
        },
        crosshair: {
          mode: 1,
          vertLine: { width: 1, color: '#758696', style: 3 },
          horzLine: { width: 1, color: '#758696', style: 3 },
        },
        grid: {
          vertLines: { color: '#2B2B43', style: 1 },
          horzLines: { color: '#2B2B43', style: 1 },
        },
      });
      chartRef.current = chart;

      const ro = new ResizeObserver((entries) => {
        const { width, height } = entries[0].contentRect;
        chart.applyOptions({ width, height });
      });
      ro.observe(container);

      return () => {
        ro.disconnect();
        chart.remove();
        chartRef.current = null;
        seriesMapRef.current.clear();
      };
    }, []);

    // 根据 trackedEntityIds 动态增删 series
    useEffect(() => {
      const chart = chartRef.current;
      if (!chart) return;

      const currentKeys = new Set<string>();
      const colors = METRIC_COLORS[metric];
      const names = METRIC_NAMES[metric];
      const indices = METRIC_INDICES[metric];

      trackedEntityIds.forEach((entityId, entityIdx) => {
        indices.forEach((metricIndex, axisIdx) => {
          const key = makeSeriesKey(entityId, metric, axisIdx);
          currentKeys.add(key);

          if (!seriesMapRef.current.has(key)) {
            const color = colors[entityIdx % colors.length];
            const series = chart.addSeries(LineSeries, {
              color,
              lineWidth: 2,
              title: `${entityId} ${names[axisIdx]}`,
            });
            seriesMapRef.current.set(key, series);

            // 首次加载：用 setData 填充历史数据
            const buf = chartBuffers.get(entityId);
            if (buf) {
              const data = buf.getAllSeriesData(metricIndex);
              if (data.length > 0) {
                series.setData(data as LineData<Time>[]);
              }
            }
          }
        });
      });

      // 移除不再追踪的 series
      for (const [key, series] of seriesMapRef.current) {
        if (!currentKeys.has(key)) {
          chart.removeSeries(series);
          seriesMapRef.current.delete(key);
        }
      }
    }, [trackedEntityIds, metric]);

    // 时间窗口变化 → 调整 visible range
    useEffect(() => {
      const chart = chartRef.current;
      if (!chart) return;

      const now = performance.now() / 1000;
      if (timeWindow === '5s') {
        chart.timeScale().setVisibleRange({ from: now - 5, to: now });
      } else if (timeWindow === '30s') {
        chart.timeScale().setVisibleRange({ from: now - 30, to: now });
      } else {
        chart.timeScale().fitContent();
      }
    }, [timeWindow]);

    // 暴露 imperative API
    useImperativeHandle(ref, () => ({
      refreshAll: () => {
        const chart = chartRef.current;
        if (!chart) return;
        const now = performance.now() / 1000;
        const indices = METRIC_INDICES[metric];

        for (const entityId of trackedEntityIds) {
          const buf = chartBuffers.get(entityId);
          if (!buf) continue;

          indices.forEach((metricIndex, axisIdx) => {
            const key = makeSeriesKey(entityId, metric, axisIdx);
            const series = seriesMapRef.current.get(key);
            if (!series) return;

            // 根据时间窗口决定读取范围
            let data: { time: number; value: number }[];
            if (timeWindow === '5s') {
              data = buf.getSeriesData(metricIndex, now - 5, now);
            } else if (timeWindow === '30s') {
              data = buf.getSeriesData(metricIndex, now - 30, now);
            } else {
              data = buf.getAllSeriesData(metricIndex);
            }

            // 增量更新：只 update 最新的数据点
            if (data.length > 0) {
              const lastPoint = data[data.length - 1];
              series.update(lastPoint as LineData<Time>);
            }
          });
        }
      },
      setTimeWindow: (window) => {
        const chart = chartRef.current;
        if (!chart) return;
        const now = performance.now() / 1000;
        if (window === '5s') {
          chart.timeScale().setVisibleRange({ from: now - 5, to: now });
        } else if (window === '30s') {
          chart.timeScale().setVisibleRange({ from: now - 30, to: now });
        } else {
          chart.timeScale().fitContent();
        }
      },
    }));

    return (
      <div
        ref={containerRef}
        style={{ width: '100%', height: '100%', minHeight: 0 }}
      />
    );
  }
);
```

创建 `frontend/src/components/__tests__/ChartCanvas.test.tsx`，覆盖上述 6 个 behavior case。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/__tests__/ChartCanvas.test.tsx --reporter=verbose</automated>
  </verify>
  <done>ChartCanvas 可用；6 个集成测试通过；imperative API 暴露 refreshAll 和 setTimeWindow</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ChartMetricTabs.tsx — 4 类指标 Tab 切换</name>
  <files>frontend/src/components/ChartMetricTabs.tsx</files>
  <action>
创建 `frontend/src/components/ChartMetricTabs.tsx`：

```typescript
import { useChartDataStore, type MetricType } from '../store/chartDataStore';

const METRICS: { key: MetricType; label: string; color: string }[] = [
  { key: 'position', label: '位置', color: '#3b82f6' },
  { key: 'velocity', label: '速度', color: '#22c55e' },
  { key: 'acceleration', label: '加速度', color: '#f97316' },
  { key: 'energy', label: '能量', color: '#a855f7' },
];

interface ChartMetricTabsProps {
  activeMetric: MetricType;
  onChange: (metric: MetricType) => void;
}

export function ChartMetricTabs({ activeMetric, onChange }: ChartMetricTabsProps) {
  return (
    <div className="flex gap-1.5 px-3 py-2 shrink-0">
      {METRICS.map((m) => (
        <button
          key={m.key}
          type="button"
          onClick={() => onChange(m.key)}
          className={`flex-1 px-2 py-1 rounded-lg text-xs transition-all
            ${activeMetric === m.key
              ? 'bg-[rgba(59,130,246,0.2)] border border-[#3b82f6] text-[#e0e0e0]'
              : 'bg-[rgba(255,255,255,0.04)] border border-transparent text-[#888] hover:bg-[rgba(59,130,246,0.1)]'}
          `}
        >
          {m.label}
        </button>
      ))}
    </div>
  );
}
```
  </action>
  <verify>
    <automated>grep -n "ChartMetricTabs" frontend/src/components/ChartMetricTabs.tsx</automated>
  </verify>
  <done>ChartMetricTabs 可用；4 个 Tab 样式与 EnvironmentPanel 预设按钮一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ChartCanvas → lightweight-charts | Canvas 渲染在独立 2D 上下文，与 WebGL 分离 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | ChartCanvas series 泄漏 | mitigate | useEffect cleanup 中 chart.remove() + seriesMapRef.clear() |
| T-03-02 | Denial of Service | ResizeObserver 泄漏 | mitigate | cleanup 中 ro.disconnect() |
</threat_model>

<verification>
### 自动化验证

```bash
cd frontend
# 类型检查
npx tsc --noEmit

# 单元测试
npx vitest run src/components/__tests__/ChartCanvas.test.tsx --reporter=verbose
```
</verification>

<success_criteria>
1. ChartCanvas.tsx 使用 useRef + imperative API，不触发 React re-render
2. 支持 overlay 和 separate 两种布局模式
3. 曲线颜色按指标优先（位置蓝/速度绿/加速度橙/能量紫），深浅区分实体
4. update() 增量更新，不用 setData() 每帧全量替换
5. ResizeObserver 驱动 chart 尺寸自适应
6. cleanup 中 chart.remove() + ResizeObserver.disconnect()
7. ChartMetricTabs.tsx 提供 4 类指标 Tab 切换
8. ChartCanvas.test.tsx 6 个 test case 全部 PASS
9. TypeScript 编译通过，无新增错误
</success_criteria>

<output>
After completion, create `.planning/phases/02-实时物理量图表/02-03-SUMMARY.md`
</output>
