---
phase: 02-实时物理量图表
plan: 05
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified:
  - frontend/src/components/PropertyPanel.tsx
  - frontend/src/components/EnvironmentPanel.tsx
  - frontend/src/components/Toolbar.tsx
  - frontend/src/components/App.tsx
autonomous: false
requirements:
  - CHART-01
  - CHART-02
  - CHART-03

must_haves:
  truths:
    - "PropertyPanel 中为选中实体添加「图表追踪」Switch，默认关闭，切换时调用 toggleTracking"
    - "EnvironmentPanel 中添加「势能参考高度」滑块，范围 -50~50m，步进 0.1m"
    - "Toolbar 中添加「图表」toggle 按钮，控制 ChartPanel 显示/隐藏"
    - "App.tsx 中挂载 ChartPanel 组件，与 PropertyPanel 同级"
    - "App.tsx 中 ChartPanel 的 open 状态由 Toolbar 图表按钮控制"
    - "图表开关不影响采样——关闭面板后数据继续写入 buffer"
  artifacts:
    - path: "frontend/src/components/PropertyPanel.tsx"
      provides: "实体图表追踪 Switch"
      contains: "图表追踪"
      contains: "toggleTracking"
    - path: "frontend/src/components/EnvironmentPanel.tsx"
      provides: "势能参考高度 peReferenceY 输入"
      contains: "peReferenceY"
    - path: "frontend/src/components/Toolbar.tsx"
      provides: "图表面板 toggle 按钮"
      contains: "图表"
    - path: "frontend/src/components/App.tsx"
      provides: "ChartPanel 挂载 + 状态管理"
      contains: "ChartPanel"
  key_links:
    - from: "PropertyPanel 图表追踪 Switch"
      to: "chartDataStore.ts toggleTracking"
      via: "onCheckedChange={() => useChartDataStore.getState().toggleTracking(entityId)}"
      pattern: "toggleTracking"
    - from: "EnvironmentPanel peReferenceY 滑块"
      to: "chartDataStore.ts setPeReferenceY"
      via: "onChange={(v) => useChartDataStore.getState().setPeReferenceY(v)}"
      pattern: "setPeReferenceY"
    - from: "Toolbar 图表按钮"
      to: "App.tsx chartPanelOpen state"
      via: "onClick={() => setChartPanelOpen(!chartPanelOpen)}"
      pattern: "chartPanelOpen"
---

<objective>
将 Phase 2 所有新建组件集成到现有 UI 中：PropertyPanel 添加图表追踪开关、EnvironmentPanel 添加势能参考高度输入、Toolbar 添加图表面板 toggle、App.tsx 挂载 ChartPanel。这是"接线"阶段——让所有图表组件协同工作。

Purpose: 让用户可以通过现有 UI 入口控制图表功能（开关追踪、调整参考高度、显示/隐藏面板）。
Output: 4 个修改后的文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-实时物理量图表/02-CONTEXT.md (D-02-08, D-02-09)
@.planning/phases/02-实时物理量图表/PATTERNS.md (PropertyPanel Switch 模式, EnvironmentPanel HighlightSlider 模式, Toolbar 按钮模式)

<interfaces>
从 frontend/src/store/chartDataStore.ts (Plan 01 产出):
```typescript
export interface ChartConfigState {
  trackedEntityIds: Set<string>;
  peReferenceY: number;
  toggleTracking: (id: string) => void;
  setPeReferenceY: (y: number) => void;
}
export const useChartDataStore = create<ChartConfigState>()(...);
```

从 frontend/src/components/ChartPanel.tsx (Plan 04 产出):
```typescript
export function ChartPanel({ open, onClose }: { open: boolean; onClose: () => void }): JSX.Element;
```

从 frontend/src/components/PropertyPanel.tsx (当前):
```typescript
// 可视化开关区域（line 489-504）
<div className="pt-2 mt-2 border-t border-white/10 space-y-1.5">
  <div className="flex items-center justify-between">
    <label className="text-xs text-white/60">显示轨迹</label>
    <Switch checked={trailComp?.visible ?? true} onCheckedChange={(v) => toggleTrailVisibility(selectedEntity.id, v)} />
  </div>
  <div className="flex items-center justify-between">
    <label className="text-xs text-white/60">显示矢量</label>
    <Switch checked={vectorComp?.showVelocity ?? true} onCheckedChange={(v) => toggleVectorVisibility(selectedEntity.id, v)} />
  </div>
</div>
```

从 frontend/src/components/EnvironmentPanel.tsx (当前):
```typescript
// HighlightSlider 组件（line 19-67）
function HighlightSlider({ value, onChange, min, max, step, disabled, unit }) { ... }
// 在 Drag 区域之后添加 peReferenceY 输入
```

从 frontend/src/components/Toolbar.tsx (当前):
```typescript
// 可视化控制按钮区域（line 162-209）
<div className="flex items-center gap-1 pl-1 border-l border-white/10">
  <button onClick={toggleTrails}>轨迹</button>
  <button onClick={toggleVelocityVectors}>速度</button>
  <button onClick={toggleForceVectors}>受力</button>
  <button onClick={...}>{vectorDisplayMode === 'all' ? '全部' : '选中'}</button>
</div>
```

从 frontend/src/components/App.tsx (当前):
```typescript
// appState === 'ready' 渲染树
<>
  <MenuBar ... />
  <SceneBanner />
  <SnapshotManager ... />
  <PresetSelector ... />
  <Scene3D />
  <Toolbar />
  <Toolbox />
  {!propertyPanelCollapsed && <PropertyPanel />}
  ...
</>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PropertyPanel 添加图表追踪 Switch</name>
  <files>frontend/src/components/PropertyPanel.tsx</files>
  <read_first>frontend/src/components/PropertyPanel.tsx, frontend/src/store/chartDataStore.ts</read_first>
  <action>
修改 `frontend/src/components/PropertyPanel.tsx`：

1. 在文件顶部添加 import：
```typescript
import { useChartDataStore } from '../store/chartDataStore';
```

2. 在 PropertyPanel 函数体开头（靠近其他 store 访问器）添加：
```typescript
const trackedIds = useChartDataStore((s) => s.trackedEntityIds);
const toggleTracking = useChartDataStore((s) => s.toggleTracking);
```

3. 在两个「可视化开关」区域（弹簧属性和普通实体属性各一个）中，在「显示轨迹」和「显示矢量」之后添加第三个 Switch：
```tsx
<div className="flex items-center justify-between">
  <label className="text-xs text-white/60">图表追踪</label>
  <Switch
    checked={trackedIds.has(selectedEntity.id)}
    onCheckedChange={(v) => toggleTracking(selectedEntity.id)}
  />
</div>
```

注意：PropertyPanel 中有两个可视化开关区域（一个用于弹簧属性，一个用于普通实体属性），都需要添加图表追踪 Switch。
  </action>
  <verify>
    <automated>grep -n "图表追踪\|toggleTracking" frontend/src/components/PropertyPanel.tsx | grep -v "^#" | wc -l</automated>
  </verify>
  <done>PropertyPanel 中为弹簧和普通实体都添加了图表追踪 Switch；toggleTracking 绑定正确</done>
</task>

<task type="auto">
  <name>Task 2: EnvironmentPanel 添加势能参考高度输入</name>
  <files>frontend/src/components/EnvironmentPanel.tsx</files>
  <read_first>frontend/src/components/EnvironmentPanel.tsx, frontend/src/store/chartDataStore.ts</read_first>
  <action>
修改 `frontend/src/components/EnvironmentPanel.tsx`：

1. 在文件顶部添加 import：
```typescript
import { useChartDataStore } from '../store/chartDataStore';
```

2. 在 EnvironmentPanel 函数体开头添加：
```typescript
const peReferenceY = useChartDataStore((s) => s.peReferenceY);
const setPeReferenceY = useChartDataStore((s) => s.setPeReferenceY);
```

3. 在 Drag 区域之后（文件末尾 `</div>` 之前）添加势能参考高度输入：
```tsx
<div className="h-px bg-[rgba(255,255,255,0.06)] my-3" />
<div className="mb-2">
  <div className="text-xs font-medium text-[#a0a0a0] mb-2">势能参考高度 (y=0)</div>
  <HighlightSlider
    value={peReferenceY}
    onChange={setPeReferenceY}
    min={-50}
    max={50}
    step={0.1}
    disabled={false}
    unit="m"
  />
</div>
```
  </action>
  <verify>
    <automated>grep -n "peReferenceY\|势能参考高度" frontend/src/components/EnvironmentPanel.tsx | grep -v "^#" | wc -l</automated>
  </verify>
  <done>EnvironmentPanel 中添加了势能参考高度滑块，范围 -50~50m，步进 0.1m</done>
</task>

<task type="auto">
  <name>Task 3: Toolbar 添加图表面板 toggle 按钮</name>
  <files>frontend/src/components/Toolbar.tsx</files>
  <read_first>frontend/src/components/Toolbar.tsx</read_first>
  <action>
修改 `frontend/src/components/Toolbar.tsx`：

1. 在 Toolbar 组件 Props 或函数签名中不需要修改（按钮状态由 App.tsx 通过 prop 传入）。

2. 修改 Toolbar 的函数签名，接收 chartPanelOpen 和 toggleChartPanel：
```typescript
interface ToolbarProps {
  chartPanelOpen?: boolean;
  onToggleChartPanel?: () => void;
}

export default function Toolbar({ chartPanelOpen, onToggleChartPanel }: ToolbarProps = {}) {
```

3. 在可视化控制按钮区域之后（`</div>` 之后，下一个 `<div style={{ width: 1...` 之前）添加图表按钮：
```tsx
<div style={{ width: 1, height: 20, backgroundColor: 'rgba(255, 255, 255, 0.1)' }} />
<div className="flex items-center gap-1 pl-1 border-l border-white/10">
  <button
    type="button"
    onClick={onToggleChartPanel}
    className={cn(
      'px-2 py-1 text-xs rounded',
      chartPanelOpen ? 'bg-white/15 text-white' : 'text-white/50 hover:text-white/80'
    )}
    title={chartPanelOpen ? '隐藏图表' : '显示图表'}
  >
    图表
  </button>
</div>
```
  </action>
  <verify>
    <automated>grep -n "图表\|chartPanelOpen\|onToggleChartPanel" frontend/src/components/Toolbar.tsx | grep -v "^#" | wc -l</automated>
  </verify>
  <done>Toolbar 中添加了图表 toggle 按钮，接收 chartPanelOpen 和 onToggleChartPanel props</done>
</task>

<task type="auto">
  <name>Task 4: App.tsx 挂载 ChartPanel + 状态管理</name>
  <files>frontend/src/components/App.tsx</files>
  <read_first>frontend/src/components/App.tsx, frontend/src/components/ChartPanel.tsx, frontend/src/components/Toolbar.tsx</read_first>
  <action>
修改 `frontend/src/components/App.tsx`：

1. 在文件顶部添加 import：
```typescript
import { ChartPanel } from './ChartPanel';
```

2. 在 App 函数体中添加状态：
```typescript
const [chartPanelOpen, setChartPanelOpen] = useState(false);
```

3. 修改 Toolbar 调用，传入图表状态：
```tsx
<Toolbar
  chartPanelOpen={chartPanelOpen}
  onToggleChartPanel={() => setChartPanelOpen((v) => !v)}
/>
```

4. 在 App.tsx 的渲染树中添加 ChartPanel（与 PropertyPanel 同级，在 `</>` 之前）：
```tsx
{/* Phase 2: 实时物理量图表面板 */}
<ChartPanel
  open={chartPanelOpen}
  onClose={() => setChartPanelOpen(false)}
/>
```

注意：ChartPanel 使用 fixed 定位，不需要关心在渲染树中的相对顺序（z-index 控制层级）。放在 PropertyPanel 之后即可。
  </action>
  <verify>
    <automated>grep -n "ChartPanel\|chartPanelOpen" frontend/src/components/App.tsx | grep -v "^#" | wc -l</automated>
  </verify>
  <done>App.tsx 中挂载了 ChartPanel；Toolbar 传入图表状态；chartPanelOpen 状态管理正确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI 开关 → chartDataStore | 纯本地状态切换，无外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | — | UI integration | accept | 纯组件组合和状态绑定，无安全影响 |
</threat_model>

<verification>
### 自动化验证

```bash
cd frontend
# 类型检查
npx tsc --noEmit

# 检查关键集成点
grep -n "图表追踪\|toggleTracking" frontend/src/components/PropertyPanel.tsx
grep -n "势能参考高度\|peReferenceY" frontend/src/components/EnvironmentPanel.tsx
grep -n "图表\|chartPanelOpen" frontend/src/components/Toolbar.tsx
grep -n "ChartPanel\|chartPanelOpen" frontend/src/components/App.tsx
```

### 手动验证（checkpoint）

1. 启动 dev server：`npm run dev`
2. 创建一个实体，选中后在 PropertyPanel 中确认「图表追踪」Switch 存在
3. 开启图表追踪，点击 Toolbar「图表」按钮，确认 ChartPanel 浮动面板出现
4. 在 EnvironmentPanel 中确认「势能参考高度」滑块存在，调整数值
5. 运行仿真，确认 ChartPanel 中显示实时曲线
6. 关闭 ChartPanel，确认曲线隐藏但数据继续采样（重新打开后能看到历史数据）
</verification>

<success_criteria>
1. PropertyPanel 中为选中实体添加「图表追踪」Switch，默认关闭
2. EnvironmentPanel 中添加「势能参考高度」滑块，范围 -50~50m，步进 0.1m
3. Toolbar 中添加「图表」toggle 按钮，控制 ChartPanel 显示/隐藏
4. App.tsx 中挂载 ChartPanel，状态由 Toolbar 控制
5. 图表面板关闭后不影响采样（重新打开能看到历史数据）
6. TypeScript 编译通过，无新增错误
</success_criteria>

<output>
After completion, create `.planning/phases/02-实时物理量图表/02-05-SUMMARY.md`
</output>
