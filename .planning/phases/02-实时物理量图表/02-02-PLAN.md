---
phase: 02-实时物理量图表
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - frontend/src/utils/physicsCalc.ts
  - frontend/src/ecs/ChartSampler.ts
  - frontend/src/components/Scene3D.tsx
  - frontend/src/utils/__tests__/physicsCalc.test.ts
  - frontend/src/ecs/__tests__/ChartSampler.test.ts
autonomous: true
requirements:
  - CHART-01
  - CHART-03

must_haves:
  truths:
    - "physicsCalc.ts 导出 computeEnergy() 和 computeSmoothedAcceleration()，公式与 RESEARCH.md 一致"
    - "ChartSampler.ts 在 useFrame 中 60Hz 采样被追踪实体，写入 chartBuffer"
    - "暂停时 sample() 直接 return（冻结）"
    - "重置时（resetCounter 变化）清空所有 chartBuffers"
    - "加速度使用 SMA(5) 平滑，静止物体加速度噪声 < 0.05 m/s²"
    - "能量计算包含 KE + PE_gravity + PE_spring，弹簧振子场景总能量漂移 < 5%"
  artifacts:
    - path: "frontend/src/utils/physicsCalc.ts"
      provides: "能量计算 + 加速度差分 + SMA 平滑"
      exports: ["computeEnergy", "AccelerationSmoother"]
      min_lines: 80
    - path: "frontend/src/ecs/ChartSampler.ts"
      provides: "60Hz useFrame 采样循环"
      exports: ["ChartSampler"]
      min_lines: 60
    - path: "frontend/src/utils/__tests__/physicsCalc.test.ts"
      provides: "能量守恒 + 加速度平滑单元测试"
      contains: "describe('computeEnergy'"
    - path: "frontend/src/ecs/__tests__/ChartSampler.test.ts"
      provides: "采样逻辑单元测试"
      contains: "describe('ChartSampler'"
  key_links:
    - from: "ChartSampler.ts useFrame"
      to: "chartBuffer.ts push()"
      via: "getOrCreateBuffer(entityId).push(time, metrics)"
      pattern: "getOrCreateBuffer.*push"
    - from: "ChartSampler.ts"
      to: "physicsCalc.ts computeEnergy"
      via: "每帧调用 computeEnergy(rigidBody, mass, gravityY, peReferenceY, springs, getPos)"
      pattern: "computeEnergy"
    - from: "ChartSampler.ts"
      to: "physicsCalc.ts AccelerationSmoother"
      via: "每实体一个 smoother 实例，push(vel) -> getSmoothedAcceleration(dt)"
      pattern: "AccelerationSmoother"
---

<objective>
构建物理量采样核心：physicsCalc.ts（能量公式 + 加速度 SMA 平滑）+ ChartSampler.ts（60Hz useFrame 采样循环）。这是图表数据的"生产端"——所有后续渲染都依赖这里产出的数据。

Purpose: 从 Rapier 物理引擎实时提取物理量，计算能量和加速度，写入环形缓冲区。遵循 D-02-04（共享 R3F useFrame）和 D-02-07（暂停冻结/重置清空）。
Output: 2 个新文件 + 2 个修改文件 + 2 个测试文件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-实时物理量图表/02-CONTEXT.md (D-02-04, D-02-07)
@.planning/phases/02-实时物理量图表/02-RESEARCH.md (Pitfall #4, Pitfall #6, Code Examples: 能量计算 + SMA 平滑)
@.planning/phases/02-实时物理量图表/PATTERNS.md (ChartSampler 模式 — TrajectoryRenderer 类比)
@.planning/phases/02-实时物理量图表/VALIDATION.md (V-CHART-01 能量守恒, V-CHART-02 加速度噪声, V-CHART-04 暂停冻结, V-CHART-05 重置清空)

<interfaces>
从 frontend/src/ecs/TrajectoryRenderer.tsx:
```typescript
useFrame(() => {
  if (!showTrails) return;
  const now = performance.now() / 1000;
  for (const [entityId, entity] of entities) {
    const rb = getRef(entityId);
    if (!rb || !rb.current) continue;
    const vel = rb.current.linvel();
    const speed = Math.sqrt(vel.x ** 2 + vel.y ** 2 + vel.z ** 2);
    if (speed < VELOCITY_THRESHOLD) continue;
    const prevTime = lastSampleTime.current.get(entityId);
    if (prevTime !== undefined && now - prevTime < SAMPLE_INTERVAL) continue;
    lastSampleTime.current.set(entityId, now);
    const pos = rb.current.translation();
    buf.push(new THREE.Vector3(pos.x, pos.y, pos.z), now);
  }
});
```

从 frontend/src/store/chartBuffer.ts (Plan 01 产出):
```typescript
export class ChartDataBuffer {
  push(time: number, metrics: Float64Array /* length 12 */): void;
}
export const chartBuffers = new Map<string, ChartDataBuffer>();
export function getOrCreateBuffer(entityId: string): ChartDataBuffer;
export function clearAllBuffers(): void;
```

从 frontend/src/store/chartDataStore.ts (Plan 01 产出):
```typescript
export const useChartDataStore = create<ChartConfigState>()((set) => ({
  trackedEntityIds: new Set(),
  peReferenceY: 0,
  // ...
}));
```

从 frontend/src/store/simulationSlice.ts:
```typescript
export interface SimulationSlice {
  isRunning: boolean;
  resetCounter: number;
  environment: EnvironmentState; // gravity, peReferenceY
}
```

从 frontend/src/components/RigidBodyRefContext.tsx:
```typescript
export function useRigidBodyRefRegistry() {
  return useContext(RigidBodyRefContext); // { getRef: (entityId: string) => any | undefined }
}
```

从 frontend/src/ecs/types.ts:
```typescript
export interface ConstraintComponent extends Component {
  type: 'constraint';
  kind: 'spring';
  entityAId: string;
  entityBId: string;
  params: { stiffness: number; restLength: number; damping: number };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 physicsCalc.ts — 能量计算 + 加速度 SMA 平滑</name>
  <files>frontend/src/utils/physicsCalc.ts, frontend/src/utils/__tests__/physicsCalc.test.ts</files>
  <behavior>
    - Test 1: computeEnergy — 质量 2kg、速度 (3,4,0) 的物体，KE = 0.5 * 2 * 25 = 25 J
    - Test 2: computeEnergy — 质量 1kg、位置 y=5、peReferenceY=0、gravityY=-9.81，PE_gravity = 1 * 9.81 * 5 = 49.05 J
    - Test 3: computeEnergy — 弹簧 stiffness=100、restLength=2、当前长度 3，PE_spring = 0.5 * 100 * 1² = 50 J
    - Test 4: computeEnergy — 总能量 = KE + PE_gravity + PE_spring（上述组合）
    - Test 5: AccelerationSmoother — 静止物体（vel 恒为 0）SMA(5) 后加速度接近 0，绝对值 < 0.05
    - Test 6: AccelerationSmoother — 匀加速物体（vel 每帧 +1），SMA 后加速度接近 1/dt
    - Test 7: 能量守恒 — 弹簧振子场景（无重力），KE + PE_spring 在 30 秒内相对漂移 < 5%
  </behavior>
  <action>
创建 `frontend/src/utils/physicsCalc.ts`：

```typescript
import type { RigidBody } from '@dimforge/rapier3d-compat'; // 或从 rapier 类型推断
import * as THREE from 'three';

export interface SpringInfo {
  stiffness: number;
  restLength: number;
  entityAId: string;
  entityBId: string;
}

/**
 * 计算实体能量
 * KE = 0.5 * m * |v|²
 * PE_gravity = m * |g| * (y - peReferenceY)
 * PE_spring = Σ 0.5 * k * (|Δx| - L0)²
 */
export function computeEnergy(
  rigidBody: { linvel(): { x: number; y: number; z: number }; translation(): { x: number; y: number; z: number }; mass(): number },
  mass: number,
  gravityY: number,
  peReferenceY: number,
  springs: SpringInfo[],
  getEntityPosition: (id: string) => THREE.Vector3 | null,
): { ke: number; peGravity: number; peSprings: number; total: number } {
  const vel = rigidBody.linvel();
  const pos = rigidBody.translation();

  const v2 = vel.x * vel.x + vel.y * vel.y + vel.z * vel.z;
  const ke = 0.5 * mass * v2;

  const peGravity = mass * Math.abs(gravityY) * (pos.y - peReferenceY);

  let peSprings = 0;
  for (const spring of springs) {
    const pA = getEntityPosition(spring.entityAId);
    const pB = getEntityPosition(spring.entityBId);
    if (!pA || !pB) continue;
    const dx = pB.x - pA.x;
    const dy = pB.y - pA.y;
    const dz = pB.z - pA.z;
    const currentLength = Math.sqrt(dx * dx + dy * dy + dz * dz);
    const delta = currentLength - spring.restLength;
    peSprings += 0.5 * spring.stiffness * delta * delta;
  }

  return { ke, peGravity, peSprings, total: ke + peGravity + peSprings };
}

/**
 * 加速度 SMA(5) 平滑器
 * 存储最近 5 帧速度，计算平均加速度
 */
export class AccelerationSmoother {
  private velHistory: Float64Array;
  private idx = 0;
  private filled = false;

  constructor(private windowSize: number = 5) {
    this.velHistory = new Float64Array(windowSize * 3);
  }

  push(vx: number, vy: number, vz: number): void {
    const i = this.idx * 3;
    this.velHistory[i] = vx;
    this.velHistory[i + 1] = vy;
    this.velHistory[i + 2] = vz;
    this.idx = (this.idx + 1) % this.windowSize;
    if (this.idx === 0) this.filled = true;
  }

  getSmoothedAcceleration(dt: number): [number, number, number] {
    const n = this.filled ? this.windowSize : this.idx;
    if (n < 2) return [0, 0, 0];

    let sumAx = 0, sumAy = 0, sumAz = 0;
    const count = n - 1;
    for (let i = 0; i < count; i++) {
      const currIdx = ((this.idx - 1 - i + this.windowSize) % this.windowSize) * 3;
      const prevIdx = ((this.idx - 2 - i + this.windowSize) % this.windowSize) * 3;
      sumAx += (this.velHistory[currIdx] - this.velHistory[prevIdx]) / dt;
      sumAy += (this.velHistory[currIdx + 1] - this.velHistory[prevIdx + 1]) / dt;
      sumAz += (this.velHistory[currIdx + 2] - this.velHistory[prevIdx + 2]) / dt;
    }
    return [sumAx / count, sumAy / count, sumAz / count];
  }

  reset(): void {
    this.idx = 0;
    this.filled = false;
  }
}
```

创建 `frontend/src/utils/__tests__/physicsCalc.test.ts`，覆盖上述 7 个 behavior case。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/utils/__tests__/physicsCalc.test.ts --reporter=verbose</automated>
  </verify>
  <done>physicsCalc.ts 可用；7 个单元测试通过；能量公式和 SMA 平滑验证正确</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 ChartSampler.ts — 60Hz useFrame 采样循环</name>
  <files>frontend/src/ecs/ChartSampler.ts, frontend/src/ecs/__tests__/ChartSampler.test.ts, frontend/src/components/Scene3D.tsx</files>
  <behavior>
    - Test 1: isRunning=false 时 sample() 不写入缓冲（V-CHART-04）
    - Test 2: 被追踪实体采样后缓冲区 count > 0；未被追踪实体不写入
    - Test 3: resetCounter 变化后所有缓冲区被清空（V-CHART-05）
    - Test 4: 采样数据包含 12 个指标，metricIndex 0-2 为位置，3-5 为速度，6-8 为加速度，9-11 为能量
    - Test 5: 多实体同时追踪时各自独立缓冲
  </behavior>
  <action>
创建 `frontend/src/ecs/ChartSampler.ts`：

```typescript
import { useRef, useEffect } from 'react';
import { useFrame } from '@react-three/fiber';
import * as THREE from 'three';
import { useSimulationStore } from '../store';
import { useChartDataStore } from '../store/chartDataStore';
import { getOrCreateBuffer, clearAllBuffers, METRICS_PER_ENTITY } from '../store/chartBuffer';
import { useRigidBodyRefRegistry } from '../components/RigidBodyRefContext';
import { computeEnergy, AccelerationSmoother } from '../utils/physicsCalc';
import type { ConstraintComponent } from './types';

const SAMPLE_INTERVAL = 1 / 60; // 60Hz

export function ChartSampler() {
  const isRunning = useSimulationStore((s) => s.isRunning);
  const resetCounter = useSimulationStore((s) => s.resetCounter);
  const entities = useSimulationStore((s) => s.entities);
  const gravity = useSimulationStore((s) => s.environment.gravity);
  const { getRef } = useRigidBodyRefRegistry();

  // 每实体一个加速度平滑器
  const smootherMap = useRef<Map<string, AccelerationSmoother>>(new Map());
  const lastSampleTime = useRef<number>(0);
  const prevResetCounter = useRef(resetCounter);

  // 监听 resetCounter 变化 → 清空缓冲（D-02-07）
  useEffect(() => {
    if (resetCounter !== prevResetCounter.current) {
      clearAllBuffers();
      smootherMap.current.clear();
      prevResetCounter.current = resetCounter;
    }
  }, [resetCounter]);

  useFrame(() => {
    // D-02-07: 暂停时冻结
    if (!isRunning) return;

    const now = performance.now() / 1000;
    if (now - lastSampleTime.current < SAMPLE_INTERVAL) return;
    lastSampleTime.current = now;

    const trackedIds = useChartDataStore.getState().trackedEntityIds;
    const peReferenceY = useChartDataStore.getState().peReferenceY;

    if (trackedIds.size === 0) return;

    // 收集所有弹簧约束信息（用于能量计算）
    const springs: Array<{
      stiffness: number;
      restLength: number;
      entityAId: string;
      entityBId: string;
    }> = [];
    for (const [, entity] of entities) {
      const constraint = entity.components.get('constraint') as ConstraintComponent | undefined;
      if (constraint && constraint.kind === 'spring') {
        springs.push({
          stiffness: constraint.params.stiffness,
          restLength: constraint.params.restLength,
          entityAId: constraint.entityAId,
          entityBId: constraint.entityBId,
        });
      }
    }

    // 获取实体位置的辅助函数
    const getEntityPosition = (id: string): THREE.Vector3 | null => {
      const rbRef = getRef(id);
      if (!rbRef?.current) return null;
      const t = rbRef.current.translation();
      return new THREE.Vector3(t.x, t.y, t.z);
    };

    for (const entityId of trackedIds) {
      const rbRef = getRef(entityId);
      if (!rbRef?.current) continue;

      const rb = rbRef.current;
      const vel = rb.linvel();
      const pos = rb.translation();
      const mass = rb.mass();

      // 加速度：差分 + SMA 平滑
      let smoother = smootherMap.current.get(entityId);
      if (!smoother) {
        smoother = new AccelerationSmoother(5);
        smootherMap.current.set(entityId, smoother);
      }
      smoother.push(vel.x, vel.y, vel.z);
      const [ax, ay, az] = smoother.getSmoothedAcceleration(SAMPLE_INTERVAL);

      // 能量计算
      const { ke, peGravity, peSprings, total } = computeEnergy(
        rb,
        mass,
        gravity[1],
        peReferenceY,
        springs,
        getEntityPosition,
      );

      // 组装 12 指标
      const metrics = new Float64Array(METRICS_PER_ENTITY);
      metrics[0] = pos.x;
      metrics[1] = pos.y;
      metrics[2] = pos.z;
      metrics[3] = vel.x;
      metrics[4] = vel.y;
      metrics[5] = vel.z;
      metrics[6] = ax;
      metrics[7] = ay;
      metrics[8] = az;
      metrics[9] = ke;
      metrics[10] = peGravity + peSprings; // 总势能
      metrics[11] = total;

      const buf = getOrCreateBuffer(entityId);
      buf.push(now, metrics);
    }
  });

  return null; // 纯逻辑组件
}
```

修改 `frontend/src/components/Scene3D.tsx`：
在 `VectorRenderer` 之后添加 `ChartSampler`：
```tsx
{/* Phase 2: 图表采样 — ChartSampler */}
<ChartSampler />
```
并在文件顶部添加 import：
```typescript
import { ChartSampler } from '../ecs/ChartSampler';
```

创建 `frontend/src/ecs/__tests__/ChartSampler.test.ts`，覆盖上述 5 个 behavior case。
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/ecs/__tests__/ChartSampler.test.ts --reporter=verbose</automated>
  </verify>
  <done>ChartSampler 可用；5 个单元测试通过；Scene3D.tsx 中已挂载；暂停冻结和重置清空行为正确</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rapier rigidBody → ChartSampler | 物理引擎数据读取，只读不修改 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | ChartSampler 遍历 springs | accept | 弹簧数量受 MAX_ENTITIES 限制（≤50），遍历开销可忽略 |
| T-02-04 | Information Disclosure | computeEnergy 读取实体位置 | accept | 只读取当前用户场景中的实体数据，无外部输入 |
</threat_model>

<verification>
### 自动化验证

```bash
cd frontend
# 类型检查
npx tsc --noEmit

# 单元测试
npx vitest run src/utils/__tests__/physicsCalc.test.ts --reporter=verbose
npx vitest run src/ecs/__tests__/ChartSampler.test.ts --reporter=verbose
```
</verification>

<success_criteria>
1. physicsCalc.ts 导出 computeEnergy() 和 AccelerationSmoother，公式与 RESEARCH.md 一致
2. physicsCalc.test.ts 7 个 test case 全部 PASS（含能量守恒、加速度噪声 < 0.05）
3. ChartSampler.ts 在 useFrame 中 60Hz 采样，写入 chartBuffer
4. ChartSampler.test.ts 5 个 test case 全部 PASS（含暂停冻结、重置清空）
5. Scene3D.tsx 中已挂载 `<ChartSampler />`
6. TypeScript 编译通过，无新增错误
</success_criteria>

<output>
After completion, create `.planning/phases/02-实时物理量图表/02-02-SUMMARY.md`
</output>
