---
phase: 01-simulation-core-3d-render
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/src/store/index.ts
  - frontend/src/store/simulationSlice.ts
  - frontend/src/simulation/types.ts
  - frontend/src/simulation/hardcodedScene.ts
  - frontend/src/components/Scene3D.tsx
autonomous: true
requirements: [SIM-01, SIM-03, REN-01, REN-02]
user_setup: []

must_haves:
  truths:
    - "场景中出现 10+ 个不同形状的物体，每个物体有柔和的随机颜色"
    - "地面平面可见，占据场景视野底部"
    - "物体在重力作用下自然下落，互相碰撞并在地面堆叠"
    - "摄像机以 45° 对角线俯瞰场景中心，用户可自由旋转/平移/缩放"
    - "参考网格和 RGB 三色坐标轴始终可见"
    - "场景中有环境光和平行光，物体有阴影投射"
    - "Canvas 随窗口大小自适应缩放"
  artifacts:
    - path: "frontend/src/store/simulationSlice.ts"
      provides: "仿真控制状态: isRunning, showDebug, fps, objectCount，及 play/pause/toggle/reset actions"
      exports: ["createSimulationSlice", "SimulationSlice"]
    - path: "frontend/src/store/index.ts"
      provides: "Zustand store 入口，组合所有 slices"
      exports: ["useSimulationStore"]
    - path: "frontend/src/simulation/types.ts"
      provides: "物理场景类型定义: SceneObject, RigidBodyKind, ColliderShape"
      exports: ["SceneObject", "RigidBodyKind", "ColliderShape"]
    - path: "frontend/src/simulation/hardcodedScene.ts"
      provides: "Phase 1 硬编码初始场景: 10+ 物体含位置/形状/尺寸/颜色"
      exports: ["INITIAL_SCENE_OBJECTS"]
    - path: "frontend/src/components/Scene3D.tsx"
      provides: "R3F Canvas + Rapier Physics + 场景物体渲染 + 摄像机 + 辅助元素"
      exports: ["Scene3D"] (default)
  key_links:
    - from: "frontend/src/components/Scene3D.tsx"
      to: "frontend/src/store/index.ts"
      via: "useSimulationStore hook"
      pattern: "import.*useSimulationStore"
    - from: "frontend/src/components/Scene3D.tsx [Physics]"
      to: "@react-three/rapier"
      via: "timeStep=1/120 prop"
      pattern: "timeStep=\\{1\\s*/\\s*120\\}"
    - from: "frontend/src/components/Scene3D.tsx [ground]"
      to: "@react-three/rapier RigidBody type=fixed"
      via: "CuboidCollider args=[50, 0.5, 50]"
      pattern: "type=\"fixed\""
    - from: "frontend/src/simulation/hardcodedScene.ts"
      to: "frontend/src/components/Scene3D.tsx"
      via: "INITIAL_SCENE_OBJECTS import, mapped to RigidBody + Collider + mesh"
      pattern: "import.*INITIAL_SCENE_OBJECTS"
---

<objective>
构建物理仿真核心和 3D 渲染层：创建 Zustand 仿真控制 store、定义硬编码初始场景（10+ 物体、随机柔和颜色）、搭建 R3F Canvas 场景（含 Physics、地面、动态物体、摄像机 45° 对角线、网格/坐标轴辅助、光照/阴影）。

Purpose: 这是 Phase 1 的核心交付——可运行的 3D 物理沙盒（不含 UI 控制面板，UI 由 Plan 03 添加）。
Output: 3D 场景渲染正确，物体在重力下碰撞堆叠，用户可旋转观察。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/01-UI-SPEC.md
@.planning/phases/01-simulation-core-3d-render/01-CONTEXT.md

<interfaces>
<!-- Types and contracts that the executor needs -->
From @react-three/rapier (Plan uses these R3F components):
```typescript
// Physics world setup
<Physics timeStep={1/120} paused={!isRunning} debug={showDebug} gravity={[0, -9.81, 0]}>
  {/* RigidBody: dynamic (falling), fixed (ground/slopes), kinematic */}
  <RigidBody type="dynamic" position={[x,y,z]} colliders={false}>
    <CuboidCollider args={[hw, hh, hd]} />
    <BallCollider args={[radius]} />
    <CylinderCollider args={[halfHeight, radius]} />
  </RigidBody>
</Physics>
```

From drei:
```typescript
import { OrbitControls, Grid, GizmoHelper, GizmoViewport } from '@react-three/drei';
```

From three:
```typescript
import { Canvas } from '@react-three/fiber';
```

From frontend/src/store/simulationSlice.ts (to be created in Task 1):
```typescript
export interface SimulationSlice {
  isRunning: boolean;        // D-04: 初始 false（启动暂停）
  showDebug: boolean;        // D-07: 物理调试线框
  fps: number;               // 实时帧率
  objectCount: number;       // 场景物体数量
  play: () => void;
  pause: () => void;
  toggle: () => void;
  reset: () => void;
  setShowDebug: (v: boolean) => void;
  setFps: (fps: number) => void;
}
export const useSimulationStore = create<SimulationSlice>(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Zustand simulation control store</name>
  <files>frontend/src/store/simulationSlice.ts, frontend/src/store/index.ts</files>
  <read_first>
    frontend/src/store/api.ts (现有 store 骨架——需要理解 Zustand 使用模式和类型风格)
    .planning/01-UI-SPEC.md (D-04: 初始暂停状态; D-07: debug 默认关闭)
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-04, D-07 决策详情)
  </read_first>
  <action>
创建两个文件：

---

**1. `frontend/src/store/simulationSlice.ts`** — 仿真控制状态切片

```typescript
import type { StateCreator } from 'zustand';

/**
 * 仿真控制状态切片
 *
 * Phase 1: 管理播放/暂停/重置/调试开关。
 * 注意：物理帧数据（物体位姿）不经过 Zustand（避免重渲染风暴——PITFALLS #6）。
 * 只有仿真控制元数据（运行状态、开关）通过 store 驱动 React UI。
 */

export interface SimulationSlice {
  /** 仿真是否正在运行 — D-04: 初始 false（启动后暂停等待用户操作） */
  isRunning: boolean;
  /** 物理调试线框/碰撞体可视化 — D-07: 默认关闭 */
  showDebug: boolean;
  /** 当前实时帧率 */
  fps: number;
  /** 场景中动态物体数量 */
  objectCount: number;

  // Actions
  /** 开始仿真 */
  play: () => void;
  /** 暂停仿真 */
  pause: () => void;
  /** 播放/暂停切换 */
  toggle: () => void;
  /** 重置仿真 — 恢复初始场景 */
  reset: () => void;
  /** 切换物理调试线框显示 */
  setShowDebug: (value: boolean) => void;
  /** 更新 FPS 计数器 */
  setFps: (fps: number) => void;
  /** 设置物体数量 */
  setObjectCount: (count: number) => void;
}

export type SimulationStore = SimulationSlice;

export const createSimulationSlice: StateCreator<SimulationSlice, [], [], SimulationSlice> = (set) => ({
  // D-04: 初始状态为暂停——场景渲染但物理不运行，给用户时间观察初始布局
  isRunning: false,
  // D-07: 调试线框默认关闭
  showDebug: false,
  fps: 0,
  objectCount: 0,

  play: () => set({ isRunning: true }),
  pause: () => set({ isRunning: false }),
  toggle: () => set((state) => ({ isRunning: !state.isRunning })),
  reset: () => set({ isRunning: false }),
  setShowDebug: (value) => set({ showDebug: value }),
  setFps: (fps) => set({ fps }),
  setObjectCount: (count) => set({ objectCount: count }),
});
```

---

**2. `frontend/src/store/index.ts`** — Zustand store 入口

```typescript
import { create } from 'zustand';
import { createSimulationSlice, type SimulationSlice } from './simulationSlice';

/**
 * Physis 全局 Store
 *
 * Phase 1: 仅包含 simulationSlice。
 * Phase 2+ 将添加 sceneSlice、uiSlice 等（STACK.md 切片模式）。
 */

export const useSimulationStore = create<SimulationSlice>()((...args) => ({
  ...createSimulationSlice(...args),
}));
```

---

**设计原则（遵循 PITFALLS #6）：**
- 此 store 仅存储仿真控制元数据——不存储每帧物理变换数据
- 物理变换由 @react-three/rapier 内部直接同步到 Three.js Object3D，绕过 React reconciliation
- 只有 Toolbar 等低频 UI 组件订阅此 store（播放/暂停状态、FPS、物体计数）

创建完成后，执行 `cd frontend && npx tsc --noEmit --project tsconfig.app.json` 确认零类型错误。
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error"</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "isRunning: false" frontend/src/store/simulationSlice.ts 返回 >= 1（D-04: 初始暂停）</criterion>
  <criterion>grep -c "showDebug: false" frontend/src/store/simulationSlice.ts 返回 >= 1（D-07: debug 默认关闭）</criterion>
  <criterion>grep -c "toggle:" frontend/src/store/simulationSlice.ts 返回 >= 1</criterion>
  <criterion>grep -c "createSimulationSlice" frontend/src/store/simulationSlice.ts 返回 >= 1</criterion>
  <criterion>grep -c "createSimulationSlice" frontend/src/store/index.ts 返回 >= 1</criterion>
  <criterion>grep -c "export const useSimulationStore" frontend/src/store/index.ts 返回 1</criterion>
  <criterion>grep -c "from './simulationSlice'" frontend/src/store/index.ts 返回 1</criterion>
  <criterion>TypeScript 编译零错误（npx tsc --noEmit 无输出）</criterion>
</acceptance_criteria>
<done>Zustand store 创建完成：simulationSlice 实现所有仿真控制状态和 actions，index.ts 组合导出 useSimulationStore。TypeScript 编译零错误。</done>
</task>

<task type="auto">
  <name>Task 2: Define hardcoded initial scene (10+ objects, D-01/D-02/D-03)</name>
  <files>frontend/src/simulation/types.ts, frontend/src/simulation/hardcodedScene.ts</files>
  <read_first>
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-01: ~10+ objects, D-02: ground implicit, D-03: random soft colors)
  </read_first>
  <action>
创建两个文件——类型定义和硬编码场景数据。

---

**1. `frontend/src/simulation/types.ts`** — 物理场景类型定义

```typescript
/**
 * 物理场景类型定义
 *
 * Phase 1: 硬编码场景的数据结构。
 * 注意：这些类型在 Phase 2 将被组件化 ECS 系统取代（ARCHITECTURE.md Pattern 1）。
 * 当前设计保持简洁以服务引擎验证目的（D-12: 严禁模板模式）。
 */

/** 刚体类型 */
export type RigidBodyKind = 'dynamic' | 'fixed';

/** 碰撞体形状 */
export type ColliderShape = 'sphere' | 'cuboid' | 'cylinder';

/** 单个场景物体的初始定义 */
export interface SceneObject {
  /** 唯一标识 */
  id: string;
  /** 显示名称（调试用） */
  name: string;
  /** 刚体类型: dynamic=受重力影响, fixed=静态（地面/斜面） */
  kind: RigidBodyKind;
  /** 碰撞体形状 */
  shape: ColliderShape;
  /** 形状参数 — 球体: [radius], 方块: [halfWidth, halfHeight, halfDepth], 圆柱: [halfHeight, radius] */
  shapeArgs: [number, number, number];
  /** 初始位置 [x, y, z] */
  position: [number, number, number];
  /** 初始旋转 [x, y, z]（欧拉角，弧度） — 例如斜面需要绕 Z 轴旋转 */
  rotation: [number, number, number];
  /** 物体颜色 (hex 字符串，如 '#7eb8da') — D-03: 随机柔和色彩 */
  color: string;
  /** 弹性系数 (restitution), 0=完全非弹性, 1=完全弹性 */
  restitution: number;
}
```

---

**2. `frontend/src/simulation/hardcodedScene.ts`** — Phase 1 硬编码初始场景

D-01 要求 ~10+ 个随机物体作为压力测试。包含以下组合：
- 3 个球体（不同大小，不同高度）
- 3 个方块（不同尺寸，堆叠和独立）
- 2 个圆柱体（会滚动）
- 1 个静态斜面（方块旋转）
- 1 个静态平台（方块，水平）
- 1 个额外球体（高处释放，增大碰撞多样性）

**总计 14 个物体（含：2 静态斜面/平台 + 12 动态物体）。超出 D-01 "~10+" 要求以验证引擎稳定性。**

```typescript
import type { SceneObject } from './types';

/**
 * Phase 1 硬编码初始场景
 *
 * D-01: ~10+ 个随机物体，作为压力测试场景
 * D-02: 地面是隐式基础设施（由 Scene3D 组件直接创建，不在此列表中）
 * D-03: 每个物体随机分配柔和色彩（从预定义调色板选取）
 * D-12: 此硬编码场景仅用于 Phase 1 引擎验证，Phase 2 将替换为 UI 自由添加
 *
 * 柔和色彩调色板（设计为视觉差异明显但不刺眼）:
 *   珊瑚红 #e08a7d, 天空蓝 #7eb8da, 薄荷绿 #8fbc8f,
 *   暖橙 #f0a878, 薰衣草 #b8a0d0, 向日葵黄 #e8d878,
 *   玫瑰粉 #e8a0b8, 浅蓝灰 #90b8c8, 杏色 #e8c8a0,
 *   鼠尾草绿 #a0c8a8, 柔紫 #c0a8d8, 奶白 #e8e0d0
 */

export const INITIAL_SCENE_OBJECTS: SceneObject[] = [
  // ──── 球体 (3个，不同大小/高度) ────
  {
    id: 'ball-01',
    name: '大球',
    kind: 'dynamic',
    shape: 'sphere',
    shapeArgs: [1.0, 0, 0],           // radius=1.0
    position: [-3.5, 6.0, 0],
    rotation: [0, 0, 0],
    color: '#7eb8da',                  // 天空蓝
    restitution: 0.7,
  },
  {
    id: 'ball-02',
    name: '中球',
    kind: 'dynamic',
    shape: 'sphere',
    shapeArgs: [0.7, 0, 0],           // radius=0.7
    position: [0.5, 8.0, 1.5],
    rotation: [0, 0, 0],
    color: '#e8a0b8',                  // 玫瑰粉
    restitution: 0.6,
  },
  {
    id: 'ball-03',
    name: '小球',
    kind: 'dynamic',
    shape: 'sphere',
    shapeArgs: [0.4, 0, 0],           // radius=0.4
    position: [3.0, 10.0, -1.0],
    rotation: [0, 0, 0],
    color: '#f0a878',                  // 暖橙
    restitution: 0.85,
  },

  // ──── 方块 (4个，不同尺寸/位置，部分用于堆叠) ────
  {
    id: 'box-01',
    name: '大方块',
    kind: 'dynamic',
    shape: 'cuboid',
    shapeArgs: [1.5, 1.5, 1.5],       // 3x3x3
    position: [-5.0, 3.0, 0],
    rotation: [0, 0, 0],
    color: '#e08a7d',                  // 珊瑚红
    restitution: 0.3,
  },
  {
    id: 'box-02',
    name: '中方块',
    kind: 'dynamic',
    shape: 'cuboid',
    shapeArgs: [1.0, 0.8, 1.0],       // 2x1.6x2
    position: [-5.5, 6.5, 0],          // 叠在大方块上方
    rotation: [0, 0.3, 0],             // 轻微旋转，增加不稳定性
    color: '#b8a0d0',                  // 薰衣草
    restitution: 0.4,
  },
  {
    id: 'box-03',
    name: '扁方块',
    kind: 'dynamic',
    shape: 'cuboid',
    shapeArgs: [1.8, 0.4, 1.2],       // 扁长方体
    position: [4.0, 2.5, 0],
    rotation: [0, 0, 0.15],
    color: '#a0c8a8',                  // 鼠尾草绿
    restitution: 0.2,
  },
  {
    id: 'box-04',
    name: '高方块',
    kind: 'dynamic',
    shape: 'cuboid',
    shapeArgs: [0.6, 2.0, 0.6],       // 高柱形
    position: [-2.0, 1.5, 2.5],
    rotation: [0, 0, 0],
    color: '#e8d878',                  // 向日葵黄
    restitution: 0.35,
  },

  // ──── 圆柱体 (2个，会滚动的视觉元素) ────
  {
    id: 'cylinder-01',
    name: '大圆柱',
    kind: 'dynamic',
    shape: 'cylinder',
    shapeArgs: [1.2, 0.6, 0],         // halfHeight=1.2, radius=0.6
    position: [2.0, 5.0, 2.0],
    rotation: [0, 0, 0],
    color: '#90b8c8',                  // 浅蓝灰
    restitution: 0.5,
  },
  {
    id: 'cylinder-02',
    name: '小圆柱',
    kind: 'dynamic',
    shape: 'cylinder',
    shapeArgs: [0.8, 0.35, 0],        // halfHeight=0.8, radius=0.35
    position: [-3.0, 4.5, -2.0],
    rotation: [Math.PI / 2, 0, 0],     // 横放——会滚动！
    color: '#e8c8a0',                  // 杏色
    restitution: 0.55,
  },

  // ──── 静态斜面 (D-02: ground is infrastructure, static slopes are objects) ────
  {
    id: 'slope-01',
    name: '长斜面',
    kind: 'fixed',                     // 静态——不移动
    shape: 'cuboid',
    shapeArgs: [4.0, 0.3, 2.0],       // 薄长块
    position: [6.0, 0.5, 2.0],
    rotation: [0, 0, Math.PI / 6],     // 绕 Z 轴 30°
    color: '#c0a8d8',                  // 柔紫
    restitution: 0.5,
  },
  {
    id: 'slope-02',
    name: '短斜面',
    kind: 'fixed',                     // 静态
    shape: 'cuboid',
    shapeArgs: [2.5, 0.3, 1.5],
    position: [-6.0, 1.0, -1.5],
    rotation: [0, 0, -Math.PI / 8],    // 反方向
    color: '#8fbc8f',                  // 薄荷绿
    restitution: 0.5,
  },

  // ──── 静态平台 (额外水平平台，丰富场景层次) ────
  {
    id: 'platform-01',
    name: '小平板',
    kind: 'fixed',
    shape: 'cuboid',
    shapeArgs: [2.0, 0.3, 2.0],
    position: [0, 2.0, -3.0],
    rotation: [0, 0, 0],
    color: '#e8e0d0',                  // 奶白
    restitution: 0.4,
  },

  // ──── 额外动态物体 (增加碰撞多样性) ────
  {
    id: 'ball-04',
    name: '高速球',
    kind: 'dynamic',
    shape: 'sphere',
    shapeArgs: [0.55, 0, 0],
    position: [7.0, 9.0, -2.0],        // 从斜面上方高处释放
    rotation: [0, 0, 0],
    color: '#e0a070',                  // 暖杏色
    restitution: 0.9,                   // 高弹性——会有更多弹跳
  },
  {
    id: 'box-05',
    name: '超大块',
    kind: 'dynamic',
    shape: 'cuboid',
    shapeArgs: [2.0, 1.0, 2.0],       // 4x2x4 大方块
    position: [-1.0, 12.0, -2.5],      // 高处释放——大量动能
    rotation: [0, 0.2, 0.1],
    color: '#d0a0c0',                  // 浅粉紫
    restitution: 0.15,                  // 低弹性——落地冲击明显
  },
];

// 统计信息（运行时验证用）
export const SCENE_STATS = {
  totalObjects: INITIAL_SCENE_OBJECTS.length,
  dynamicCount: INITIAL_SCENE_OBJECTS.filter(o => o.kind === 'dynamic').length,
  fixedCount: INITIAL_SCENE_OBJECTS.filter(o => o.kind === 'fixed').length,
};
// 预期: totalObjects=14, dynamicCount=11, fixedCount=3
```

创建完成后，执行 TypeScript 编译检查。
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | grep -c "error"</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "export interface SceneObject" frontend/src/simulation/types.ts 返回 1</criterion>
  <criterion>grep -c "export type RigidBodyKind" frontend/src/simulation/types.ts 返回 1</criterion>
  <criterion>grep -c "export type ColliderShape" frontend/src/simulation/types.ts 返回 1</criterion>
  <criterion>grep -c "export const INITIAL_SCENE_OBJECTS" frontend/src/simulation/hardcodedScene.ts 返回 1</criterion>
  <criterion>grep -c "kind: 'dynamic'" frontend/src/simulation/hardcodedScene.ts 返回 >= 10（D-01: ~10+ dynamic objects）</criterion>
  <criterion>grep -c "kind: 'fixed'" frontend/src/simulation/hardcodedScene.ts 返回 >= 3（斜面和平台）</criterion>
  <criterion>grep -c "#[0-9a-fA-F]\{6\}" frontend/src/simulation/hardcodedScene.ts 返回 >= 14（D-03: 每个物体有颜色）</criterion>
  <criterion>grep -v '^//' frontend/src/simulation/hardcodedScene.ts | grep -c "color:" 返回 >= 14（非注释行的颜色分配）</criterion>
  <criterion>grep -c "SCENE_STATS" frontend/src/simulation/hardcodedScene.ts 返回 >= 1</criterion>
  <criterion>TypeScript 编译零错误</criterion>
</acceptance_criteria>
<done>类型定义和硬编码场景数据创建完成。14 个物体（11 动态 + 3 静态），每个有随机柔和色彩。符合 D-01 (~10+ 物体)、D-02 (地面独立于物体列表)、D-03 (随机柔和色彩) 决策。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build R3F Scene3D component with Physics, camera, helpers, objects</name>
  <files>frontend/src/components/Scene3D.tsx</files>
  <read_first>
    .planning/research/ARCHITECTURE.md (Pattern 2: 固定时间步长, Pattern 3: 物理-渲染分离)
    .planning/research/PITFALLS.md (Pitfall 1: 可变时间步长, Pitfall 8: 坐标系不匹配, Pitfall 10: "先做功能后加 3D"陷阱)
    .planning/01-UI-SPEC.md (颜色值, 视口背景 #0a0a0a)
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-05: 摄像机 45°, D-06: Grid + Axes + 灯光/阴影, D-04: 启动暂停, D-10: Canvas 自适应)
    frontend/src/store/simulationSlice.ts (Task 1 创建 — 需要 store 接口)
    frontend/src/simulation/hardcodedScene.ts (Task 2 创建 — 需要场景数据)
  </read_first>
  <behavior>
    测试规范（Scene3D 为 R3F 组件，验证以运行时行为检查为主）:
    - Canvas 尺寸: 浏览器窗口全尺寸（resize 后 Canvas 尺寸同步变化）
    - 初始暂停: Physics paused={true}（isRunning 初始为 false），物体静止不动
    - 固定时间步长: Physics timeStep={1/120}
    - 摄像机: 初始化位置 [12, 10, 12]，lookAt [0, 2, 0]（45° 对角线）
    - 地面: 位置 [0, -0.5, 0]，尺寸 100x1x100，static
    - 网格: 20x20 单位，每 1 单位一条线
    - 坐标轴: RGB 三色，长度 5 单位
    - 光照: ambient intensity=0.4 + directional intensity=0.8 castShadow
    - 物体数量: 14 个 RigidBody（从 INITIAL_SCENE_OBJECTS 映射）
    - 颜色: 每个物体使用其 color 属性
  </behavior>
  <action>
创建 `frontend/src/components/Scene3D.tsx` —— Phase 1 的 3D 场景核心组件。

**架构说明:**
- 物理引擎（Rapier WASM）通过 @react-three/rapier 的 `<Physics>` 组件在主线程运行（STACK.md Phase 1 策略）
- 固定 120Hz 时间步长（ARCHITECTURE.md 推荐），通过 `<Physics timeStep={1/120}>` 实现
- 物理为权威状态，Three.js 网格仅作为视觉镜像（PITFALLS #3 防护：@react-three/rapier 内部确保物理步进→矩阵同步→渲染的顺序）
- D-04: 场景渲染但物理暂停（`paused={!isRunning}`，isRunning 初始 false）
- D-10: Canvas 自适应窗口（R3F 默认行为——通过 `style` 或自动 resize）

**完整实现代码:**

```tsx
import { Canvas } from '@react-three/fiber';
import { Physics, RigidBody, CuboidCollider, BallCollider, CylinderCollider } from '@react-three/rapier';
import { OrbitControls, Grid, GizmoHelper, GizmoViewport } from '@react-three/drei';
import { useRef, useEffect, useMemo } from 'react';
import * as THREE from 'three';
import { INITIAL_SCENE_OBJECTS, SCENE_STATS } from '../simulation/hardcodedScene';
import { useSimulationStore } from '../store';
import type { SceneObject } from '../simulation/types';

// ──── 被 @react-three/rapier 驱动的物理网格组件 ────

/**
 * 单个物理物体的 3D 渲染
 *
 * 使用 @react-three/rapier 的声明式组件:
 * - <RigidBody> 注册到 Rapier 世界并接收物理变换
 * - <Collider> 定义碰撞几何
 * - <mesh> 是纯视觉——由 @react-three/rapier 自动同步变换
 */
function PhysicsObject({ obj }: { obj: SceneObject }) {
  const colliderProps = useMemo(() => {
    switch (obj.shape) {
      case 'sphere':
        return { type: 'ball' as const, args: [obj.shapeArgs[0]] as [number] };
      case 'cuboid':
        return { type: 'cuboid' as const, args: obj.shapeArgs as [number, number, number] };
      case 'cylinder':
        return { type: 'cylinder' as const, args: obj.shapeArgs as [number, number] };
    }
  }, [obj.shape, obj.shapeArgs]);

  const geometry = useMemo(() => {
    switch (obj.shape) {
      case 'sphere':
        return <sphereGeometry args={[obj.shapeArgs[0], 32, 32]} />;
      case 'cuboid':
        return <boxGeometry args={[
          obj.shapeArgs[0] * 2,
          obj.shapeArgs[1] * 2,
          obj.shapeArgs[2] * 2,
        ]} />;
      case 'cylinder':
        return <cylinderGeometry args={[
          obj.shapeArgs[1],  // radiusTop
          obj.shapeArgs[1],  // radiusBottom
          obj.shapeArgs[0] * 2,  // height
          32,
        ]} />;
    }
  }, [obj.shape, obj.shapeArgs]);

  return (
    <RigidBody
      type={obj.kind}
      position={obj.position}
      rotation={obj.rotation}
      restitution={obj.restitution}
      colliders={false}
    >
      {/* 碰撞体 —— 决定物理行为 */}
      {colliderProps.type === 'ball' && <BallCollider args={colliderProps.args as [number]} />}
      {colliderProps.type === 'cuboid' && <CuboidCollider args={colliderProps.args as [number, number, number]} />}
      {colliderProps.type === 'cylinder' && <CylinderCollider args={colliderProps.args as [number, number]} />}

      {/* 视觉网格 —— 纯渲染，不参与物理 */}
      <mesh castShadow receiveShadow>
        {geometry}
        <meshStandardMaterial
          color={obj.color}
          roughness={0.6}
          metalness={0.1}
        />
      </mesh>
    </RigidBody>
  );
}

// ──── 地面 ────
// D-02: 地面是隐式基础设施——不属于"物体"，始终存在

function Ground() {
  return (
    <RigidBody type="fixed" position={[0, -0.5, 0]} restitution={0.5}>
      <CuboidCollider args={[50, 0.5, 50]} />
      <mesh receiveShadow>
        <boxGeometry args={[100, 1, 100]} />
        <meshStandardMaterial
          color="#1a1a1a"        // secondary 色（UI-SPEC）
          roughness={0.9}
          metalness={0.0}
        />
      </mesh>
    </RigidBody>
  );
}

// ──── FPS 追踪器 (useFrame-based, 不触发 React re-render) ────

function FpsTracker() {
  const frameCountRef = useRef(0);
  const lastTimeRef = useRef(performance.now());
  const setFps = useSimulationStore((s) => s.setFps);

  useEffect(() => {
    let rafId: number;
    const tick = () => {
      frameCountRef.current++;
      const now = performance.now();
      const elapsed = now - lastTimeRef.current;
      if (elapsed >= 500) {           // 每 500ms 更新一次 FPS（而非每帧——减少 store 写入）
        const fps = Math.round(frameCountRef.current / (elapsed / 1000));
        setFps(fps);
        frameCountRef.current = 0;
        lastTimeRef.current = now;
      }
      rafId = requestAnimationFrame(tick);
    };
    rafId = requestAnimationFrame(tick);
    return () => cancelAnimationFrame(rafId);
  }, [setFps]);

  return null; // 无 DOM 输出——纯逻辑组件
}

// ──── 场景初始化 (设置物体数量) ────

function SceneInitializer() {
  const setObjectCount = useSimulationStore((s) => s.setObjectCount);

  useEffect(() => {
    // 报告物体数量 — D-04 暂停时显示初始场景统计
    setObjectCount(SCENE_STATS.totalObjects);
  }, [setObjectCount]);

  return null;
}

// ──── 主场景组件 ────

/**
 * Scene3D — Phase 1 3D 仿真画布
 *
 * 包含:
 * - R3F Canvas（WebGL 渲染器）
 * - Rapier Physics 世界（固定 120Hz 时间步长）
 * - 硬编码初始场景物体（D-01）
 * - 地面（D-02）
 * - 轨道摄像机 45° 对角线（D-05）
 * - 参考网格 + RGB 坐标轴 (D-06)
 * - 环境光 + 平行光 + 阴影 (D-06)
 * - Canvas 自适应窗口 (D-10)
 */
export default function Scene3D() {
  const isRunning = useSimulationStore((s) => s.isRunning);
  const showDebug = useSimulationStore((s) => s.showDebug);

  const initialCameraPosition: [number, number, number] = [12, 10, 12];   // D-05: 45° 对角线
  const initialCameraTarget: [number, number, number] = [0, 2, 0];       // 场景中心偏上

  return (
    <Canvas
      shadows
      camera={{
        position: initialCameraPosition,
        fov: 45,
        near: 0.1,
        far: 200,
      }}
      gl={{
        antialias: true,
        toneMapping: THREE.ACESFilmicToneMapping,
        toneMappingExposure: 1.0,
      }}
      style={{
        position: 'fixed',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',                    // D-10: 全窗口自适应
        background: '#0a0a0a',             // dominant 色（UI-SPEC）
      }}
    >
      {/* ── 调试与初始化 ── */}
      <FpsTracker />
      <SceneInitializer />

      {/* ── 物理世界 (Rapier WASM) ── */}
      <Physics
        timeStep={1 / 120}                  // ARCHITECTURE.md: 固定 120Hz
        paused={!isRunning}                 // D-04: 初始暂停（isRunning=false）
        debug={showDebug}                   // D-07: 调试线框
        gravity={[0, -9.81, 0]}            // 标准重力
        interpolate={true}                  // 渲染插值——平滑视觉
      >
        {/* 地面 — D-02: 隐式基础设施 */}
        <Ground />

        {/* 场景物体 — D-01, D-03 */}
        {INITIAL_SCENE_OBJECTS.map((obj) => (
          <PhysicsObject key={obj.id} obj={obj} />
        ))}
      </Physics>

      {/* ── 摄像机控制 (D-05) ── */}
      <OrbitControls
        target={initialCameraTarget}
        enableDamping={true}
        dampingFactor={0.1}
        minDistance={2}
        maxDistance={80}
        maxPolarAngle={Math.PI * 0.85}    // 防止钻到地下
        screenSpacePanning={true}
      />

      {/* ── 辅助视觉 (D-06) ── */}
      {/* 参考网格 — 物理课堂风格 */}
      <Grid
        position={[0, 0.01, 0]}            // 略高于地面避免 z-fighting
        args={[30, 30]}                    // 30x30 单位
        cellSize={1}                       // 1 单位 = 1 米
        cellThickness={0.5}
        cellColor="#333333"
        sectionSize={5}                    // 每 5 单位加粗线
        sectionThickness={1.0}
        sectionColor="#555555"
        fadeDistance={60}
        fadeStrength={1.5}
        infiniteGrid={false}
      />

      {/* RGB 三色坐标轴 — 使用 drei 的 GizmoHelper */}
      <GizmoHelper alignment="bottom-right" margin={[80, 80]}>
        <GizmoViewport
          axisColors={['#ff3653', '#8df038', '#3299ff']}  // R=X轴, G=Y轴, B=Z轴
          labelColor="#aaaaaa"
        />
      </GizmoHelper>

      {/* ── 光照与阴影 (D-06) ── */}
      {/* 环境光 — 提供基础照明，防止阴影区域全黑 */}
      <ambientLight intensity={0.4} />

      {/* 主平行光 — 产生方向性阴影 */}
      <directionalLight
        position={[15, 25, 10]}
        intensity={0.8}
        castShadow
        shadow-mapSize-width={2048}
        shadow-mapSize-height={2048}
        shadow-camera-far={80}
        shadow-camera-left={-30}
        shadow-camera-right={30}
        shadow-camera-top={30}
        shadow-camera-bottom={-30}
        shadow-bias={-0.0005}
      />

      {/* 补光 — 从另一方向减弱暗面 */}
      <directionalLight
        position={[-10, 8, -5]}
        intensity={0.2}
      />
    </Canvas>
  );
}
```

**设计决策和注意事项:**
- `useSimulationStore` 的选择器使用精确到字段的 selector（如 `s => s.isRunning`），避免 Zustand 重渲染风暴（PITFALLS #6）
- FPS 跟踪器使用 `requestAnimationFrame` + ref（不触发 React reconciliation），每 500ms 写入一次 store（降频到 ~2Hz，UI 更新足够）
- `interpolate={true}` — @react-three/rapier 内置插值，处理物理步长与渲染帧之间的时间差
- 阴影贴图 2048x2048 — 桌面端合理平衡质量和性能
- `GizmoHelper` 替代手写坐标轴——drei 提供稳定、美观的 RGB 轴指示器

创建完成后，在 `frontend` 目录执行 TypeScript 编译检查：
```bash
cd frontend && npx tsc --noEmit --project tsconfig.app.json
```
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "export default function Scene3D" frontend/src/components/Scene3D.tsx 返回 1</criterion>
  <criterion>grep -c "timeStep={1 / 120}" frontend/src/components/Scene3D.tsx 返回 1（固定 120Hz 时间步长）</criterion>
  <criterion>grep -c "paused={!isRunning}" frontend/src/components/Scene3D.tsx 返回 1（D-04: 初始暂停）</criterion>
  <criterion>grep -c "debug={showDebug}" frontend/src/components/Scene3D.tsx 返回 1（D-07: debug 控制）</criterion>
  <criterion>grep -c "\[12, 10, 12\]" frontend/src/components/Scene3D.tsx 返回 1（D-05: 45° 对角线摄像机）</criterion>
  <criterion>grep -c "type=\"fixed\"" frontend/src/components/Scene3D.tsx 返回 >= 3（地面 + 2条斜面物体路径）— grep 只检查组件存在</criterion>
  <criterion>grep -c "useSimulationStore" frontend/src/components/Scene3D.tsx 返回 >= 2（订阅 isRunning 和 showDebug）</criterion>
  <criterion>grep -c "INITIAL_SCENE_OBJECTS" frontend/src/components/Scene3D.tsx 返回 >= 1（导入并映射场景物体）</criterion>
  <criterion>grep -c 'from.*drei' frontend/src/components/Scene3D.tsx 返回 1（导入 OrbitControls/Grid/GizmoHelper）</criterion>
  <criterion>grep -c 'ambientLight' frontend/src/components/Scene3D.tsx 返回 1（D-06: 环境光）</criterion>
  <criterion>grep -c 'directionalLight' frontend/src/components/Scene3D.tsx 返回 >= 2（D-06: 主光 + 补光）</criterion>
  <criterion>grep -c 'GizmoHelper' frontend/src/components/Scene3D.tsx 返回 1（D-06: RGB 坐标轴）</criterion>
  <criterion>grep -c "#0a0a0a" frontend/src/components/Scene3D.tsx 返回 1（D-06 背景色）</criterion>
  <criterion>grep -c "gravity=\{\[0, -9.81, 0\]\}" frontend/src/components/Scene3D.tsx 返回 1（标准重力）</criterion>
  <criterion>TypeScript 编译零错误</criterion>
</acceptance_criteria>
<done>Scene3D 组件创建完成。Canvas 全窗口渲染，Physics 固定 120Hz 时间步长，14 个物体从硬编码场景渲染，地面 + 参考网格 + RGB 坐标轴 + 光照阴影全部就位。摄像机 45° 对角线，初始暂停状态。符合 D-01 至 D-06、D-10 决策。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM 加载 | @dimforge/rapier2d-compat 的 WASM 模块在页面加载时初始化——失败会导致场景无法渲染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Denial of Service | Scene3D → WASM 初始化 | mitigate | Plan 03 的 LoadingScreen + ErrorFallback 处理加载失败（D-11 要求）——Scene3D 本身由父组件条件渲染 |
| T-01-03 | Denial of Service | Scene3D → WebGL 上下文 | mitigate | Plan 03 的 App 组件检测 WebGL 可用性，不可用时渲染 ErrorFallback（UI-SPEC 错误文案） |
| T-01-04 | Elevation of Privilege | N/A (无用户输入) | accept | Phase 1 无用户输入，所有场景数据硬编码，无注入风险 |
</threat_model>

<verification>
启动开发服务器并验证 3D 场景:
```bash
cd frontend && npx vite
```

手动验证清单（浏览器）:
1. 页面打开后看到 14 个不同颜色物体 + 地面 + 网格 + 坐标轴——所有正确渲染
2. 物体静止不动（暂停状态）
3. 鼠标左键拖拽可旋转视角，滚轮缩放，右键平移
4. 坐标轴指示器在右下角可见
5. 物体有柔和阴影投射到地面
</verification>

<success_criteria>
- [ ] Scene3D 组件编译零错误
- [ ] Zustand store 正确导出 useSimulationStore
- [ ] 硬编码场景包含 14 个物体（11 动态 + 3 静态），每个有颜色
- [ ] Canvas 自适应浏览器窗口全尺寸
- [ ] Physics 组件使用 timeStep=1/120（固定 120Hz）
- [ ] 摄像机初始位置 [12, 10, 12] 以 45° 对角线观测场景
- [ ] 网格叠加在 y≈0 平面，RGB 坐标轴可见
- [ ] 场景初始暂停——物体不下落
</success_criteria>

<output>
After completion, create `.planning/phases/01-simulation-core-3d-render/01-02-SUMMARY.md`
</output>
