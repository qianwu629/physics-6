---
phase: 01-simulation-core-3d-render
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - frontend/src/components/Toolbar.tsx
  - frontend/src/components/LoadingScreen.tsx
  - frontend/src/components/ErrorFallback.tsx
  - frontend/src/components/App.tsx
autonomous: true
requirements: [SIM-06]
user_setup: []

must_haves:
  truths:
    - "用户点击 ▶ 播放 按钮后物体开始下落碰撞——按钮变为 ⏸ 暂停 + 蓝色底色"
    - "用户点击 ⏸ 暂停 按钮后物体停止——按钮变为 ▶ 继续"
    - "用户点击 ↺ 重置 按钮后物体恢复初始位置和状态"
    - "按下 Space 键切换播放/暂停，按下 R 键重置"
    - "WASM 加载期间中央显示旋转圆环和「正在加载物理引擎...」文字"
    - "WebGL 不可用时显示错误卡片含正确文案"
    - "WASM 加载失败时显示错误卡片含正确文案"
    - "工具栏浮动在视口上方，半透明背景，显示 FPS 和物体数量"
    - "物理调试开关可切换碰撞体线框的显示/隐藏"
  artifacts:
    - path: "frontend/src/components/Toolbar.tsx"
      provides: "顶部浮动工具栏: 播放/暂停/重置按钮 + 调试开关 + FPS + 物体计数"
    - path: "frontend/src/components/LoadingScreen.tsx"
      provides: "WASM 加载指示器: 旋转圆环 + 中文加载文字"
    - path: "frontend/src/components/ErrorFallback.tsx"
      provides: "错误状态展示: WebGL 不可用 / WASM 加载失败"
    - path: "frontend/src/components/App.tsx"
      provides: "应用根组件: 协调加载→场景→错误状态, 键盘快捷键, WASM 初始化"
  key_links:
    - from: "frontend/src/components/App.tsx"
      to: "frontend/src/components/Scene3D.tsx"
      via: "条件渲染: wasmReady ? <Scene3D /> : <LoadingScreen />"
      pattern: "import Scene3D"
    - from: "frontend/src/components/App.tsx"
      to: "frontend/src/components/Toolbar.tsx"
      via: "浮动叠加在 Scene3D 上方"
      pattern: "import Toolbar"
    - from: "frontend/src/components/Toolbar.tsx"
      to: "frontend/src/store/index.ts"
      via: "useSimulationStore: isRunning, toggle, reset, showDebug, fps, objectCount"
      pattern: "import.*useSimulationStore"
    - from: "frontend/src/components/App.tsx"
      to: "frontend/src/store/index.ts"
      via: "键盘事件 → store.toggle() / store.reset()"
      pattern: "useSimulationStore.*toggle"
---

<objective>
创建所有 UI 组件并组装应用：浮动工具栏（播放/暂停/重置 + 调试开关 + 状态指示器）、WASM 加载屏幕、错误回退卡片、应用根组件（协调加载/场景/错误状态、键盘快捷键、WASM 初始化）。

Purpose: 完成 Phase 1 的用户交互层——SIM-06（播放/暂停/重置模拟）和所有 UI 相关决策（D-07, D-08, D-11）。
Output: 完整的可交互物理沙盒——工具栏控制模拟、键盘快捷键、加载和错误处理。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/01-UI-SPEC.md
@.planning/research/PITFALLS.md
@.planning/phases/01-simulation-core-3d-render/01-CONTEXT.md

<interfaces>
<!-- 依赖的模块接口 (由 Plan 02 创建) -->

From frontend/src/store/index.ts (Plan 02 Task 1):
```typescript
import { useSimulationStore } from '@/store';
// State: isRunning, showDebug, fps, objectCount
// Actions: play(), pause(), toggle(), reset(), setShowDebug(v)
```

From frontend/src/components/Scene3D.tsx (Plan 02 Task 3):
```typescript
import Scene3D from '@/components/Scene3D';
// Renders: <Canvas> with Physics, objects, camera, helpers
// Props: none (reads from useSimulationStore internally)
```

UI-SPEC 合同值:
- 颜色: dominant #0a0a0a, secondary #1a1a1a, accent #3b82f6, destructive #ef4444
- 间距: xs=4px, sm=8px, md=16px, lg=24px
- 字号: Body=16px, Label=14px, Heading=20px, Display=28px
- 按钮文案: ▶ 播放, ⏸ 暂停, ▶ 继续, ↺ 重置
- 工具栏: 半透明 rgba(26, 26, 26, 0.85) + backdrop-filter: blur(8px)
- 加载文案: "正在加载物理引擎..."
- WASM 错误文案: "物理引擎加载失败 — Rapier WASM 初始化失败，请刷新页面重试。如持续出现，请检查浏览器是否支持 WebAssembly。"
- WebGL 错误文案: "WebGL 不可用 — 您的浏览器不支持 WebGL 2.0。请使用最新版 Chrome、Firefox 或 Edge。"
- aria-labels: aria-label="播放仿真", aria-label="暂停仿真", aria-label="重置仿真"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Toolbar component with all controls</name>
  <files>frontend/src/components/Toolbar.tsx</files>
  <read_first>
    .planning/01-UI-SPEC.md (完整 UI 合同: 文案/颜色/间距/排版/交互状态/accessibility)
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-07: 顶部浮动工具栏, D-08: 键盘快捷键 tooltip)
    frontend/src/store/simulationSlice.ts (store 接口: isRunning, showDebug, fps, objectCount, toggle, reset, setShowDebug)
  </read_first>
  <behavior>
    测试规范（Toolbar 交互行为——通过检查 DOM 结构验证）:
    - 初始状态: isRunning=false → 按钮显示 "▶ 播放"（灰色图标）
    - 运行状态: isRunning=true → 按钮显示 "⏸ 暂停"（蓝色底色 accent #3b82f6, 白色图标）
    - 暂停状态: isRunning=false → 按钮显示 "▶ 播放"
    - 按钮均包含 aria-label: 播放仿真 / 暂停仿真 / 重置仿真
    - FPS 显示格式: "{n} FPS"（等宽字体）
    - 物体数量格式: "物体: {n}"
    - 工具栏背景: rgba(26, 26, 26, 0.85), backdrop-filter: blur(8px), 圆角 8px
    - 工具栏位置: 固定顶部居中, z-index 高于 Canvas
    - 调试开关: showDebug=false → 灰色; showDebug=true → 蓝色
    - Hover 状态: 重置按钮 hover 时变红 (red-500)，工具栏整体 hover 时增加不透明度至 0.95
  </behavior>
  <action>
创建 `frontend/src/components/Toolbar.tsx` —— Phase 1 浮动控制工具栏，实现所有 UI-SPEC 合同要求。

```tsx
import { Play, Pause, RotateCcw, Bug, Gauge, Boxes } from 'lucide-react';
import { useSimulationStore } from '../store';
import { cn } from '../lib/utils';

/**
 * Toolbar — 顶部浮动控制栏
 *
 * D-07: 播放/暂停/重置 + 物理调试开关 + FPS/物体计数
 * D-08: 键盘快捷键提示通过 title 属性（原生 tooltip）
 *
 * UI-SPEC 合同:
 * - 半透明背景: rgba(26, 26, 26, 0.85)
 * - 毛玻璃效果: backdrop-filter: blur(8px)
 * - 按钮间距: 8px (sm token)
 * - 工具栏内边距: 16px (md token)
 * - 状态指示器间距: 24px (lg token)
 */
export default function Toolbar() {
  const isRunning = useSimulationStore((s) => s.isRunning);
  const showDebug = useSimulationStore((s) => s.showDebug);
  const fps = useSimulationStore((s) => s.fps);
  const objectCount = useSimulationStore((s) => s.objectCount);
  const toggle = useSimulationStore((s) => s.toggle);
  const reset = useSimulationStore((s) => s.reset);
  const setShowDebug = useSimulationStore((s) => s.setShowDebug);

  const isPlaying = isRunning;

  return (
    <div
      className={cn(
        'fixed top-4 left-1/2 -translate-x-1/2 z-50',
        'flex items-center gap-2',
        'px-4 py-2',
        'rounded-lg',
        'transition-all duration-200',
        'select-none',
      )}
      style={{
        background: 'rgba(26, 26, 26, 0.85)',
        backdropFilter: 'blur(8px)',
        WebkitBackdropFilter: 'blur(8px)',
        border: '1px solid rgba(255, 255, 255, 0.06)',
        boxShadow: '0 4px 24px rgba(0, 0, 0, 0.4)',
      }}
      onMouseEnter={(e) => {
        e.currentTarget.style.background = 'rgba(26, 26, 26, 0.95)';
      }}
      onMouseLeave={(e) => {
        e.currentTarget.style.background = 'rgba(26, 26, 26, 0.85)';
      }}
    >
      {/* 播放/暂停按钮 — D-07, UI-SPEC 按钮合同 */}
      <button
        type="button"
        aria-label={isPlaying ? '暂停仿真' : '播放仿真'}
        title={isPlaying ? '暂停仿真 (Space)' : '播放仿真 (Space)'}
        onClick={toggle}
        className={cn(
          'flex items-center gap-1.5',
          'px-3 py-1.5',
          'rounded-md',
          'text-sm font-medium',
          'transition-all duration-150',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[#3b82f6]',
        )}
        style={{
          backgroundColor: isPlaying ? '#3b82f6' : 'transparent',
          color: isPlaying ? '#ffffff' : '#a0a0a0',
        }}
        onMouseEnter={(e) => { if (!isPlaying) e.currentTarget.style.color = '#e0e0e0'; }}
        onMouseLeave={(e) => { if (!isPlaying) e.currentTarget.style.color = '#a0a0a0'; }}
      >
        {isPlaying ? <Pause size={16} strokeWidth={2} /> : <Play size={16} strokeWidth={2} />}
        <span>{isPlaying ? '⏸ 暂停' : '▶ 播放'}</span>
      </button>

      <div style={{ width: 1, height: 20, backgroundColor: 'rgba(255, 255, 255, 0.1)' }} />

      {/* 重置按钮 — D-07, destructive hover */}
      <button
        type="button"
        aria-label="重置仿真"
        title="重置仿真 (R)"
        onClick={reset}
        className={cn(
          'flex items-center gap-1.5',
          'px-3 py-1.5',
          'rounded-md',
          'text-sm font-medium',
          'transition-all duration-150',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[#3b82f6]',
        )}
        style={{ color: '#a0a0a0' }}
        onMouseEnter={(e) => { e.currentTarget.style.backgroundColor = '#ef4444'; e.currentTarget.style.color = '#ffffff'; }}
        onMouseLeave={(e) => { e.currentTarget.style.backgroundColor = 'transparent'; e.currentTarget.style.color = '#a0a0a0'; }}
      >
        <RotateCcw size={16} strokeWidth={2} />
        <span>↺ 重置</span>
      </button>

      <div style={{ width: 1, height: 20, backgroundColor: 'rgba(255, 255, 255, 0.1)' }} />

      {/* 调试开关 — D-07 */}
      <button
        type="button"
        aria-label={showDebug ? '关闭物理调试' : '开启物理调试'}
        title={showDebug ? '关闭碰撞体线框' : '显示碰撞体线框'}
        onClick={() => setShowDebug(!showDebug)}
        className={cn(
          'flex items-center gap-1.5',
          'px-3 py-1.5',
          'rounded-md',
          'text-sm font-medium',
          'transition-all duration-150',
          'focus:outline-none focus-visible:ring-2 focus-visible:ring-[#3b82f6]',
        )}
        style={{
          backgroundColor: showDebug ? '#3b82f6' : 'transparent',
          color: showDebug ? '#ffffff' : '#a0a0a0',
        }}
        onMouseEnter={(e) => { if (!showDebug) e.currentTarget.style.color = '#e0e0e0'; }}
        onMouseLeave={(e) => { if (!showDebug) e.currentTarget.style.color = '#a0a0a0'; }}
      >
        <Bug size={16} strokeWidth={2} />
      </button>

      <div style={{ width: 1, height: 20, backgroundColor: 'rgba(255, 255, 255, 0.1)' }} />

      {/* 状态指示器 — FPS + 物体计数 */}
      <div
        className="flex items-center gap-1.5 text-sm"
        style={{ color: '#888888', fontFamily: 'var(--font-mono)', fontVariantNumeric: 'tabular-nums' }}
        title="实时帧率"
      >
        <Gauge size={14} strokeWidth={2} />
        <span>{fps} FPS</span>
      </div>

      <div
        className="flex items-center gap-1.5 text-sm"
        style={{ color: '#888888', fontFamily: 'var(--font-mono)', fontVariantNumeric: 'tabular-nums' }}
        title="场景物体数量"
      >
        <Boxes size={14} strokeWidth={2} />
        <span>物体: {objectCount}</span>
      </div>
    </div>
  );
}
```

**UI 设计要点:**
- 播放按钮在 running/paused 间切换: ▶ 播放（灰色）↔ ⏸ 暂停（蓝色底色 #3b82f6，白色图标）
- 重置按钮 hover: 背景 #ef4444 (destructive)，白色图标
- 调试按钮激活: 蓝色底色 #3b82f6
- FPS + 物体计数: 等宽字体 tabular-nums，防止数字跳动
- 所有按钮 focus-visible: 蓝色焦点环 (#3b82f6)
- 键盘快捷键: 通过原生 title 属性（D-08: Space / R）

创建完成后 TypeScript 编译检查。
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "export default function Toolbar" frontend/src/components/Toolbar.tsx 返回 1</criterion>
  <criterion>grep -c "useSimulationStore" frontend/src/components/Toolbar.tsx 返回 >= 7（订阅 7 个 store 字段/actions）</criterion>
  <criterion>grep -c "aria-label.*播放仿真" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "aria-label.*暂停仿真" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "aria-label.*重置仿真" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "rgba(26, 26, 26, 0.85)" frontend/src/components/Toolbar.tsx 返回 1</criterion>
  <criterion>grep -c "blur(8px)" frontend/src/components/Toolbar.tsx 返回 2</criterion>
  <criterion>grep -c "#3b82f6" frontend/src/components/Toolbar.tsx 返回 >= 3</criterion>
  <criterion>grep -c "#ef4444" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "FPS" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "物体" frontend/src/components/Toolbar.tsx 返回 >= 1</criterion>
  <criterion>grep -c "from 'lucide-react'" frontend/src/components/Toolbar.tsx 返回 1</criterion>
  <criterion>TypeScript 编译零错误</criterion>
</acceptance_criteria>
<done>Toolbar 组件创建完成。播放/暂停/重置/调试开关全部实现，匹配 UI-SPEC 合同要求（文案、颜色、交互状态、accessibility）。FPS + 物体计数显示。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create LoadingScreen and ErrorFallback components</name>
  <files>frontend/src/components/LoadingScreen.tsx, frontend/src/components/ErrorFallback.tsx</files>
  <read_first>
    .planning/01-UI-SPEC.md (加载状态: 旋转圆环 + 文案; 错误状态: 两种错误完整文案合同; 颜色值)
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-11: WASM 加载指示器)
  </read_first>
  <behavior>
    测试规范:
    - LoadingScreen: 渲染 Loader2 图标（spin 动画） + "正在加载物理引擎..." 文本
    - LoadingScreen: 背景色 #0a0a0a，内容居中
    - ErrorFallback type="webgl": 标题 "WebGL 不可用"，描述含 "WebGL 2.0"
    - ErrorFallback type="wasm": 标题 "物理引擎加载失败"，描述含 "WebAssembly"
    - ErrorFallback: 背景色 #0a0a0a，错误卡片居中，有刷新页面按钮
  </behavior>
  <action>
创建两个系统状态组件：

---

**1. `frontend/src/components/LoadingScreen.tsx`** — WASM 加载指示器 (D-11)

```tsx
import { Loader2 } from 'lucide-react';

/**
 * LoadingScreen — WASM 引擎加载中屏幕
 *
 * D-11: WASM 加载期间中央显示加载动画 + "正在加载物理引擎..." 文字。
 * 加载完成后自动渲染场景并进入暂停状态（D-04）。
 *
 * UI-SPEC 合同:
 * - 背景: dominant #0a0a0a
 * - 图标: Lucide Loader2 + spin CSS 动画
 * - 文字: "正在加载物理引擎..." (16px body 字号, #888888)
 * - 布局: 垂直居中, flex column, gap 16px
 */
export default function LoadingScreen() {
  return (
    <div
      style={{
        position: 'fixed',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '16px',
        backgroundColor: '#0a0a0a',
        zIndex: 100,
      }}
    >
      <Loader2
        size={40}
        strokeWidth={1.5}
        color="#3b82f6"
        style={{ animation: 'spin 1s linear infinite' }}
      />
      <p
        style={{
          color: '#888888',
          fontSize: '16px',
          fontWeight: 400,
          fontFamily: 'var(--font-sans)',
        }}
      >
        正在加载物理引擎...
      </p>
      <style>{`
        @keyframes spin {
          from { transform: rotate(0deg); }
          to { transform: rotate(360deg); }
        }
      `}</style>
    </div>
  );
}
```

---

**2. `frontend/src/components/ErrorFallback.tsx`** — 错误状态展示

```tsx
import { AlertTriangle, RefreshCw } from 'lucide-react';

/**
 * ErrorFallback — 致命错误时显示的错误卡片
 *
 * 两种错误类型对应 UI-SPEC 合同文案:
 * - type="webgl": 浏览器不支持 WebGL 2.0
 * - type="wasm": Rapier WASM 初始化失败
 */

export type ErrorType = 'webgl' | 'wasm';

const ERROR_CONFIG: Record<ErrorType, { heading: string; description: string }> = {
  webgl: {
    heading: 'WebGL 不可用',
    description: '您的浏览器不支持 WebGL 2.0。请使用最新版 Chrome、Firefox 或 Edge。',
  },
  wasm: {
    heading: '物理引擎加载失败',
    description: 'Rapier WASM 初始化失败，请刷新页面重试。如持续出现，请检查浏览器是否支持 WebAssembly。',
  },
};

export default function ErrorFallback({ type }: { type: ErrorType }) {
  const config = ERROR_CONFIG[type];

  return (
    <div
      style={{
        position: 'fixed',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        backgroundColor: '#0a0a0a',
        zIndex: 200,
      }}
    >
      <div
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: '16px',
          padding: '40px 48px',
          backgroundColor: '#1a1a1a',
          borderRadius: '12px',
          border: '1px solid rgba(239, 68, 68, 0.3)',
          maxWidth: '420px',
          textAlign: 'center',
        }}
      >
        <AlertTriangle size={40} strokeWidth={1.5} color="#ef4444" />

        <h2 style={{
          color: '#fafafa',
          fontSize: '20px',
          fontWeight: 600,
          fontFamily: 'var(--font-sans)',
          margin: 0,
        }}>
          {config.heading}
        </h2>

        <p style={{
          color: '#888888',
          fontSize: '14px',
          fontWeight: 400,
          fontFamily: 'var(--font-sans)',
          lineHeight: 1.5,
          margin: 0,
        }}>
          {config.description}
        </p>

        <button
          type="button"
          onClick={() => window.location.reload()}
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: '8px',
            padding: '8px 20px',
            marginTop: '8px',
            backgroundColor: '#3b82f6',
            color: '#ffffff',
            border: 'none',
            borderRadius: '6px',
            fontSize: '14px',
            fontWeight: 500,
            fontFamily: 'var(--font-sans)',
            cursor: 'pointer',
            transition: 'background-color 150ms',
          }}
          onMouseEnter={(e) => { e.currentTarget.style.backgroundColor = '#2563eb'; }}
          onMouseLeave={(e) => { e.currentTarget.style.backgroundColor = '#3b82f6'; }}
        >
          <RefreshCw size={16} strokeWidth={2} />
          刷新页面
        </button>
      </div>
    </div>
  );
}
```

创建完成后 TypeScript 编译检查。
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -20</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "正在加载物理引擎..." frontend/src/components/LoadingScreen.tsx 返回 1</criterion>
  <criterion>grep -c "Loader2" frontend/src/components/LoadingScreen.tsx 返回 1</criterion>
  <criterion>grep -c "WebGL 不可用" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>grep -c "物理引擎加载失败" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>grep -c "WebGL 2.0" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>grep -c "WebAssembly" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>grep -c "window.location.reload()" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>grep -c "#0a0a0a" frontend/src/components/LoadingScreen.tsx 返回 1</criterion>
  <criterion>grep -c "#0a0a0a" frontend/src/components/ErrorFallback.tsx 返回 1</criterion>
  <criterion>TypeScript 编译零错误</criterion>
</acceptance_criteria>
<done>LoadingScreen 和 ErrorFallback 组件创建完成。LoadingScreen 符合 D-11 和 UI-SPEC 加载合同；ErrorFallback 覆盖 webgl 和 wasm 两种错误类型，使用 UI-SPEC 准确文案，含刷新页面按钮。</done>
</task>

<task type="auto">
  <name>Task 3: Create App component — WASM init, keyboard shortcuts, state orchestration</name>
  <files>frontend/src/components/App.tsx</files>
  <read_first>
    .planning/01-UI-SPEC.md (错误文案、布局)
    .planning/phases/01-simulation-core-3d-render/01-CONTEXT.md (D-04: 启动暂停, D-08: Space/R 快捷键, D-11: WASM 加载流程, D-12: 严禁模板)
    .planning/research/PITFALLS.md (Pitfall 1: tab-out 保护需要 visibilitychange 处理)
    frontend/src/store/simulationSlice.ts (toggle, reset actions)
    frontend/src/components/Scene3D.tsx (Plan 02 创建 — 确认默认导出名)
    frontend/src/components/LoadingScreen.tsx (当前 plan Task 2 创建 — 确认默认导出名)
    frontend/src/components/ErrorFallback.tsx (当前 plan Task 2 创建 — 确认 ErrorType 类型)
  </read_first>
  <action>
创建 `frontend/src/components/App.tsx` —— 应用根组件，负责 WASM 初始化协调、状态编排、键盘快捷键注册。

```tsx
import { useState, useEffect, useCallback } from 'react';
import Rapier from '@dimforge/rapier2d-compat';
import { useSimulationStore } from '../store';
import Scene3D from './Scene3D';
import Toolbar from './Toolbar';
import LoadingScreen from './LoadingScreen';
import ErrorFallback from './ErrorFallback';
import type { ErrorType } from './ErrorFallback';

/**
 * App — Phase 1 应用根组件
 *
 * 负责:
 * 1. WASM 引擎初始化协调 (Rapier.init())
 * 2. WebGL 可用性检测 (canvas.getContext('webgl2'))
 * 3. 键盘快捷键注册 (D-08: Space = 播放/暂停, R = 重置)
 * 4. 页面可见性变化处理 (PITFALLS #1: 切标签页自动暂停)
 * 5. 加载/错误/正常状态的渲染切换
 *
 * 状态机:
 *   init → WebGL 检测 → WASM 加载 → ready (渲染场景 + 工具栏)
 *              ↓ 失败              ↓ 失败
 *         ErrorFallback(webgl)  ErrorFallback(wasm)
 *
 * D-04: 就绪后场景渲染但物理暂停——用户需手动点击播放。
 * D-11: WASM 加载期间显示 LoadingScreen。
 * D-12: 硬编码场景仅为引擎验证——Phase 2 替换为自由添加。
 */

type AppState = 'loading' | 'error' | 'ready';

export default function App() {
  const [appState, setAppState] = useState<AppState>('loading');
  const [errorType, setErrorType] = useState<ErrorType | null>(null);

  const toggle = useSimulationStore((s) => s.toggle);
  const reset = useSimulationStore((s) => s.reset);

  // ──── 步骤 1: WebGL 可用性检测 ────
  const checkWebGL = useCallback((): boolean => {
    try {
      const canvas = document.createElement('canvas');
      const gl = canvas.getContext('webgl2') || canvas.getContext('webgl');
      return gl !== null;
    } catch {
      return false;
    }
  }, []);

  // ──── 步骤 2: WASM 物理引擎初始化 ────
  const initWasm = useCallback(async () => {
    try {
      await Rapier.init();
      setAppState('ready');
    } catch (err) {
      console.error('Rapier WASM 初始化失败:', err);
      setErrorType('wasm');
      setAppState('error');
    }
  }, []);

  // ──── 启动初始化流程 ────
  useEffect(() => {
    if (!checkWebGL()) {
      setErrorType('webgl');
      setAppState('error');
      return;
    }
    // 100ms 延迟让 LoadingScreen DOM 先渲染（避免白屏闪烁）
    const timer = setTimeout(() => { initWasm(); }, 100);
    return () => clearTimeout(timer);
  }, [checkWebGL, initWasm]);

  // ──── 键盘快捷键 (D-08) ────
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      // 忽略输入框内按键
      const target = e.target as HTMLElement;
      if (
        target.tagName === 'INPUT' ||
        target.tagName === 'TEXTAREA' ||
        target.tagName === 'SELECT' ||
        target.isContentEditable
      ) {
        return;
      }

      switch (e.code) {
        case 'Space':
          e.preventDefault();           // 防止页面滚动
          toggle();                      // D-08: Space = 播放/暂停
          break;
        case 'KeyR':
          if (!e.ctrlKey && !e.metaKey && !e.altKey) {
            e.preventDefault();
            reset();                     // D-08: R = 重置
          }
          break;
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [toggle, reset]);

  // ──── 切标签页保护 (PITFALLS #1) ────
  useEffect(() => {
    const handleVisibilityChange = () => {
      if (document.hidden) {
        const store = useSimulationStore.getState();
        if (store.isRunning) {
          store.pause();               // 自动暂停
        }
      }
      // 切回时不会自动恢复——用户需手动播放
    };
    document.addEventListener('visibilitychange', handleVisibilityChange);
    return () => document.removeEventListener('visibilitychange', handleVisibilityChange);
  }, []);

  // ──── 状态机渲染 ────
  if (appState === 'error' && errorType) {
    return <ErrorFallback type={errorType} />;
  }

  if (appState === 'loading') {
    return <LoadingScreen />;
  }

  // appState === 'ready' — 3D 场景 + 工具栏
  return (
    <>
      <Scene3D />
      <Toolbar />
    </>
  );
}
```

**关键设计决策:**
1. **WASM 初始化时机:** 100ms setTimeout 确保 LoadingScreen 先渲染到 DOM。
2. **WebGL 检测:** 尝试 webgl2 回退 webgl，任一成功即可。
3. **切标签页保护 (PITFALLS #1):** visibilitychange → 自动暂停；切回不自动恢复（教育场景：明确用户意图）。
4. **键盘安全:** 过滤输入框；R 键检查修饰键防止拦截 Ctrl+R。
5. **状态机单向:** loading → error（不可逆）或 loading → ready（不可逆）。

完整编译检查后启动开发服务器:
```bash
cd frontend && npx tsc --noEmit --project tsconfig.app.json
cd frontend && npx vite
```
</action>
<verify>
  <automated>cd frontend && npx tsc --noEmit --project tsconfig.app.json 2>&1 | head -30</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "export default function App" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "from '@dimforge/rapier2d-compat'" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "Rapier.init()" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "getContext('webgl2')" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "case 'Space':" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "case 'KeyR':" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "visibilitychange" frontend/src/components/App.tsx 返回 1</criterion>
  <criterion>grep -c "LoadingScreen" frontend/src/components/App.tsx 返回 >= 2</criterion>
  <criterion>grep -c "ErrorFallback" frontend/src/components/App.tsx 返回 >= 2</criterion>
  <criterion>grep -c "Scene3D" frontend/src/components/App.tsx 返回 >= 2</criterion>
  <criterion>grep -c "Toolbar" frontend/src/components/App.tsx 返回 >= 2</criterion>
  <criterion>grep -c "'ready'" frontend/src/components/App.tsx 返回 >= 2</criterion>
  <criterion>TypeScript 编译零错误（Plan 02 的 Scene3D.tsx 和 store 文件必须存在）</criterion>
</acceptance_criteria>
<done>App 根组件创建完成。WASM 初始化 + WebGL 检测 + 状态机（loading → error/ready）+ 键盘快捷键（Space/R）+ 切标签页保护全部实现。覆盖 D-04、D-08、D-11 决策。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户键盘输入 | window keydown listener 接收 → 需过滤输入框场景 |
| WASM 模块初始化 | 第三方 WASM 模块在浏览器沙箱内执行 → 需处理初始化失败 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | App → Rapier.init() | mitigate | try/catch 捕获 WASM 初始化失败 → 渲染 ErrorFallback(wasm) + 刷新按钮 |
| T-01-06 | Denial of Service | App → checkWebGL | mitigate | WebGL 上下文获取失败 → 渲染 ErrorFallback(webgl)，阻止后续渲染 |
| T-01-07 | Spoofing | App → keydown listener | mitigate | 过滤 INPUT/TEXTAREA/SELECT/contentEditable；R 键检查修饰键防止拦截系统快捷键 |
| T-01-08 | Elevation of Privilege | N/A | accept | Phase 1 无用户数据输入/存储，无权限提升风险 |
</threat_model>

<verification>
完整功能验证清单（浏览器手动测试）:

1. **加载流程:** 页面打开 → LoadingScreen（旋转圆环 + "正在加载物理引擎..."）→ 3D 场景出现（物体静止）→ LoadingScreen 消失
2. **工具栏:** 浮动在视口顶部居中，半透明毛玻璃效果
3. **播放/暂停:** 点击 ▶ 播放 → 物体下落碰撞 → 按钮变为 ⏸ 暂停（蓝色）；点击 ⏸ 暂停 → 物体停止 → 按钮变为 ▶ 播放
4. **重置:** 点击 ↺ 重置 → 物体恢复初始位置，仿真暂停
5. **快捷键:** Space → 播放/暂停切换；R → 重置
6. **摄像机:** 鼠标拖拽旋转、滚轮缩放、右键平移
7. **调试:** 点击 Bug 图标 → 碰撞体线框显示/隐藏
8. **FPS/物体数:** 工具栏右侧显示实时帧率和 "物体: 14"
9. **窗口缩放:** 拖动窗口边缘 → Canvas 自适应
10. **切标签页:** 运行中切标签页 → 回来时仿真已暂停
</verification>

<success_criteria>
- [ ] 应用完整启动流程: LoadingScreen → 3D 场景 + 工具栏
- [ ] 所有工具栏按钮功能正常（播放/暂停 toggle、重置、调试开关）
- [ ] Space 键切换播放/暂停，R 键重置
- [ ] 工具栏显示实时 FPS 和物体计数
- [ ] WebGL 不可用时显示 ErrorFallback（webgl 类型，准确文案）
- [ ] WASM 初始化失败时显示 ErrorFallback（wasm 类型，准确文案）
- [ ] 切标签页时仿真自动暂停
- [ ] TypeScript 编译零错误，Vite 生产构建成功
</success_criteria>

<output>
After completion, create `.planning/phases/01-simulation-core-3d-render/01-03-SUMMARY.md`
</output>
