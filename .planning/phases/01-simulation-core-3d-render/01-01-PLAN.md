---
phase: 01-simulation-core-3d-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/index.css
  - frontend/src/lib/utils.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "执行 npm install 后所有依赖安装成功，无版本冲突"
    - "执行 npx vite 开发服务器启动成功，浏览器打开可见空白深色页面"
    - "Tailwind CSS 在开发服务器中正确编译，类名生效"
  artifacts:
    - path: "frontend/package.json"
      provides: "项目依赖声明"
      contains: ["react", "three", "@react-three/fiber", "@react-three/rapier", "@dimforge/rapier2d-compat", "zustand"]
    - path: "frontend/vite.config.ts"
      provides: "Vite 构建配置，含 WASM 兼容设置"
      contains: ["optimizeDeps", "exclude", "rapier2d-compat", "target: 'esnext'"]
    - path: "frontend/tsconfig.json"
      provides: "TypeScript 严格模式编译配置"
      contains: ["\"strict\": true"]
    - path: "frontend/src/main.tsx"
      provides: "React 应用入口，渲染 <App /> 到 DOM"
    - path: "frontend/src/index.css"
      provides: "全局样式：暗色主题、Tailwind 指令、Geist 字体"
      contains: ["@tailwind base", "#0a0a0a", "Geist"]
    - path: "frontend/src/lib/utils.ts"
      provides: "shadcn cn() 工具函数"
      exports: ["cn"]
  key_links:
    - from: "frontend/index.html"
      to: "frontend/src/main.tsx"
      via: "script module src"
      pattern: "src=\"/src/main.tsx\""
    - from: "frontend/src/main.tsx"
      to: "frontend/src/index.css"
      via: "import statement"
      pattern: "import.*index.css"
---

<objective>
初始化 Vite + React 19 + TypeScript 5.7 项目骨架，安装全部依赖，配置 Tailwind CSS + shadcn 风格基础设施，创建暗色主题全局样式和 React 应用入口。

Purpose: 项目当前无 package.json 和构建配置——这是所有后续 Plan 的前置依赖。
Output: 可启动的开发服务器，空白暗色页面渲染正常，Tailwind 类名可编译。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/01-UI-SPEC.md

<interfaces>
<!-- Existing code patterns the executor must follow -->
From frontend/src/store/api.ts:
```typescript
// Existing Zustand store — Phase 1 extends this with slices
import type { PhysisStore } from './index'
export interface ApiActions { ... }
export const createApiActions = (set, get): ApiActions => ({ ... })
```

Key patterns:
- TypeScript strict mode throughout (existing code convention)
- File naming: kebab-case for non-component files, PascalCase for React components
- Export pattern: named exports for slices/utilities, default export for components
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with all dependencies and install</name>
  <files>frontend/package.json</files>
  <read_first>
    .planning/research/STACK.md (完整依赖列表和精确版本范围)
    .planning/01-UI-SPEC.md (shadcn 组件需求: Button, Tooltip → 需要的 Tailwind/Radix 依赖)
  </read_first>
  <action>
创建 `frontend/package.json`，包含以下精确内容：

**项目元数据:**
```json
{
  "name": "physis",
  "private": true,
  "version": "0.1.0",
  "type": "module"
}
```

**scripts:**
- `"dev": "vite"`
- `"build": "tsc -b && vite build"`
- `"preview": "vite preview"`

**dependencies (生产依赖):**
- `"react": "^19.1.0"`
- `"react-dom": "^19.1.0"`
- `"three": "^0.174.0"`
- `"@react-three/fiber": "^9.1.0"`
- `"@react-three/drei": "^9.120.0"`
- `"@react-three/rapier": "^2.2.0"`
- `"@dimforge/rapier2d-compat": "^0.19.3"`
- `"zustand": "^5.0.5"`
- `"lucide-react": "^0.487.0"`
- `"class-variance-authority": "^0.7.1"`
- `"clsx": "^2.1.1"`
- `"tailwind-merge": "^3.2.0"`
- `"@radix-ui/react-tooltip": "^1.1.8"`
- `"@radix-ui/react-slot": "^1.1.2"`

**devDependencies (开发依赖):**
- `"typescript": "~5.7.3"`
- `"@types/react": "^19.1.0"`
- `"@types/react-dom": "^19.1.0"`
- `"@types/three": "^0.173.0"`
- `"vite": "^6.3.0"`
- `"@vitejs/plugin-react": "^4.4.0"`
- `"tailwindcss": "^4.1.0"`
- `"@tailwindcss/vite": "^4.1.0"`
- `"autoprefixer": "^10.4.20"`
- `"postcss": "^8.5.0"`

创建完成后执行 `cd frontend && npm install`。

**注意:** 使用 Tailwind CSS v4（与 Vite 6 配合最佳），通过 `@tailwindcss/vite` 插件而非 PostCSS 插件方式引入。
</action>
<verify>
  <automated>node -e "const p = require('./frontend/package.json'); const deps = {...p.dependencies, ...p.devDependencies}; ['react','three','@react-three/fiber','@react-three/rapier','@dimforge/rapier2d-compat','zustand','vite'].forEach(k => { if (!deps[k]) throw new Error('Missing: '+k) }); process.exit(0)"</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c '"react"' frontend/package.json 返回 >= 1</criterion>
  <criterion>grep -c '"@react-three/rapier"' frontend/package.json 返回 >= 1</criterion>
  <criterion>grep -c '"@dimforge/rapier2d-compat"' frontend/package.json 返回 >= 1</criterion>
  <criterion>grep -c '"vite"' frontend/package.json 返回 >= 1</criterion>
  <criterion>test -d frontend/node_modules/@react-three/fiber 返回 true</criterion>
  <criterion>test -d frontend/node_modules/@dimforge/rapier2d-compat 返回 true</criterion>
</acceptance_criteria>
<done>package.json 包含所有声明的依赖，执行 npm install 成功，node_modules 包含 Rapier WASM 和 R3F。</done>
</task>

<task type="auto">
  <name>Task 2: Create Vite config, TypeScript config, and global styles</name>
  <files>frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.app.json, frontend/tsconfig.node.json, frontend/index.html, frontend/src/main.tsx, frontend/src/index.css, frontend/src/lib/utils.ts</files>
  <read_first>
    .planning/research/STACK.md (Vite 配置关键项: optimizeDeps.exclude, build.target)
    .planning/research/PITFALLS.md (WASM 兼容性: COOP/COEP 暂不需要)
    .planning/01-UI-SPEC.md (颜色值: dominant #0a0a0a, secondary #1a1a1a, accent #3b82f6, destructive #ef4444)
  </read_first>
  <action>
创建以下文件，每个文件内容必须与指定值完全一致：

---

**1. `frontend/vite.config.ts`:**
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import path from 'path';

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  optimizeDeps: {
    exclude: ['@dimforge/rapier2d-compat'],
  },
  build: {
    target: 'esnext',
  },
  server: {
    port: 5173,
    open: true,
  },
});
```

关键配置说明:
- `optimizeDeps.exclude: ['@dimforge/rapier2d-compat']` — Rapier WASM 不可被 Vite 预构建（会破坏 WASM 内联 base64）
- `build.target: 'esnext'` — 支持 top-level await（WASM 异步初始化需要）
- `@tailwindcss/vite` — Tailwind CSS v4 的 Vite 插件

---

**2. `frontend/tsconfig.json`:**
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

---

**3. `frontend/tsconfig.app.json`:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src"]
}
```

---

**4. `frontend/tsconfig.node.json`:**
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "noEmit": true
  },
  "include": ["vite.config.ts"]
}
```

---

**5. `frontend/index.html`:**
```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Physis — 物理模拟平台</title>
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>⚛</text></svg>" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

---

**6. `frontend/src/main.tsx`:**
```typescript
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './components/App';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

---

**7. `frontend/src/index.css`:**
使用 Tailwind CSS v4 语法（`@import "tailwindcss"`），定义暗色主题全局变量和 Geist 字体。

```css
@import "tailwindcss";

/* Geist 字体 — 从 Google Fonts 加载 */
@import url('https://fonts.googleapis.com/css2?family=Geist:wght@400;600&family=Geist+Mono:wght@400;600&display=swap');

@theme {
  --font-sans: 'Geist', 'Geist Fallback', system-ui, -apple-system, sans-serif;
  --font-mono: 'Geist Mono', 'Geist Mono Fallback', 'Consolas', monospace;
}

/* 全局暗色主题 — 对应 UI-SPEC 颜色合同 */
:root {
  color-scheme: dark;
  background-color: #0a0a0a;
  color: #fafafa;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #root {
  width: 100%;
  height: 100%;
  overflow: hidden;
  font-family: var(--font-sans);
}

/* 禁止文本选中（沙盒模式 UI 不需要文本选择） */
body {
  user-select: none;
  -webkit-user-select: none;
}

/* 滚动条暗色主题 */
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track {
  background: #0a0a0a;
}
::-webkit-scrollbar-thumb {
  background: #333;
  border-radius: 3px;
}
```

---

**8. `frontend/src/lib/utils.ts`** (shadcn cn() 工具函数):
```typescript
import { type ClassValue, clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

---

创建所有文件后，执行 `cd frontend && npx vite --host`，确认开发服务器启动无报错，浏览器打开后页面背景色为 `#0a0a0a`（深黑色），控制台无报错。
</action>
<verify>
  <automated>cd frontend && npx vite --host &amp;&amp; sleep 3 &amp;&amp; curl -s http://localhost:5173 | grep -c 'id="root"'</automated>
</verify>
<acceptance_criteria>
  <criterion>grep -c "exclude.*rapier2d-compat" frontend/vite.config.ts 返回 >= 1</criterion>
  <criterion>grep -c '"strict": true' frontend/tsconfig.app.json 返回 >= 1</criterion>
  <criterion>grep -c 'id="root"' frontend/index.html 返回 1</criterion>
  <criterion>grep -c "import App from" frontend/src/main.tsx 返回 1</criterion>
  <criterion>grep -c '#0a0a0a' frontend/src/index.css 返回 >= 1</criterion>
  <criterion>grep -c "export function cn" frontend/src/lib/utils.ts 返回 1</criterion>
  <criterion>grep -c "@import \"tailwindcss\"" frontend/src/index.css 返回 1</criterion>
</acceptance_criteria>
<done>所有配置文件已创建，Vite 开发服务器启动成功，浏览器打开显示深色 (#0a0a0a) 背景页面，控制台无报错，Tailwind 类名可编译。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无外部边界 | Phase 1 无后端/无用户输入——所有数据硬编码，无信任边界需防护 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Vite dev server | accept | 仅本地开发服务器，无外部暴露 |

（Phase 1 基础设施层无用户数据流——安全威胁模型在 Plan 02/03 中覆盖 WebGL/WASM 错误处理。）
</threat_model>

<verification>
启动验证:
```bash
cd frontend && npx tsc --noEmit --project tsconfig.app.json
```
TypeScript 编译零错误。

```bash
cd frontend && npx vite build
```
生产构建成功，无警告。
</verification>

<success_criteria>
- [ ] `frontend/package.json` 包含所有声明的依赖，版本号精确
- [ ] `npm install` 成功，node_modules 包含 @react-three/fiber、@react-three/rapier、@dimforge/rapier2d-compat
- [ ] `npx vite` 启动无报错，浏览器自动打开，页面背景为深黑色 (#0a0a0a)
- [ ] TypeScript 严格模式编译通过 (`npx tsc --noEmit`)
- [ ] `npx vite build` 生产构建成功
</success_criteria>

<output>
After completion, create `.planning/phases/01-simulation-core-3d-render/01-01-SUMMARY.md`
</output>
