---
wave: 2
depends_on: ['04-01']
files_modified:
  - frontend/src/components/TrajectoryRenderer.tsx
  - frontend/src/components/Scene3D.tsx
autonomous: true
requirements_addressed: [DIF-02]
---

# Plan 04-02: 轨迹渲染系统

## Objective
实现运动轨迹残影的 3D 渲染：TrajectoryRenderer 组件在 `useFrame` 中按 30Hz 采样实体位置，以 BufferGeometry + 顶点颜色渐变渲染"彗星尾巴"效果。挂载到 Scene3D 的 `<RigidBodyRefContext.Provider>` 内。

## must_haves
- TrajectoryRenderer 组件在 `useFrame` 中通过 `RigidBodyRefContext` 读取实体位置
- 30Hz 采样频率，静止物体（|velocity| < 0.01）跳过采样
- 渐变公式：旧点 alpha→0（透明），新点 alpha→1（实色），线性插值
- 深度测试正常开启——轨迹被遮挡时不可见
- 模拟重置时所有轨迹清空（监听 isRunning 从 false→true 或 reset 信号）

## truths
- PITFALLS #6：轨迹采样在 useFrame 中使用 useRef 持有 TrajectoryBuffer，禁止写入 Zustand
- 使用的 hook 名称为 `useRigidBodyRefRegistry`（来自 `RigidBodyRefContext.tsx`）
- visualizationStore 已在 04-01 中创建，直接从 `@/store/visualizationStore` 导入
- 实体数据通过 `useSimulationStore` 的 `entities` 访问（组合式 store hook）

---

### Task 1: 实现 TrajectoryRenderer 组件

<read_first>
- frontend/src/components/EntityRenderer.tsx（参考模式）
- frontend/src/components/RigidBodyRefContext.tsx（确认 hook 名称：useRigidBodyRefRegistry）
- frontend/src/store/index.ts（确认 useSimulationStore 导出 entities 的方式）
- frontend/src/ecs/TrajectoryBuffer.ts（04-01 Task 4 产出）
- frontend/src/store/visualizationStore.ts（04-01 Task 3 产出）
- frontend/src/ecs/types.ts（04-01 Task 1 产出，TrailComponent 定义）
</read_first>

<action>
创建 `frontend/src/components/TrajectoryRenderer.tsx`：

```tsx
import { useRef, useEffect } from 'react';
import { useFrame } from '@react-three/fiber';
import * as THREE from 'three';
import { useSimulationStore } from '../store';
import { useVisualizationStore } from '../store/visualizationStore';
import { TrajectoryBuffer } from '../ecs/TrajectoryBuffer';
import { useRigidBodyRefRegistry } from './RigidBodyRefContext';
import type { TrailComponent } from '../ecs/types';

const SAMPLE_INTERVAL = 1 / 30; // 30Hz
const VELOCITY_THRESHOLD = 0.01;

interface TrailEntry {
  buffer: TrajectoryBuffer;
  line: THREE.Line;
}

export function TrajectoryRenderer() {
  const entities = useSimulationStore((s) => s.entities);
  const showTrails = useVisualizationStore((s) => s.showTrails);
  const rigidBodyRefs = useRigidBodyRefRegistry();

  const trailMap = useRef<Map<string, TrailEntry>>(new Map());
  const groupRef = useRef<THREE.Group>(null);

  // 创建/更新 trail lines
  useFrame((_, delta) => {
    if (!showTrails) return;

    const now = performance.now() / 1000;

    for (const [entityId, entity] of entities) {
      const trailComp = entity.components.get('trail') as
        | TrailComponent
        | undefined;
      if (trailComp && trailComp.visible === false) continue;

      const rb = rigidBodyRefs.get(entityId);
      if (!rb || !rb.current) continue;

      const vel = rb.current.linvel();
      const speed = Math.sqrt(vel.x ** 2 + vel.y ** 2 + vel.z ** 2);
      if (speed < VELOCITY_THRESHOLD) continue;

      let entry = trailMap.current.get(entityId);
      if (!entry) {
        // 创建 BufferGeometry + Line
        const geometry = new THREE.BufferGeometry();
        const positions = new Float32Array(300 * 3);
        const colors = new Float32Array(300 * 3);
        geometry.setAttribute(
          'position',
          new THREE.BufferAttribute(positions, 3)
        );
        geometry.setAttribute(
          'color',
          new THREE.BufferAttribute(colors, 3)
        );
        geometry.setDrawRange(0, 0);

        const material = new THREE.LineBasicMaterial({
          vertexColors: true,
          transparent: true,
          opacity: 0.8,
          depthTest: true,
        });

        const line = new THREE.Line(geometry, material);
        line.frustumCulled = false;
        groupRef.current?.add(line);

        entry = { buffer: new TrajectoryBuffer(), line };
        trailMap.current.set(entityId, entry);
      }

      const buf = entry.buffer;
      const lastTime =
        buf.getCount() > 0 ? now : now - SAMPLE_INTERVAL - 0.001;

      if (now - lastTime >= SAMPLE_INTERVAL) {
        const pos = rb.current.translation();
        buf.push(
          new THREE.Vector3(pos.x, pos.y, pos.z),
          now
        );

        // 更新 BufferGeometry
        const { positions: pts, count } = buf.getPoints(now);
        const geo = entry.line.geometry;
        const posAttr = geo.attributes
          .position as THREE.BufferAttribute;
        const colAttr = geo.attributes
          .color as THREE.BufferAttribute;

        const baseColor =
          entity.components.get('material') &&
          typeof (entity.components.get('material') as any).color === 'string'
            ? new THREE.Color(
                (entity.components.get('material') as any).color
              )
            : new THREE.Color('#ffffff');

        for (let i = 0; i < count; i++) {
          posAttr.setXYZ(i, pts[i].x, pts[i].y, pts[i].z);
          const t = count > 1 ? i / (count - 1) : 1; // 0=旧, 1=新
          colAttr.setXYZ(
            i,
            baseColor.r * t,
            baseColor.g * t,
            baseColor.b * t
          );
        }

        posAttr.needsUpdate = true;
        colAttr.needsUpdate = true;
        geo.setDrawRange(0, count);
      }
    }

    // 清理已删除实体的 trail
    for (const [entityId, entry] of trailMap.current) {
      if (!entities.has(entityId)) {
        groupRef.current?.remove(entry.line);
        entry.line.geometry.dispose();
        (entry.line.material as THREE.Material).dispose();
        trailMap.current.delete(entityId);
      }
    }
  });

  // 重置模拟时清空所有轨迹（D-03）
  const isRunning = useSimulationStore((s) => s.isRunning);
  const prevRunning = useRef(isRunning);
  useEffect(() => {
    if (!prevRunning.current && isRunning) {
      // 从暂停→运行 = 重置（Phase 2 D-12 语义）
      for (const [, entry] of trailMap.current) {
        entry.buffer.clear();
      }
    }
    prevRunning.current = isRunning;
  });

  // 全局开关关闭时隐藏所有 lines
  useEffect(() => {
    if (groupRef.current) {
      groupRef.current.visible = showTrails;
    }
  }, [showTrails]);

  return <group ref={groupRef} />;
}
```
</action>

<acceptance_criteria>
- `frontend/src/components/TrajectoryRenderer.tsx` 存在且导出 `TrajectoryRenderer`
- 导入 `useRigidBodyRefRegistry`（非 `useRigidBodyRefs`）
- 导入 `useSimulationStore`（非 `useEntityStore`）
- `showTrails` 从 `useVisualizationStore` 读取
- 静止物体（speed < 0.01）跳过采样
- 渐变：旧点颜色 t→0，新点颜色 t→1
- 实体删除时对应的 line 被 dispose 和 remove
- 重置时 `TrajectoryBuffer.clear()` 被调用
</acceptance_criteria>

---

### Task 2: 挂载 TrajectoryRenderer 到 Scene3D

<read_first>
- frontend/src/components/Scene3D.tsx
</read_first>

<action>
在 `frontend/src/components/Scene3D.tsx` 中：

1. 添加导入：
```typescript
import { TrajectoryRenderer } from './TrajectoryRenderer';
```

2. 在 `<RigidBodyRefContext.Provider>` 内部、`<SpringRenderer />` 之后添加：
```tsx
<TrajectoryRenderer />
```

确保渲染顺序：`EntityRenderer` → `SpringRenderer` → `TrajectoryRenderer`
</action>

<acceptance_criteria>
- `Scene3D.tsx` 导入 `TrajectoryRenderer`
- `TrajectoryRenderer` 挂载在 `<RigidBodyRefContext.Provider>` 内
- `tsc --noEmit` 或 `npm run build` 无编译错误
</acceptance_criteria>
