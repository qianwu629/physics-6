---
phase: 01-持久化与场景库
plan: 05
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - frontend/src/components/App.tsx
  - frontend/src/components/Scene3D.tsx
autonomous: false
requirements:
  - PERSIST-01
  - PERSIST-02
  - PERSIST-03
  - PERSIST-04

must_haves:
  truths:
    - "App.tsx 渲染 MenuBar 在页面顶部"
    - "MenuBar [文件] → 快照管理 打开右侧 SnapshotManager Drawer"
    - "MenuBar [文件] → 预设场景库 打开 PresetSelector Dialog"
    - "加载场景后摄像机自适应实体包围盒（不是固定默认位置）"
    - "导入/预设加载/快照恢复时的 warnings 通过 SceneBanner 显示黄色横幅"
    - "Toolbar 位置下移 36px 为 MenuBar 腾出空间"
  artifacts:
    - path: "frontend/src/components/App.tsx"
      provides: "集成 MenuBar + SnapshotManager Sheet + PresetSelector Dialog + SceneBanner"
      contains: "MenuBar"
      contains: "SnapshotManager"
      contains: "PresetSelector"
      contains: "SceneBanner"
      min_lines: 250
    - path: "frontend/src/components/Scene3D.tsx"
      provides: "添加摄像机自适应（监听 resetCounter → 计算 bounding box → 更新 camera）"
      contains: "Box3"
      contains: "fitCameraToScene"
      min_lines: 300
  key_links:
    - from: "App.tsx MenuBar onOpenSnapshots"
      to: "App.tsx snapshotDrawerOpen state → Sheet open={snapshotDrawerOpen}"
      via: "useState(false) 控制 Sheet 开/关"
      pattern: "snapshotDrawerOpen.*Sheet"
    - from: "App.tsx MenuBar onOpenPresets"
      to: "App.tsx presetSelectorOpen state → Dialog open={presetSelectorOpen}"
      via: "useState(false) 控制 Dialog 开/关"
      pattern: "presetSelectorOpen.*Dialog"
    - from: "Scene3D.tsx resetCounter useEffect"
      to: "OrbitControls ref + camera ref"
      via: "useEffect → Box3.setFromObject(scene) → compute cameraDistance → update camera.position + controls.target"
      pattern: "useEffect.*resetCounter.*Box3"
---

<objective>
将 Phase 1 所有新建组件集成到 App.tsx，并增强 Scene3D 的摄像机自适应功能。这是"接线"阶段——MenuBar、SnapshotManager Drawer、PresetSelector Dialog、SceneBanner 均在此阶段挂载到应用中。

Purpose: 让所有 Phase 1 组件协同工作。MenuBar 的菜单项触发 SnapshotManager 和 PresetSelector 的打开；SceneLoader 的 warnings 通过 SceneBanner 显示；摄像机在场景加载后自适应实体包围盒。
Output: 修改后的 App.tsx + 修改后的 Scene3D.tsx（添加摄像机自适应）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-持久化与场景库/01-CONTEXT.md (D-01-03, D-01-04, D-01-05, D-01-08)
@.planning/phases/01-持久化与场景库/01-RESEARCH.md (Pattern 4, Pitfall #3)

<interfaces>
新组件接口：

从 frontend/src/components/MenuBar.tsx (Plan 03):
```typescript
interface MenuBarProps {
  onOpenSnapshots?: () => void;
  onOpenPresets?: () => void;
}
export default function MenuBar(props: MenuBarProps): JSX.Element;
```

从 frontend/src/components/SnapshotManager.tsx (Plan 02):
```typescript
interface SnapshotManagerProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
export default function SnapshotManager(props: SnapshotManagerProps): JSX.Element;
```

从 frontend/src/components/PresetSelector.tsx (Plan 04):
```typescript
interface PresetSelectorProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
export default function PresetSelector(props: PresetSelectorProps): JSX.Element;
```

从 frontend/src/components/SceneLoader.tsx (Plan 03):
```typescript
export function SceneBanner(): JSX.Element; // 渲染黄色警告横幅
export function useSceneBanner(): { warnings: string[]; addWarning: (w: string) => void; clearWarnings: () => void; dismissWarning: (index: number) => void; };
```

现有组件接口：

从 frontend/src/components/App.tsx (当前):
```typescript
export default function App(): JSX.Element;
// 当前渲染: Scene3D + Toolbar + Toolbox + PropertyPanel + CreationDialog + EnvironmentPanel + SpringCreationBanner + SpringCreationDialog
// 当前 z-index: Toolbar z-40, PropertyPanel z-40, Toolbox z-40
```

从 frontend/src/components/Scene3D.tsx (当前):
```typescript
export default function Scene3D(): JSX.Element;
// 当前 camera: { position: [12, 10, 12], fov: 45, near: 0.1, far: 200 }
// 当前 OrbitControls: target={[0, 2, 0]}
// 当前 Physics: key={resetCounter}
```

Three.js 相关 API (来自 RESEARCH.md Pattern 4):
```typescript
import { Box3, Vector3 } from 'three';
// Box3: new Box3(), box.expandByPoint(pos), box.getCenter(center), box.getSize(size)
// PerspectiveCamera: camera.position.set(x, y, z), camera.fov
// OrbitControls (drei): controls.target.copy(center), controls.update()
// useThree(): { camera, scene }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 集成 MenuBar + SnapshotManager + PresetSelector + SceneBanner 到 App.tsx</name>
  <files>frontend/src/components/App.tsx</files>
  <read_first>frontend/src/components/App.tsx, frontend/src/components/MenuBar.tsx, frontend/src/components/SnapshotManager.tsx, frontend/src/components/PresetSelector.tsx, frontend/src/components/SceneLoader.tsx, frontend/src/components/Toolbar.tsx</read_first>
  <action>
修改 `frontend/src/components/App.tsx`，在现有渲染树中集成 Phase 1 新组件。

**变更内容：**

1. **新增 import（文件顶部）：**
```typescript
import MenuBar from './MenuBar';
import SnapshotManager from './SnapshotManager';
import PresetSelector from './PresetSelector';
import { SceneBanner } from './SceneLoader';
```

2. **新增状态变量（在 App 函数体开头，靠近其他 useState）：**
```typescript
const [snapshotDrawerOpen, setSnapshotDrawerOpen] = useState(false);
const [presetSelectorOpen, setPresetSelectorOpen] = useState(false);
```

3. **修改 JSX 渲染树（在 `appState === 'ready'` 的返回块中）：**

当前结构：
```
<Scene3D />
<Toolbar />
<Toolbox />
{!propertyPanelCollapsed && <PropertyPanel />}
{propertyPanelCollapsed && <展开按钮>}
<CreationDialog />
<EnvironmentPanel />
<SpringCreationBanner />
<SpringCreationDialog />
```

修改为（MenuBar 在最顶层，SnapshotManager/PresetSelector/SceneBanner 在之后）：

```tsx
return (
  <>
    {/* Phase 1: MenuBar — 固定在页面顶部 z-50 */}
    <MenuBar
      onOpenSnapshots={() => setSnapshotDrawerOpen(true)}
      onOpenPresets={() => setPresetSelectorOpen(true)}
    />

    {/* Phase 1: SceneBanner — schema 版本不匹配等警告 */}
    <SceneBanner />

    {/* Phase 1: SnapshotManager Drawer (Sheet side="right") — 由 MenuBar 触发 */}
    <SnapshotManager
      open={snapshotDrawerOpen}
      onOpenChange={setSnapshotDrawerOpen}
    />

    {/* Phase 1: PresetSelector Dialog — 由 MenuBar 触发 */}
    <PresetSelector
      open={presetSelectorOpen}
      onOpenChange={setPresetSelectorOpen}
    />

    <Scene3D />
    <Toolbar />
    <Toolbox />
    {!propertyPanelCollapsed && <PropertyPanel />}
    {propertyPanelCollapsed && (
      <button /* 展开属性面板按钮 — 原有代码保持不变 */ />
    )}
    <CreationDialog />
    <EnvironmentPanel />
    <SpringCreationBanner />
    <SpringCreationDialog />
  </>
);
```

4. **Toolbar 位置调整：**

MenuBar 高度为 36px（`h-9`），固定定位且 z-index 高于 Toolbar。当前 Toolbar 可能被 MenuBar 遮挡。

**方案：** 检查 Toolbar.tsx 中其定位方式。如果 Toolbar 使用 `fixed top-0` 定位，需要改为 `fixed top-9`（即 top: 36px）以为 MenuBar 腾出空间。

由于 Plan 05 的 files_modified 仅含 App.tsx 和 Scene3D.tsx，采用以下策略处理 Toolbar 偏移：

在 App.tsx 的 `useEffect` 中注入全局样式：
```tsx
useEffect(() => {
  const style = document.createElement('style');
  style.id = 'phase1-menu-offset';
  // 选择器根据 Toolbar.tsx 的实际结构确定，此处假设使用 data-slot 属性或固定类名
  style.textContent = `
    [data-toolbar] { top: 40px !important; }
  `;
  document.head.appendChild(style);
  return () => {
    const el = document.getElementById('phase1-menu-offset');
    if (el) el.remove();
  };
}, []);
```
如果 Toolbar 没有 `data-toolbar` 属性，执行时先读取 Toolbar.tsx 确定其实际的顶层类名或定位方式，然后调整选择器。

最可靠的方式：在 MenuBar 组件中设置其 CSS `position: fixed; top: 0; height: 36px`（z-50），确保它不会与其他 fixed 组件重叠。Toolbar 如果位置被遮挡，在其最外层容器添加 `style={{ top: '40px' }}`。由于 Toolbar.tsx 不在 files_modified 中，通过 CSS 注入是最小侵入方案。
</action>
  <verify>
    <automated>grep -n "MenuBar\|SnapshotManager\|PresetSelector\|SceneBanner" frontend/src/components/App.tsx | grep -v "^#" > /dev/null && echo "Imports found" || echo "MISSING_IMPORTS"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/App.tsx` 中 `grep -c "import MenuBar"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "import SnapshotManager"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "import PresetSelector"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "import.*SceneBanner"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "<MenuBar"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "<SnapshotManager"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "<PresetSelector"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "<SceneBanner"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "snapshotDrawerOpen\|snapshotDrawer.*useState"` 返回 >= 1
    - `frontend/src/components/App.tsx` 中 `grep -c "presetSelectorOpen\|presetSelector.*useState"` 返回 >= 1
    - App.tsx 保持原有 `<Scene3D />` `<Toolbar />` `<Toolbox />` 等渲染不变（不删除现有组件）
    - TypeScript 编译通过（`npx tsc --noEmit` 在 frontend 目录下无新增错误）
  </acceptance_criteria>
  <done>MenuBar / SnapshotManager / PresetSelector / SceneBanner 全部集成到 App.tsx；原有组件保持渲染不动</done>
</task>

<task type="auto">
  <name>Task 2: 增强 Scene3D — 摄像机自适应实体包围盒</name>
  <files>frontend/src/components/Scene3D.tsx</files>
  <read_first>frontend/src/components/Scene3D.tsx, frontend/src/components/EntityRenderer.tsx</read_first>
  <action>
修改 `frontend/src/components/Scene3D.tsx`，添加场景加载后的摄像机自适应功能（D-01-03 的 camera 部分）。

**新增功能：** 当 `resetCounter` 变化时（意味着场景已重置或新场景已加载），摄像机自适应场景中所有实体的包围盒。

**实现步骤：**

1. **新增 import（文件顶部）：**
```typescript
import { useThree } from '@react-three/fiber';
import { Box3, Vector3, type PerspectiveCamera } from 'three';
```

2. **定义 CameraFitter 组件（在文件顶层，Scene3D 函数体外）：**
```tsx
function CameraFitter({ controlsRef }: { controlsRef: React.MutableRefObject<any> }) {
  const { camera } = useThree();
  const resetCounter = useSimulationStore((s) => s.resetCounter);

  useEffect(() => {
    if (resetCounter === 0) return; // 首次挂载不触发自适应（保留默认视角）
    const timer = setTimeout(() => {
      const ctrl = controlsRef.current;
      if (!ctrl) return;

      // 从 store 读取实体位置计算包围盒
      const store = useSimulationStore.getState();
      const box = new Box3();
      let hasAny = false;

      for (const [, entity] of store.entities) {
        const t = entity.components.get('transform');
        if (t) {
          const p = (t as any).position as [number, number, number];
          if (p && Array.isArray(p) && p.length === 3) {
            box.expandByPoint(new Vector3(p[0], p[1], p[2]));
            hasAny = true;
          }
        }
      }

      if (!hasAny) {
        // 空场景 → 默认视角
        (camera as PerspectiveCamera).position.set(12, 10, 12);
        ctrl.target.set(0, 2, 0);
        ctrl.update();
        return;
      }

      // 确保包围盒有最小体积（用于单点或共线场景）
      box.expandByScalar(1);

      const center = new Vector3();
      box.getCenter(center);
      const size = new Vector3();
      box.getSize(size);

      const maxDim = Math.max(size.x, size.y, size.z);
      const fovRad = (camera as PerspectiveCamera).fov * (Math.PI / 180);
      const dist = (maxDim / (2 * Math.tan(fovRad / 2))) * 1.5;

      // 对角线方向摄像机位置
      (camera as PerspectiveCamera).position.set(
        center.x + dist * 0.7,
        center.y + dist * 0.6,
        center.z + dist * 0.7,
      );
      ctrl.target.copy(center);
      ctrl.update(); // Pitfall #3: 必须调用 update() 才能让 OrbitControls 生效
    }, 200);

    return () => clearTimeout(timer);
  }, [resetCounter, camera, controlsRef]);

  return null; // 纯逻辑组件，无 DOM 输出
}
```

3. **在 Scene3D 函数体内添加 ref：**
在 `const initialCameraPosition:` 之后、`return (...)` 之前添加：
```typescript
const controlsRef = useRef<any>(null);
```

4. **修改 `<OrbitControls>` 添加 ref 属性：**
将现有：
```tsx
<OrbitControls
  target={initialCameraTarget}
  enableDamping={true}
  ...
/>
```
修改为：
```tsx
<OrbitControls
  ref={controlsRef}
  target={initialCameraTarget}
  enableDamping={true}
  ...
/>
```

5. **在 `<Canvas>` 内部、`<FpsTracker />` 之后添加 CameraFitter：**
```tsx
<FpsTracker />
<SceneInitializer />
<CameraFitter controlsRef={controlsRef} />
```

**关于 Pitfall #3（OrbitControls 更新必须调用 update()）：** 上述代码在修改 target 和 position 后调用 `ctrl.update()`，确保摄像机变更生效。

**关于 `expandByScalar(1)`：** 当场景中只有单点或共线/共面实体时，包围盒在某个维度大小为 0，会导致 `maxDim / tan(fovRad/2)` 计算异常。`expandByScalar(1)` 确保最小包围盒体积，避免极端情况下的 NaN 摄像机距离。
</action>
  <verify>
    <automated>grep -n "Box3\|fitCamera\|CameraFitter\|controlsRef" frontend/src/components/Scene3D.tsx | grep -v "^#" > /dev/null && echo "Camera adaptive code found" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/Scene3D.tsx` 中 `grep -c "Box3"` 返回 >= 1
    - `frontend/src/components/Scene3D.tsx` 中 `grep -c "expandByPoint\|getCenter\|getSize"` 返回 >= 1
    - `frontend/src/components/Scene3D.tsx` 中 `grep -c "controlsRef\|controls\\.update"` 返回 >= 1
    - `frontend/src/components/Scene3D.tsx` 中 `grep -c "resetCounter"` 返回 >= 1（监听 resetCounter 变化触发自适应）
    - `frontend/src/components/Scene3D.tsx` 中 `<OrbitControls` 包含 `ref={controlsRef}` 属性
    - `frontend/src/components/Scene3D.tsx` 中 `CameraFitter` 组件包含 `return null`（纯逻辑组件）
    - TypeScript 编译通过（`npx tsc --noEmit` 在 frontend 目录下无新增错误）
  </acceptance_criteria>
  <done>Scene3D 摄像机自适应就位；场景加载后摄像机自动适配实体包围盒；空场景时回退默认视角</done>
</task>

</tasks>

<checkpoint_usage>
<!-- Plan 05 需要 visual 验证 checkpoint 因为涉及 UI 布局变更和摄像机自适应视觉效果 -->
</checkpoint_usage>

<threat_model>
## Trust Boundaries

无新增 — App.tsx 集成仅涉及组件挂载/卸载，不引入新的数据边界。

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | — | App.tsx integration | accept | 纯组件组合，无安全影响 |
| T-05-02 | — | Scene3D CameraFitter | accept | 仅读取 store 数据计算摄像机位置，不修改数据 |
</threat_model>

<verification>
### 自动化验证

```bash
# 类型检查
npx tsc --noEmit --project frontend/tsconfig.json 2>&1 | grep -i "App\|Scene3D"

# 检查关键导入和渲染
grep -c "MenuBar\|SnapshotManager\|PresetSelector\|SceneBanner" frontend/src/components/App.tsx
grep -c "Box3\|CameraFitter" frontend/src/components/Scene3D.tsx
```

### 手动验证（推荐 checkpoint）

1. 启动 dev server：`npm run dev`
2. 验证 MenuBar 在页面顶部渲染（[文件 ▾] [视图 ▾] [帮助 ▾]）
3. 点击 [文件] → 快照管理 → 右侧 Drawer 滑出
4. 点击 [文件] → 预设场景库 → Dialog 弹出 5 张卡片
5. 加载预设场景 → 确认摄像机是否自适应实体包围盒（而非固定视角）
6. 检查 Toolbar 是否被 MenuBar 遮挡（如有遮挡需要修复 z-index）
</verification>

<success_criteria>
1. App.tsx 正确渲染 MenuBar/SnapshotManager/PresetSelector/SceneBanner，原有组件不受影响
2. [文件] → 快照管理 打开 SnapshotManager Sheet
3. [文件] → 预设场景库 打开 PresetSelector Dialog
4. 场景加载/重置后摄像机自适应实体包围盒（非固定默认位置）
5. 空场景时摄像机回到默认视角
6. TypeScript 编译通过，无运行时崩溃
</success_criteria>

<output>
After completion, create `.planning/phases/01-持久化与场景库/01-05-SUMMARY.md`
</output>
