---
phase: 01-simulation-core-3d-render
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
gap_closure: true
files_modified:
  - frontend/src/store/simulationSlice.ts
  - frontend/src/components/Scene3D.tsx
  - frontend/src/components/App.tsx
  - frontend/package.json
  - frontend/vite.config.ts
autonomous: true
requirements: [SIM-06]
user_setup: []
gaps_addressed:
  - CR-02  # BLOCKER — Reset功能无效
  - CR-01  # CRITICAL — 2D/3D WASM依赖不匹配
  - WR-02  # WARNING — objectCount语义不一致
  - WR-04  # WARNING — 不安全类型断言

must_haves:
  truths:
    - "用户点击 ↺ 重置 按钮后物体恢复初始位置和状态——模拟暂停，所有动态物体回到硬编码场景定义的初始坐标和旋转"
    - "应用仅加载 @react-three/rapier 所需的 3D WASM 引擎，不再下载无用的 rapier2d-compat (~1.5MB)"
    - "工具栏显示的物体计数为动态物体数量（11），而非总数（14）"
    - "WASM 加载失败（3D 引擎）能够被 ErrorFallback 正确捕获和展示"
    - "键盘快捷键在非 HTMLElement target 上不会因类型断言失败而误触发"
  artifacts:
    - path: "frontend/src/store/simulationSlice.ts"
      provides: "resetCounter 状态 + reset action 递增 resetCounter"
      contains: "resetCounter"
    - path: "frontend/src/components/Scene3D.tsx"
      provides: "Physics key={resetCounter} 强制重新挂载 + SceneInitializer 使用 dynamicCount"
      contains: "key={resetCounter}"
    - path: "frontend/src/components/App.tsx"
      provides: "移除 rapier2d-compat 导入 + Suspense 边界 + HTMLElement 类型守卫"
      contains: "Suspense"
    - path: "frontend/package.json"
      provides: "移除 @dimforge/rapier2d-compat 依赖声明"
    - path: "frontend/vite.config.ts"
      provides: "排除 @dimforge/rapier3d-compat（而非 2d-compat）于依赖预构建"
  key_links:
    - from: "simulationSlice.reset action"
      to: "Scene3D Physics key prop"
      via: "Zustand selector: useSimulationStore(s => s.resetCounter)"
      pattern: "key=\\{resetCounter\\}"
    - from: "App.tsx ready render path"
      to: "Scene3D + Toolbar"
      via: "Suspense fallback={LoadingScreen}"
      pattern: "<Suspense"
    - from: "App.tsx keyboard handler"
      to: "e.target"
      via: "instanceof HTMLElement type guard"
      pattern: "target instanceof HTMLElement"
---

<objective>
修复 Phase 1 验证发现的 1 个 BLOCKER 缺陷和代码审查中的 2 个 CRITICAL 问题及 2 个 WARNING 问题。

**BLOCKER (CR-02):** Reset 功能无效——点击"重置"仅暂停模拟，物体停留在掉落位置，不恢复初始场景。store 的 `reset` action 与 `pause` 行为完全相同。

**CRITICAL (CR-01):** App.tsx 显式加载并初始化 `@dimforge/rapier2d-compat` (2D 引擎)，而实际仿真使用的是 `@react-three/rapier`（内部依赖 3D 引擎）。导致：LoadingScreen 基于错误引擎的 init 状态隐藏；3D 引擎加载失败无法被 ErrorFallback 捕获；浪费约 1.5MB 带宽。

**WARNING (WR-02):** `SceneInitializer` 将 `SCENE_STATS.totalObjects`(14) 写入 store 的 `objectCount`，但 JSDoc 和 UI 语义描述为"动态物体数量"——实际动态物体为 11 个。

**WARNING (WR-04):** 键盘事件处理器使用 `e.target as HTMLElement` 不安全类型断言，无类型守卫。

Purpose: 将验证分数从 4/5 提升到 5/5，关闭所有 BLOCKER 和 CRITICAL 差距，确保 SIM-06 需求完全满足。
Output: 修复后的 5 个源文件，52 个已有测试继续通过，Reset 行为视觉可验证。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-simulation-core-3d-render/01-VERIFICATION.md
@.planning/phases/01-simulation-core-3d-render/01-REVIEW.md
@.planning/ROADMAP.md

### 现有代码接口摘要

From `frontend/src/store/simulationSlice.ts`:
```typescript
export interface SimulationSlice {
  isRunning: boolean;
  showDebug: boolean;
  fps: number;
  objectCount: number;
  play: () => void;
  pause: () => void;
  toggle: () => void;
  reset: () => void;           // 当前仅 set({ isRunning: false }) —— 需修复
  setShowDebug: (value: boolean) => void;
  setFps: (fps: number) => void;
  setObjectCount: (count: number) => void;
}
```

From `frontend/src/store/index.ts`:
```typescript
export const useSimulationStore = create<SimulationSlice>()((...args) => ({
  ...createSimulationSlice(...args),
}));
```

From `frontend/src/components/Scene3D.tsx`:
- `Scene3D` default export: R3F Canvas + Physics + OrbitControls + Grid + Gizmo
- `<Physics>` 组件（第188行）当前无 key 属性 —— 需添加 key={resetCounter}
- `SceneInitializer`（第127-136行）: `setObjectCount(SCENE_STATS.totalObjects)` —— 需改为 dynamicCount
- `FpsTracker`（第99-123行）: requestAnimationFrame 循环，无 DOM 输出

From `frontend/src/components/App.tsx`:
- 第2行: `import Rapier from '@dimforge/rapier2d-compat'` —— 需移除
- 第51-60行: `initWasm` 调用 `Rapier.init()` —— 需替换为 Suspense 方案
- 第78行: `const target = e.target as HTMLElement;` —— 需添加 instanceof 守卫
- 第131-136行: ready 状态渲染 `<Scene3D />` 无 Suspense 包裹

From `frontend/src/simulation/hardcodedScene.ts`:
```typescript
export const SCENE_STATS = {
  totalObjects: 14,      // 当前被 SceneInitializer 使用
  dynamicCount: 11,      // 正确的动态物体数量
  fixedCount: 3,         // 静态物体（斜面+平台）
};
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 Reset 功能——simulationSlice 添加 resetCounter + Scene3D Physics 添加 key 属性</name>
  <files>frontend/src/store/simulationSlice.ts, frontend/src/components/Scene3D.tsx</files>
  <read_first>
绝对不要重新读取 VERIFICATION.md 或 REVIEW.md（已在上文 context 中）。不要重新读取 hardcodedScene.ts（SCENE_STATS 接口已在上文 context 中提供）。仅读取本次任务需要修改的文件 simulationSlice.ts 和 Scene3D.tsx 各一次，一次性提取所有需要的信息。
  </read_first>
  <action>
**Step A — simulationSlice.ts 修改：**

1. 在 `SimulationSlice` 接口中添加新字段（在 `objectCount` 之后，actions 之前）：
   ```typescript
   /** 重置计数器——递增时触发 Physics 组件 key 变化，强制重新挂载物理世界 */
   resetCounter: number;
   ```

2. 在 `createSimulationSlice` 的初始状态中添加：
   ```typescript
   resetCounter: 0,
   ```

3. 修改 `reset` action（当前第51行），从 `reset: () => set({ isRunning: false })` 改为：
   ```typescript
   reset: () => set((state) => ({ isRunning: false, resetCounter: state.resetCounter + 1 })),
   ```
   含义：递增 resetCounter 触发下游 key 变化，同时暂停模拟（D-04: 重置后回到暂停状态）。

4. 更新 `reset()` 上方的 JSDoc 注释为 `/** 重置仿真——递增 resetCounter 触发物理世界重新挂载，恢复所有物体到初始位置/速度 */`

**Step B — Scene3D.tsx 修改：**

1. 在 `Scene3D` 组件函数体中（第154行 `const showDebug = ...` 之后），添加 resetCounter 订阅：
   ```typescript
   const resetCounter = useSimulationStore((s) => s.resetCounter);
   ```

2. 在 `<Physics>` 组件上添加 `key` 属性：
   ```typescript
   <Physics
     key={resetCounter}           // CR-02 fix: 重置时 key 变化 → React 卸载旧 Physics → 挂载新 Physics → 所有 RigidBody 回到 initialPosition
     timeStep={1 / 120}
     paused={!isRunning}
     debug={showDebug}
     gravity={[0, -9.81, 0]}
     interpolate={true}
   >
   ```

   注意：`key` 必须是 Physics 的直接属性，不能放在其子元素上。key 变化导致整个 Physics 子树重新挂载，所有 `<RigidBody>` 组件重新创建并使用 `INITIAL_SCENE_OBJECTS` 中定义的 `position` 和 `rotation`，实现真正的"重置"。

3. 确认 Physics 的 `key` 是第一个属性（放在 timeStep 之前），使意图清晰。

**重要约束：**
- **不要**添加任何新的 animation 或过渡效果——这是一次直接重置，不是动画。
- **不要**在 reset action 中手动遍历或重置 RigidBody 位置——key 驱动的重新挂载已足够。
- **不要**修改 `toggle`、`play` 或 `pause` action——它们的行为正确。
- **不要**删除 `INITIAL_SCENE_OBJECTS` 或 `SCENE_STATS`——它们被其他代码使用。
  </action>
  <verify>
  <automated>grep -n "resetCounter" frontend/src/store/simulationSlice.ts | grep -v '^#' | head -20</automated>
  
  预期输出（至少3处匹配）：
  - 接口定义行含 `resetCounter: number`
  - 初始状态行含 `resetCounter: 0`
  - reset action 行含 `resetCounter: state.resetCounter + 1`

  <automated>grep -n "key={resetCounter}" frontend/src/components/Scene3D.tsx</automated>
  
  预期输出：`<Physics` 属性行包含 `key={resetCounter}`

  <automated>grep -n "useSimulationStore.*resetCounter" frontend/src/components/Scene3D.tsx</automated>
  
  预期输出：Scene3D 组件体中存在 resetCounter 的 Zustand selector 调用
  </verify>
  <acceptance_criteria>
- `simulationSlice.ts` 的 `SimulationSlice` 接口包含 `resetCounter: number` 字段
- `reset` action 使用 `set((state) => ({ isRunning: false, resetCounter: state.resetCounter + 1 }))` 同时设置暂停并递增计数器
- `Scene3D.tsx` 的 `<Physics>` 组件具有 `key={resetCounter}` 属性（在 `timeStep` 之前）
- `Scene3D.tsx` 中 `resetCounter` 通过 `useSimulationStore((s) => s.resetCounter)` 从 store 获取
  </acceptance_criteria>
  <done>Reset 逻辑就绪——Physics key 变化将触发物理世界重新挂载，所有 RigidBody 回到初始 position/rotation。实际视觉验证需在浏览器中点击重置按钮后确认物体归位。</done>
</task>

<task type="auto">
  <name>Task 2: 修复 WASM 依赖不匹配——移除 rapier2d-compat + 添加 Suspense 边界 + 修复类型断言</name>
  <files>frontend/src/components/App.tsx, frontend/package.json, frontend/vite.config.ts</files>
  <read_first>
不要重新读取包管理文件或配置文件——所有 file_overview 信息已在 context 中。仅读取 App.tsx 一次（当前完整内容 137 行已在 context 中），直接基于 context 中提供的代码进行修改。不要读取 ErrorFallback.tsx 或 LoadingScreen.tsx——它们的接口未变。
  </read_first>
  <action>
**Step A — package.json 修改：**

1. 删除第12行的依赖项：
   ```json
   "@dimforge/rapier2d-compat": "^0.19.3",
   ```
   注意：删除整行，包括行尾逗号。保留该行前后的依赖项不变。

**Step B — vite.config.ts 修改：**

1. 第15行，将 `optimizeDeps.exclude` 数组中的项从：
   ```typescript
   exclude: ['@dimforge/rapier2d-compat'],
   ```
   改为：
   ```typescript
   exclude: ['@dimforge/rapier3d-compat'],
   ```
   原因：`@react-three/rapier` 内部依赖 `@dimforge/rapier3d-compat`——这是实际使用的 3D WASM 引擎。排除它于 Vite 预构建是 PITFALLS 文档推荐的做法（WASM 二进制不应被 Vite 预处理）。原配置排除的是不会再用到的 2D 引擎。

**Step C — App.tsx 修改（按顺序执行3处修改）：**

**C1. 移除 rapier2d-compat 导入（第2行）：**
删除：
```typescript
import Rapier from '@dimforge/rapier2d-compat';
```

**C2. 添加 Suspense 导入（修改第1行）：**
将：
```typescript
import { useState, useEffect, useCallback } from 'react';
```
改为：
```typescript
import { useState, useEffect, useCallback, Suspense } from 'react';
```

**C3. 重写 WASM 初始化逻辑（第50-72行区域）：**

删除 `initWasm` 函数（第51-60行）和启动初始化的 `useEffect`（第63-72行）中的 WASM 部分。

新的 `useEffect` 应简化为仅做 WebGL 检测：
```typescript
// ──── 启动初始化流程 ────
useEffect(() => {
  if (!checkWebGL()) {
    setErrorType('webgl');
    setAppState('error');
    return;
  }
  // 不再手动初始化 Rapier——@react-three/rapier 的 <Physics> 内部自行处理 WASM 加载。
  // 使用 Suspense 边界在 Physics 挂起时显示 LoadingScreen。
  setAppState('ready');
}, [checkWebGL]);
```

删除原有的 100ms 延迟 `setTimeout`——不再需要等待 DOM 渲染，因为 Suspense 边界会在 Physics 挂起时自动显示 LoadingScreen。

**C4. 添加 Suspense 边界包裹 Scene3D（第130-136行区域）：**

将：
```tsx
// appState === 'ready' — 3D 场景 + 工具栏
return (
  <>
    <Scene3D />
    <Toolbar />
  </>
);
```

改为：
```tsx
// appState === 'ready' — 3D 场景 + 工具栏
// CR-01 fix: Suspense 边界捕获 @react-three/rapier 的 WASM 加载挂起状态
// WR-01 fix: Physics 组件挂起时显示 LoadingScreen（而非白屏）
return (
  <>
    <Suspense fallback={<LoadingScreen />}>
      <Scene3D />
    </Suspense>
    <Toolbar />
  </>
);
```

注意：`<Toolbar />` 放在 `<Suspense>` **外部**——工具栏不依赖 Physics WASM，在加载期间即可显示。这比 LoadingScreen 覆盖全屏的用户体验更好。

**C5. 修复 WR-04：不安全类型断言（第78行）：**

将：
```typescript
const target = e.target as HTMLElement;
if (
  target.tagName === 'INPUT' ||
  target.tagName === 'TEXTAREA' ||
  target.tagName === 'SELECT' ||
  target.isContentEditable
) {
  return;
}
```

改为：
```typescript
const target = e.target;
if (!(target instanceof HTMLElement)) return;  // WR-04 fix: 类型守卫代替不安全断言
if (
  target.tagName === 'INPUT' ||
  target.tagName === 'TEXTAREA' ||
  target.tagName === 'SELECT' ||
  target.isContentEditable
) {
  return;
}
```

**步骤后操作：**
完成所有代码修改后，运行 `cd frontend && npm install` 更新 lockfile 以移除 rapier2d-compat 包。
  </action>
  <verify>
  <automated>grep -c "rapier2d-compat" frontend/package.json</automated>

  预期输出：`0`（该字符串在任何行都不再出现）

  <automated>grep "rapier3d-compat" frontend/vite.config.ts</automated>

  预期输出：第15行 `exclude: ['@dimforge/rapier3d-compat']`

  <automated>grep -n "from '@dimforge/rapier2d-compat'" frontend/src/components/App.tsx; echo "EXIT:$?"</automated>

  预期输出：`EXIT:1`（grep 无匹配——导入已被移除）

  <automated>grep -c "Suspense" frontend/src/components/App.tsx</automated>

  预期输出：至少为 `2`（导入行 + JSX 使用行）

  <automated>grep -c "target instanceof HTMLElement" frontend/src/components/App.tsx</automated>

  预期输出：至少为 `1`
  </verify>
  <acceptance_criteria>
- `package.json` 中不再包含 `@dimforge/rapier2d-compat` 依赖
- `vite.config.ts` 的 `optimizeDeps.exclude` 排除的是 `@dimforge/rapier3d-compat`
- `App.tsx` 不再从 `@dimforge/rapier2d-compat` 导入任何内容
- `App.tsx` 从 react 导入了 `Suspense`
- `App.tsx` 的 ready 渲染路径中 `Scene3D` 被 `<Suspense fallback={<LoadingScreen />}>` 包裹
- `App.tsx` 的键盘事件处理器使用 `e.target instanceof HTMLElement` 类型守卫
- WebGL 检测后 `appState` 直接设为 `'ready'`——不再有 100ms 延迟或 WASM 初始化等待
  </acceptance_criteria>
  <done>WASM 依赖对齐完成。应用仅加载 @react-three/rapier 内部的 3D 引擎。Suspense 边界在 Physics 挂起时显示 LoadingScreen，加载失败时 R3F 错误边界可被上层捕获。键盘快捷键类型安全。</done>
</task>

<task type="auto">
  <name>Task 3: 修复 objectCount 语义——SceneInitializer 使用 dynamicCount 而非 totalObjects</name>
  <files>frontend/src/components/Scene3D.tsx</files>
  <read_first>
此任务仅修改 Scene3D.tsx 中的一个值。Scene3D.tsx 完整内容（266行）已在 context 中。不要重新读取——直接基于 context 中的代码修改。
  </read_first>
  <action>
**修改 SceneInitializer 组件（第127-136行）：**

将第132行的：
```typescript
setObjectCount(SCENE_STATS.totalObjects);
```
改为：
```typescript
setObjectCount(SCENE_STATS.dynamicCount);
```

**说明：**
- `simulationSlice.ts` 第18行的 JSDoc 描述 `objectCount` 为"场景中动态物体数量"
- `SCENE_STATS.totalObjects` = 14（含 3 个 fixed 物体：2斜面+1平台）
- `SCENE_STATS.dynamicCount` = 11（实际受重力影响的动态物体）
- 此修改使存储的值与 JSDoc 描述一致，工具栏显示的"物体: 11"符合"动态物体数量"语义

**不要修改：**
- `simulationSlice.ts` 的 JSDoc——"动态物体数量"是正确的描述
- `hardcodedScene.ts` 的 `SCENE_STATS`——统计值本身是正确的
- `Toolbar.tsx` 的显示文案——"物体"标签保持简洁即可
  </action>
  <verify>
  <automated>grep "setObjectCount(SCENE_STATS.dynamicCount)" frontend/src/components/Scene3D.tsx</automated>

  预期输出：SceneInitializer 的 useEffect 回调中使用 `SCENE_STATS.dynamicCount` 而非 `SCENE_STATS.totalObjects`

  <automated>grep "setObjectCount(SCENE_STATS.totalObjects)" frontend/src/components/Scene3D.tsx; echo "EXIT:$?"</automated>

  预期输出：`EXIT:1`——旧值 `totalObjects` 不再出现
  </verify>
  <acceptance_criteria>
- `SceneInitializer` 调用 `setObjectCount(SCENE_STATS.dynamicCount)`，传入值 11（动态物体数）
- 不再使用 `SCENE_STATS.totalObjects`（14）设置 objectCount
- `simulationSlice.ts` 的 JSDoc "场景中动态物体数量"与实际存值一致
  </acceptance_criteria>
  <done>objectCount 语义修复完成。工具栏显示数字物体的数量（11），不含地面和静态斜面/平台。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| R3F Canvas | 3D 渲染边界——用户不可信输入不应影响 WebGL 上下文 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | App.tsx WASM 加载 | mitigate | 移除手动 Rapier.init()，改为 Suspense 边界——@react-three/rapier 内部处理 WASM 加载失败；Suspense fallback 显示 LoadingScreen 而非崩溃 |
| T-01-04-02 | Elevation of Privilege | App.tsx 键盘处理器 | accept | e.target 的类型守卫（instanceof HTMLElement）确保只在 HTMLElement 上访问 DOM 属性。事件监听器在 window 级别，但操作仅限于 store actions（无权限提升途径）。风险可接受。 |
</threat_model>

<verification>
运行所有已有测试确保修复不引入回归：

```bash
cd frontend && npx vitest run
```

预期：所有 52 个测试继续通过（3 test suites, 52 tests）。

新增 grep 检查：
- Reset: `grep -n "resetCounter" frontend/src/store/simulationSlice.ts` 至少3处匹配
- Reset: `grep "key={resetCounter}" frontend/src/components/Scene3D.tsx` 存在
- WASM: `grep "rapier2d-compat" frontend/package.json` 无匹配
- WASM: `grep "rapier3d-compat" frontend/vite.config.ts` 存在
- WASM: `grep "Suspense" frontend/src/components/App.tsx` 至少2处
- Type guard: `grep "target instanceof HTMLElement" frontend/src/components/App.tsx` 存在
- objectCount: `grep "setObjectCount(SCENE_STATS.dynamicCount)" frontend/src/components/Scene3D.tsx` 存在

TypeScript 编译检查：
```bash
cd frontend && npx tsc --noEmit --project tsconfig.app.json
```

预期：Phase 1 文件零错误（与修复前一致——8个预存错误在 api.ts，非 Phase 1 代码）。
</verification>

<success_criteria>
1. `simulationSlice.reset()` 递增 `resetCounter` 并设置 `isRunning: false`——行为上与 `pause()` 明确区分
2. `Scene3D` 的 `<Physics key={resetCounter}>` 在重置时强制重新挂载物理世界——所有物体恢复到 `INITIAL_SCENE_OBJECTS` 定义的初始位置和速度
3. `@dimforge/rapier2d-compat` 从项目依赖中完全移除——不再下载 2D WASM 二进制
4. `App.tsx` 使用 `<Suspense fallback={<LoadingScreen />}>` 包裹 `Scene3D`——@react-three/rapier 的 WASM 加载挂起时显示加载界面
5. `App.tsx` 键盘处理器使用 `e.target instanceof HTMLElement` 类型守卫——替换不安全的 `as HTMLElement` 断言
6. `SceneInitializer` 使用 `SCENE_STATS.dynamicCount`(11) 设置 `objectCount`——与 JSDoc "动态物体数量" 一致
7. 所有 52 个已有测试继续通过——无回归
</success_criteria>

<output>
After completion, create `.planning/phases/01-simulation-core-3d-render/01-04-SUMMARY.md`
</output>
