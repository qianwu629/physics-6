# Continue Here — Physis v2.0 Phase 1 持久化与场景库

**Paused:** 2026-05-04
**Milestone:** v2.0 力场与多维模拟
**Phase:** 1 — 持久化与场景库
**Stage:** Discuss complete (9/9 灰色地带锁定) → Ready to write CONTEXT.md → Plan phase

---

## Current Position

`/gsd-discuss-phase 1` 已完成所有交互。9 个灰色地带的决策已通过 AskUserQuestion 获得用户确认。**未写 CONTEXT.md**（因 context 已 75%，决定暂停）。

`.planning/phases/` 当前为空（v2.0 numbering reset 已生效，v1.0 phase 已归档至 `.planning/milestones/v1.0-phases/`）。

---

## 9 项已锁定决策（用户确认过的最终选项）

### 数据模型

**区域 1 — JSON Schema 范围：最小集**

只保存核心仿真数据，不保存可视化/UI/摄像机状态：

```json
{
  "schemaVersion": "1.0",
  "savedAt": "2026-05-04T...",
  "simulation": {
    "environment": { "gravity", "frictionScale", "restitutionScale", "drag" },
    "entities": [...],
    "constraints": [...]
  }
}
```

不保存的项：visualization toggles（trail/vector 全局开关）、metadata（name/description）、camera 位置、UI panel 折叠状态、运行时 trail/vector 数据。

**区域 2 — schemaVersion 不匹配：尝试加载 + 警告**

宽容模式：遇到不匹配的 schemaVersion 不直接拒绝，顶部显示黄色 banner 警告，未知字段忽略，缺字段用默认值。**不是**严格拒绝模式。

### 加载行为

**区域 3 — 加载 JSON 时：替换前确认 + 强制暂停 + 清空 trail**

- 加载前弹出确认对话框：「加载将替换当前场景，继续？」
- 加载后强制暂停 (isRunning=false)，仿真时间重置为 0
- 调用 trajectoryBuffer.reset() 清空所有实体的 trail 缓冲
- 摄像机回到默认 OrbitControls 位置（自适应新场景 bounding box）

### UI 形态

**区域 4 — 入口：顶部菜单栏 File 风格**

新增顶部 MenuBar：

```
[文件 ▾] [视图 ▾] [帮助 ▾]
  ├ 导出场景      → 直接下载 physis-scene-{savedAt}.json
  ├ 导入场景      → 原生 <input type="file" accept=".json"> picker
  ├ ─────────
  ├ 快照管理...   → 右侧 Drawer
  └ 预设场景库... → 弹出场景卡片选择器
```

**区域 5 — 快照面板：右侧滑出 Drawer**

不用 Dialog 弹窗（会遮挡 3D 视图），用右侧 Drawer 抽屉，保持仿真画面可见。Drawer 内容：5 个槽位列表（每行：名称/创建时间/实体数/[加载][重命名][删除]）+ 顶部「保存当前场景」输入区。

**区域 6 — 快照命名：用户输入名 + 不允许重名 + 可重命名**

- 保存时必须输入名称（1-30 字符，中英文/数字/空格/`-_.`）
- 不允许重名（创建时校验）
- 双击名称 inline 编辑重命名
- 覆盖前确认：「槽位 'XXX' 已有快照，覆盖？」

### 错误处理

**区域 8 — 分级处理（与区域 2 宽容模式一致）**

| 错误类型 | 反馈方式 |
|---------|---------|
| JSON 语法错 / 文件 >5MB | Modal 拒绝加载 |
| schemaVersion 不匹配 / Schema 校验失败 | 顶部黄色 banner 警告 + 尽力加载 |
| localStorage 配额满（QuotaExceededError）| 右上 Toast「存储空间不足，请删除旧快照」|
| 约束 entityId 引用失效 | 跳过该约束 + 提示「N 个约束已跳过」+ 继续加载其余 |
| localStorage 槽位损坏 | Modal「槽位 X 数据损坏，是否清除？」|

### 范围

**区域 7 — 6 个内置预设场景：5 个 v1.0 能力 + 第 6 个推迟**

Phase 1 交付：
1. **抛体运动** — 球体 + 初速度 (5,8,0)
2. **斜面滑块** — 30° 斜面 + 顶端盒块下滑
3. **自由落体堆叠** — 5 个球从不同高度落下
4. **弹簧振子** — 质量块 + 天花板锚点 + 弹簧
5. **双弹簧链** — 3 个质量块 + 4 根弹簧串联

第 6 个「点电荷力场示例」**推迟到 Phase 3** 力场系统实现后补上（需要 ForceField + Coulomb 力，v1.0 没有）。需求文档需记录该推迟。

**区域 9 — DEBT-04 范围：修复 Scene3D + 补 Phase 4 VERIFICATION.md**

- 修复 `frontend/src/components/__tests__/Scene3D.test.tsx` 9 个 baseline 失败用例（three.js Vector3 mock 缺失）
- 补回 Phase 4 缺失的 VERIFICATION.md，放到 `.planning/milestones/v1.0-phases/04-轨迹与矢量可视化/04-VERIFICATION.md`
- **不**做全面 mock 体系重写（超出 Phase 1 主题）

### 衍生决策

- **Drawer 仅含快照管理**：导出/导入/预设场景库分别走独立菜单项，不打 Tab 拼盘
- **预设场景实现方式**：作为 JSON 文件打包到 `frontend/src/presets/*.json`，与导入流程共享同一加载器（DRY）

---

## Resume Instructions

恢复时第一件事：

```bash
# 1. 创建 Phase 1 目录
mkdir -p .planning/phases/01-持久化与场景库

# 2. 把上面 9 项决策写入 CONTEXT.md（用 templates/context.md 模板格式）
# 路径: .planning/phases/01-持久化与场景库/01-CONTEXT.md
# 必填字段：Locked Decisions D-01-01..D-01-09，每项含 Decision/Why/How to apply

# 3. 启动 plan phase
/gsd-plan-phase 1
# 默认会先调研（持久化最佳实践、JSON Schema、Drawer 实现），然后规划 N 个 plan，最后验证
```

CONTEXT.md 草稿提示词（copy 给下次会话）：

> 「我有 9 项已锁定的决策（在 .continue-here.md），帮我写 .planning/phases/01-持久化与场景库/01-CONTEXT.md，使用 GSD context.md 模板，每项决策展开为 D-01-XX 编号 + Decision/Why/How to apply 三段。完成后建议 /gsd-plan-phase 1。」

---

## 阻塞器与注意事项

- **Phase 1 不依赖 Phase 2-6**，但需求映射 PERSIST-01..04 + DEBT-04 共 5 项
- 测试基线：`frontend/` 当前 22 套件 / 185 测试 PASS（含 Phase 5 新增 12 测试），9 个 Scene3D baseline 失败由 DEBT-04 解决
- localStorage 配额浏览器限制 5-10 MB，5 槽位 × ~200KB 单场景应该足够
- 预设场景的 JSON Schema 必须与导出/导入用同一份（schemaVersion 都是 "1.0"）

---

## Files Touched This Session

无（讨论阶段，未写代码）。下次会话首件事是创建 `.planning/phases/01-持久化与场景库/01-CONTEXT.md`。

---

*Paused via `/gsd-pause-work` at context 75%. STATE.md 未更新（因 v2.0 milestone 已是 planning 状态，无需变更）。*
